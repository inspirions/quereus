-- View updateability — Phase 1 + 1b working cases.
-- Single-source projection-and-filter views are writeable; writes route to the
-- base table. See docs/view-updateability.md.

-- ===================================
-- GreenMen: constant-FD default from the selection predicate (select *)
-- ===================================

create table Men (Name text primary key, Color text);
create view GreenMen as select * from Men where Color = 'green';

insert into GreenMen (Name) values ('Bob');

select * from Men order by Name;
→ [{"Name":"Bob","Color":"green"}]

select * from GreenMen order by Name;
→ [{"Name":"Bob","Color":"green"}]

-- ===================================
-- AdultsBare: constant-FD default for a projected-away column
-- ===================================

create table Adults (Name text primary key, Age integer, Country text);
create view AdultsBare as select Name, Age from Adults where Country = 'US';

insert into AdultsBare values ('Bob', 30);

select * from Adults order by Name;
→ [{"Name":"Bob","Age":30,"Country":"US"}]

select * from AdultsBare order by Name;
→ [{"Name":"Bob","Age":30}]

-- ===================================
-- Base-column default survives projection
-- ===================================

create table u (id integer primary key, name text default 'unknown');
create view uv as select id from u;

insert into uv values (5);

select * from u order by id;
→ [{"id":5,"name":"unknown"}]

-- ===================================
-- Rename projection
-- ===================================

create table users (id integer primary key, name text);
create view RV as select id as user_id, name as full_name from users;

insert into RV (user_id, full_name) values (1, 'Bob');

select * from users order by id;
→ [{"id":1,"name":"Bob"}]

select * from RV order by user_id;
→ [{"user_id":1,"full_name":"Bob"}]

-- ===================================
-- Single-source INVERSE-profile column write-through. `bp = b + 1` is a
-- non-identity invertible projection of iv_t.b; the single-source UPDATE path now
-- consumes the threaded `inverse` (parity with the multi-source join path), so
-- `update v set bp = 9` lowers to `set b = 9 - 1`, storing iv_t.b = 8, and the view
-- reads back bp = 9. An `opaque` computed column (`substr(s,1,1)` — string functions
-- carry no inverse profile) stays read-only, and the inverse column is NOT insertable
-- (an UPDATE-only claim). This matches the static column_info/view_info
-- is_updatable='YES'. See docs/vu-inverses.md § Scalar Invertibility.
-- ===================================

create table iv_t (id integer primary key, b integer null, s text);
insert into iv_t values (1, 7, 'hello'), (2, 20, 'world');
create view iv_v as select id, b + 1 as bp, substr(s, 1, 1) as s1 from iv_t;

-- read-through: bp = b + 1, s1 = first char.
select id, bp, s1 from iv_v order by id;
→ [{"id":1,"bp":8,"s1":"h"},{"id":2,"bp":21,"s1":"w"}]

-- UPDATE the inverse column: `bp = 9` lowers to `b = 9 - 1`, storing iv_t.b = 8.
update iv_v set bp = 9 where id = 1;
select id, b, s from iv_t order by id;
→ [{"id":1,"b":8,"s":"hello"},{"id":2,"b":20,"s":"world"}]

-- the write reads back through the forward transform: bp = 8 + 1 = 9.
select id, bp from iv_v order by id;
→ [{"id":1,"bp":9},{"id":2,"bp":21}]

-- the inverse is applied AFTER base-term substitution: `bp = id + 100` (id is a view
-- column) lowers to `b = (id + 100) - 1`, so for id=2 it stores b = 101.
update iv_v set bp = id + 100 where id = 2;
select id, b from iv_t order by id;
→ [{"id":1,"b":8},{"id":2,"b":101}]

-- the opaque computed column stays read-only (a string fn has no inverse profile).
update iv_v set s1 = 'X' where id = 1;
-- error: read-only

-- INSERT admits the inverse-ABSENT subset (identity + passthrough) but NOT an inverse
-- column: an explicit insert of the inverse `bp` is rejected (non-insertable even though
-- is_updatable='YES' — the envelope would store the value raw, with no hook to invert it).
insert into iv_v (id, bp) values (3, 5);
-- error: non-invertible

-- ===================================
-- Conflicting base-column assignments. An UPDATE that lands two SET targets on one
-- base column is rejected unconditionally (no value-agreement softening), whether
-- the duplication is direct, via an inverse-vs-base pair (`b`, `b+1 as bp`), or via a
-- duplicate rename (`b`, `b as b2`). The single-source spine surfaces a view-aware
-- message naming BOTH colliding view columns; the multi-source (join) spine falls
-- back to the base builder's generic "duplicate assignment" backstop (which sees only
-- base names). See docs/view-updateability.md § Diagnostics.
-- ===================================

create table cfl_t (id integer primary key, b integer null);
insert into cfl_t values (1, 99), (2, 50);

-- inverse-vs-base: `b` and `b + 1 as bp` both lower to base column b.
create view cfl_inv as select id, b, b + 1 as bp from cfl_t;
update cfl_inv set b = 5, bp = 100 where id = 1;
-- error: both target base column

-- the rejected UPDATE made no change (b stays 99, not last-wins 99 or 5).
select id, b from cfl_t order by id;
→ [{"id":1,"b":99},{"id":2,"b":50}]

-- duplicate rename: `b` and `b as b2` both lower to base column b.
create view cfl_dup as select id, b, b as b2 from cfl_t;
update cfl_dup set b = 1, b2 = 2 where id = 1;
-- error: both target base column

-- same-value collision still rejects (documents the no-softening semantics).
update cfl_dup set b = 5, b2 = 5 where id = 1;
-- error: both target base column

-- INSERT analogue: cfl_dup's `b` and `b2` both lower to base column b, so an
-- insert supplying both lands a duplicate base column in the single-source
-- spine's rewritten column list. It re-plans through the base builder and is
-- rejected by the generic "specified more than once" backstop (which names the
-- base column) — the INSERT dual of the multi-source UPDATE fallback above. See
-- ticket insert-family-duplicate-column-targets-silent-last-wins.
insert into cfl_dup (id, b, b2) values (3, 5, 6);
-- error: specified more than once

-- the rejected insert made no change (no id=3 row).
select id, b from cfl_t order by id;
→ [{"id":1,"b":99},{"id":2,"b":50}]

-- multi-source (join) same-side collision: `note` and `note collate nocase as note2`
-- both own the child base column note. The join spine routes both to the child's
-- per-base UPDATE, which the base builder re-plans and rejects via the generic backstop.
create table cfl_p (pid integer primary key, label text);
create table cfl_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references cfl_p(pid));
insert into cfl_p values (1, 'p1');
insert into cfl_c values (10, 1, 'alpha');
create view cfl_jv as
    select c.cid as cid, c.note as note, c.note collate nocase as note2
    from cfl_c c join cfl_p p on p.pid = c.pref;
update cfl_jv set note = 'x', note2 = 'y' where cid = 10;
-- error: duplicate assignment

-- ===================================
-- Single-source PASSTHROUGH-profile column write-through. `bc = b collate nocase`
-- and `nc = cast(n as integer)` (a no-op cast, same logical type) are identity-ON-VALUE
-- projections of pt_t's base columns. The single-source UPDATE path routes the FULL
-- writable-base set (identity + passthrough + inverse), so `update v set bc = V` lowers to
-- `set b = V`, storing the value VERBATIM (no inverse to apply), and the view reads it back
-- through the value-preserving transform. INSERT now consults the same writable-site map and
-- ALSO admits a passthrough column (stored verbatim) — aligning the single-source spine up to
-- the multi-source contract. An `opaque` computed column (`b || '!'` — concat carries no
-- inverse profile) stays read-only AND non-insertable. This matches the static
-- column_info/view_info is_updatable='YES'. See docs/vu-inverses.md § Scalar Invertibility.
-- ===================================

create table pt_t (id integer primary key, b text null, n integer null);
insert into pt_t values (1, 'hi', 5), (2, 'world', 20);
create view pt_v2 as select id, b collate nocase as bc, cast(n as integer) as nc, b || '!' as bo from pt_t;

-- read-through: bc = b, nc = n, bo = b || '!'.
select id, bc, nc, bo from pt_v2 order by id;
→ [{"id":1,"bc":"hi","nc":5,"bo":"hi!"},{"id":2,"bc":"world","nc":20,"bo":"world!"}]

-- UPDATE the collate passthrough: `bc = 'yo'` lowers to `b = 'yo'`, stored verbatim.
update pt_v2 set bc = 'yo' where id = 1;
select id, b, n from pt_t order by id;
→ [{"id":1,"b":"yo","n":5},{"id":2,"b":"world","n":20}]

-- UPDATE the no-op-cast passthrough: `nc = 99` lowers to `n = 99`, stored verbatim.
update pt_v2 set nc = 99 where id = 2;
select id, b, n from pt_t order by id;
→ [{"id":1,"b":"yo","n":5},{"id":2,"b":"world","n":99}]

-- the writes read back through the value-preserving forward transform.
select id, bc, nc from pt_v2 order by id;
→ [{"id":1,"bc":"yo","nc":5},{"id":2,"bc":"world","nc":99}]

-- the opaque computed column stays read-only (concat has no inverse profile).
update pt_v2 set bo = 'X' where id = 1;
-- error: read-only

-- INSERT a passthrough column on the single-source spine: now ADMITTED (this ticket aligns
-- single-source INSERT up to the multi-source contract). `bc` is a passthrough of base `b`,
-- so the value is stored VERBATIM; the omitted `nc` base column `n` is nullable and takes null.
-- At this point pt_t = (1,'yo',5),(2,'world',99).
insert into pt_v2 (id, bc) values (3, 'new');
select id, b, n from pt_t order by id;
→ [{"id":1,"b":"yo","n":5},{"id":2,"b":"world","n":99},{"id":3,"b":"new","n":null}]

-- read back through the view: bc reads 'new' verbatim; nc is null; bo recomputes b || '!'.
select id, bc, nc, bo from pt_v2 order by id;
→ [{"id":1,"bc":"yo","nc":5,"bo":"yo!"},{"id":2,"bc":"world","nc":99,"bo":"world!"},{"id":3,"bc":"new","nc":null,"bo":"new!"}]

-- IMPLICIT insert (no column list): the insertable set is (id, bc, nc) in view order — the
-- opaque computed column `bo` (no base lineage) is OMITTED rather than erroring, proving the
-- implicit-set alignment with multi-source. The two passthrough columns store verbatim.
insert into pt_v2 values (4, 'imp', 7);
select id, b, n from pt_t order by id;
→ [{"id":1,"b":"yo","n":5},{"id":2,"b":"world","n":99},{"id":3,"b":"new","n":null},{"id":4,"b":"imp","n":7}]
select id, bc, nc, bo from pt_v2 order by id;
→ [{"id":1,"bc":"yo","nc":5,"bo":"yo!"},{"id":2,"bc":"world","nc":99,"bo":"world!"},{"id":3,"bc":"new","nc":null,"bo":"new!"},{"id":4,"bc":"imp","nc":7,"bo":"imp!"}]

-- Filtered view + passthrough: the writable-site SET routing is orthogonal to the
-- conjoined body WHERE, so the filter still gates a passthrough write exactly as it
-- does a rename write (cf. AliasView). At this point pt_t =
-- (1,'yo',5),(2,'world',99),(3,'new',null),(4,'imp',7) (after the inserts above).
create view pt_vf as select id, b collate nocase as bc from pt_t where n > 10;

-- a visible row (id=2, n=99 > 10): the passthrough write lands in the base column.
update pt_vf set bc = 'zz' where id = 2;
select id, b from pt_t order by id;
→ [{"id":1,"b":"yo"},{"id":2,"b":"zz"},{"id":3,"b":"new"},{"id":4,"b":"imp"}]

-- a row outside the filter (id=1, n=5): the conjoined predicate matches no base row.
update pt_vf set bc = 'nope' where id = 1;
select id, b from pt_t order by id;
→ [{"id":1,"b":"yo"},{"id":2,"b":"zz"},{"id":3,"b":"new"},{"id":4,"b":"imp"}]

-- ===================================
-- Single-source ↔ multi-source parity for a PASSTHROUGH (collate) column. The same
-- `note collate nocase as note` projection of a base column is writable on BOTH mutation
-- spines: through a single-source view over one table, AND through a two-table inner-join
-- view. Confirms the two spines agree on an identical passthrough projection — the
-- single↔multi divergence this ticket closes. docs § Scalar Invertibility, § Inner Join.
-- ===================================

create table par_p (pid integer primary key, label text);
create table par_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references par_p(pid));
insert into par_p values (1, 'p1');
insert into par_c values (10, 1, 'alpha');

-- single-source view over par_c: the `note collate nocase` passthrough is writable.
create view par_ss as select cid, note collate nocase as note from par_c;
update par_ss set note = 'BETA' where cid = 10;
select cid, note from par_c order by cid;
→ [{"cid":10,"note":"BETA"}]

-- two-table inner-join view with the SAME passthrough projection: also writable.
create view par_mj as
    select c.cid as cid, c.note collate nocase as note, p.label as label
    from par_c c join par_p p on p.pid = c.pref;
update par_mj set note = 'gamma' where cid = 10;
select cid, note from par_c order by cid;
→ [{"cid":10,"note":"gamma"}]

-- ===================================
-- Single-source ↔ multi-source parity for a PASSTHROUGH (collate) column on INSERT.
-- The same `note collate nocase` passthrough is INSERTABLE on BOTH spines now — stored
-- VERBATIM — closing the single↔multi INSERT divergence this ticket targets (previously
-- single-source rejected the passthrough insert while multi-source admitted it). Fresh
-- tables keep the inserts free of the par_ss/par_mj seed rows above. An inverse-column
-- insert still rejects on single-source (the iv_v block, lines ~100), so the alignment is
-- precisely the inverse-absent subset. docs § Scalar Invertibility, § Inner Join.
-- ===================================

-- single-source: the passthrough `note` is inserted and stored verbatim in the base column.
create table pari_t (id integer primary key, note text);
create view pari_ss as select id, note collate nocase as note from pari_t;
insert into pari_ss (id, note) values (1, 'Alpha');
select id, note from pari_t order by id;
→ [{"id":1,"note":"Alpha"}]

-- two-table inner-join view exposing the SAME passthrough plus a directly-supplied shared
-- key: the passthrough `note` is likewise inserted verbatim into its owning base side.
create table pari_a (k integer primary key, note text);
create table pari_b (k integer primary key, bv text);
create view pari_mj as
    select a.k as k, a.note collate nocase as note, b.bv as bv
    from pari_a a join pari_b b on b.k = a.k;
insert into pari_mj (k, note, bv) values (1, 'Beta', 'x');
select k, note from pari_a order by k;
→ [{"k":1,"note":"Beta"}]
select k, bv from pari_b order by k;
→ [{"k":1,"bv":"x"}]
select k, note, bv from pari_mj order by k;
→ [{"k":1,"note":"Beta","bv":"x"}]

-- ===================================
-- Filter pass-through delete
-- ===================================

create table acct (id integer primary key, active integer, label text);
insert into acct values (5, 1, 'a'), (6, 0, 'b'), (7, 1, 'c');
create view ActiveUsers as select * from acct where active = 1;

delete from ActiveUsers where id = 5;

select id, active, label from acct order by id;
→ [{"id":6,"active":0,"label":"b"},{"id":7,"active":1,"label":"c"}]

-- ===================================
-- Filter pass-through update (the selection predicate is conjoined into WHERE)
-- ===================================

-- id=7 is visible (active=1): the update applies.
update ActiveUsers set label = 'C' where id = 7;
select id, label from acct order by id;
→ [{"id":6,"label":"b"},{"id":7,"label":"C"}]

-- id=6 is NOT visible (active=0): the conjoined predicate matches no base row.
update ActiveUsers set label = 'X' where id = 6;
select id, label from acct order by id;
→ [{"id":6,"label":"b"},{"id":7,"label":"C"}]

-- ===================================
-- Aliased base table + alias-qualified filter/projection: the alias qualifier
-- is normalized away when threading the body predicate into the base statement.
-- ===================================

create table al (id integer primary key, active integer, label text);
insert into al values (1, 1, 'a'), (2, 0, 'b'), (3, 1, 'c');
create view AliasView as select x.id as aid, x.label as alabel from al as x where x.active = 1;

-- visible row (active=1): update applies through the alias-qualified predicate.
update AliasView set alabel = 'Z' where aid = 1;
select id, label from al order by id;
→ [{"id":1,"label":"Z"},{"id":2,"label":"b"},{"id":3,"label":"c"}]

-- invisible row (active=0): conjoined alias predicate matches no base row.
update AliasView set alabel = 'X' where aid = 2;
select id, label from al order by id;
→ [{"id":1,"label":"Z"},{"id":2,"label":"b"},{"id":3,"label":"c"}]

delete from AliasView where aid = 3;
select id, label from al order by id;
→ [{"id":1,"label":"Z"},{"id":2,"label":"b"}]

-- ===================================
-- Phase 1b: per-statement mutation context threaded through the view
-- (one captured value stamped onto every row of the statement).
-- ===================================

create table evt (id integer primary key, created integer default now_ms) using memory with context (now_ms integer);
create view evt_v as select id from evt;

insert into evt_v with context now_ms = 12345 values (1), (2);

select * from evt order by id;
→ [{"id":1,"created":12345},{"id":2,"created":12345}]

-- ===================================
-- Phase 2: Multi-source key-preserving inner join — update / delete write-through.
-- The body is a two-table inner equi-join; writes route per owning base side via
-- a subquery over the join body (the row-identifying predicate). The owning side
-- can be the table whose PK the projection hides (ms_parent below).
-- See docs/vu-operators.md § Inner Join, docs/view-updateability.md § Multi-Base-Table Mutations.
-- ===================================

create table ms_parent (pid integer primary key, label text);
create table ms_child (cid integer primary key, pref integer, note text,
    foreign key (pref) references ms_parent(pid));
insert into ms_parent values (10, 'P10'), (20, 'P20');
insert into ms_child values (1, 10, 'a'), (2, 10, 'b'), (3, 20, 'c');

create view ms_jv as
    select c.cid as cid, c.note as note, p.label as label
    from ms_child c join ms_parent p on p.pid = c.pref;

-- read-through over the join
select * from ms_jv order by cid;
→ [{"cid":1,"note":"a","label":"P10"},{"cid":2,"note":"b","label":"P10"},{"cid":3,"note":"c","label":"P20"}]

-- UPDATE routed to the child side only (note is owned by ms_child).
update ms_jv set note = 'A' where cid = 1;
select cid, pref, note from ms_child order by cid;
→ [{"cid":1,"pref":10,"note":"A"},{"cid":2,"pref":10,"note":"b"},{"cid":3,"pref":20,"note":"c"}]

-- UPDATE routed to the parent side (label is owned by ms_parent, whose PK is
-- hidden by the projection — identified through the join). cid=1 joins pid=10.
update ms_jv set label = 'P10x' where cid = 1;
select * from ms_parent order by pid;
→ [{"pid":10,"label":"P10x"},{"pid":20,"label":"P20"}]

-- the parent update is visible to every child row joining pid=10.
select * from ms_jv order by cid;
→ [{"cid":1,"note":"A","label":"P10x"},{"cid":2,"note":"b","label":"P10x"},{"cid":3,"note":"c","label":"P20"}]

-- UPDATE touching BOTH sides -> two ordered base ops (FK-parent before FK-child).
update ms_jv set note = 'C', label = 'P20x' where cid = 3;
select cid, note from ms_child order by cid;
→ [{"cid":1,"note":"A"},{"cid":2,"note":"b"},{"cid":3,"note":"C"}]
select * from ms_parent order by pid;
→ [{"pid":10,"label":"P10x"},{"pid":20,"label":"P20x"}]

-- DELETE routes to the FK-many (child) side by default; the parent stays in place.
delete from ms_jv where cid = 2;
select cid from ms_child order by cid;
→ [{"cid":1},{"cid":3}]
select pid from ms_parent order by pid;
→ [{"pid":10},{"pid":20}]

-- the deleted child row is gone from the view.
select cid from ms_jv order by cid;
→ [{"cid":1},{"cid":3}]

-- BOTH-sides update predicated on the FK-PARENT's reassigned column, NO RETURNING —
-- the base-decomposition ordering clash. The FK-parent op runs first and rewrites
-- `label`; a live re-query of the join body would then find no 'P10' row, so the
-- FK-child op's identifying subquery would match nothing and silently drop the child
-- update. The up-front identity capture pins children 1,2 before any base op mutates,
-- so BOTH sides land. See view-mutation-multisource-both-sides-predicate-clash.
create table pc_parent (pid integer primary key, label text);
create table pc_child (cid integer primary key, pref integer, note text,
    foreign key (pref) references pc_parent(pid));
insert into pc_parent values (10, 'P10'), (20, 'P20');
insert into pc_child values (1, 10, 'a'), (2, 10, 'a'), (3, 20, 'b');
create view pc_jv as
    select c.cid as cid, c.note as note, p.label as label
    from pc_child c join pc_parent p on p.pid = c.pref;

update pc_jv set note = 'B', label = 'PY' where label = 'P10';
-- children 1,2 (pref 10) get note 'B'; parent 10 gets label 'PY' — neither dropped.
select cid, note from pc_child order by cid;
→ [{"cid":1,"note":"B"},{"cid":2,"note":"B"},{"cid":3,"note":"b"}]
select pid, label from pc_parent order by pid;
→ [{"pid":10,"label":"PY"},{"pid":20,"label":"P20"}]
select cid, note, label from pc_jv order by cid;
→ [{"cid":1,"note":"B","label":"PY"},{"cid":2,"note":"B","label":"PY"},{"cid":3,"note":"b","label":"P20"}]

-- BOTH-sides predicate clash through a view whose BODY carries its own WHERE.
-- The capture's identifying predicate is (user WHERE ∧ body WHERE), evaluated
-- BEFORE any base op mutates, so the body filter applies to the pre-mutation image.
-- bw_child 2 is hidden (active=0) yet shares parent pid 10 with the visible cid 1:
-- the capture pins per-CHILD-PK (only cid 1), so the FK-parent op rewriting `label`
-- on pid 10 leaves cid 2's note untouched even though both children share that parent.
create table bw_parent (pid integer primary key, label text);
create table bw_child (cid integer primary key, pref integer, note text, active integer,
    foreign key (pref) references bw_parent(pid));
insert into bw_parent values (10, 'P10'), (20, 'P20');
insert into bw_child values (1, 10, 'a', 1), (2, 10, 'a', 0), (3, 20, 'b', 1);
create view bw_jv as
    select c.cid as cid, c.note as note, p.label as label
    from bw_child c join bw_parent p on p.pid = c.pref
    where c.active = 1;

update bw_jv set note = 'B', label = 'PY' where label = 'P10';
-- Visible row matching label='P10' is only cid 1; cid 2 (active=0) is outside the
-- body filter so it is not captured and keeps note 'a'.
select cid, note from bw_child order by cid;
→ [{"cid":1,"note":"B"},{"cid":2,"note":"a"},{"cid":3,"note":"b"}]
select pid, label from bw_parent order by pid;
→ [{"pid":10,"label":"PY"},{"pid":20,"label":"P20"}]
select cid, note, label from bw_jv order by cid;
→ [{"cid":1,"note":"B","label":"PY"},{"cid":3,"note":"b","label":"P20"}]

-- ===================================
-- Multi-source rejections (still diagnosed-and-rejected this phase).
-- ===================================

-- A computed (non-invertible) join column is read-only.
create view ms_jv_c as
    select c.cid as cid, p.label || '!' as banner
    from ms_child c join ms_parent p on p.pid = c.pref;
update ms_jv_c set banner = 'z' where cid = 1;
-- error: read-only

-- RETURNING through a multi-source (join) delete is SHIPPED. The view row spans
-- both base tables, so it is produced by a re-query of the view captured BEFORE
-- the base delete fires; the deleted view-projected row is returned. (cid=1 joins
-- pid=10 → label 'P10x' from the earlier parent update; note 'A' from the child
-- update.)
delete from ms_jv where cid = 1 returning cid, note, label;
→ [{"cid":1,"note":"A","label":"P10x"}]

-- ===================================
-- Phase 2: adversarial coverage (qualifier rewriting, body-WHERE conjunction,
-- column-list overrides, and the remaining shape rejections).
-- ===================================

create table ax_parent (pid integer primary key, pv integer);
create table ax_child (cid integer primary key, pref integer, note text,
    foreign key (pref) references ax_parent(pid));
insert into ax_parent values (10, 100), (20, 200);
insert into ax_child values (1, 10, 'a'), (2, 20, 'b');

-- Swapped projection: the view column `pv` maps to the CHILD pk (ax_child.cid)
-- and the view column `cid` maps to the PARENT value (ax_parent.pv). A write must
-- route by lineage, not by name. `where cid = 100` filters on ax_parent.pv=100.
create view ax_swap as
    select p.pv as cid, c.cid as pv from ax_child c join ax_parent p on p.pid = c.pref;
select * from ax_swap order by pv;
→ [{"cid":100,"pv":1},{"cid":200,"pv":2}]
update ax_swap set pv = 7 where cid = 100;
select cid, pref from ax_child order by cid;
→ [{"cid":2,"pref":20},{"cid":7,"pref":10}]
select pid, pv from ax_parent order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- View-name-qualified predicate (jv.col) resolves to the base term.
create view ax_q as select c.cid as cid, c.note as note from ax_child c join ax_parent p on p.pid = c.pref;
update ax_q set note = 'z' where ax_q.cid = 7;
select cid, note from ax_child order by cid;
→ [{"cid":2,"note":"b"},{"cid":7,"note":"z"}]

-- The view body's own WHERE is conjoined: a predicate-less write only touches the
-- rows the body exposes (children whose parent pv > 150, i.e. pref = 20).
create view ax_filt as
    select c.cid as cid, c.note as note from ax_child c join ax_parent p on p.pid = c.pref where p.pv > 150;
select cid from ax_filt order by cid;
→ [{"cid":2}]
update ax_filt set note = 'F';
select cid, note from ax_child order by cid;
→ [{"cid":2,"note":"F"},{"cid":7,"note":"z"}]

-- The join-body parallel of the single-source blocks (t)/(v) further down: a computed
-- lineage column whose subquery correlates back to a SIDE by that side's alias. The
-- multi-source spine re-points an owning-side qualifier to the lowered per-side
-- statement's correlation alias at ANY depth (`stripSideQualifier`), so unlike the
-- single-source spine this shape never needed a depth-split fix — this block pins that
-- so the two spines cannot drift apart silently. De-correlated, `ax_lbl` would pin to
-- its first row ('one') for every row and `lbl = 'two'` would match nothing.
-- Own fixtures: the shared ax_parent / ax_child rows are asserted on by later blocks.
create table axc_p (pid integer primary key, pv integer);
create table axc_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references axc_p(pid));
create table axc_l (pid integer primary key, lbl text);
insert into axc_p values (10, 100), (20, 200);
insert into axc_c values (1, 10, 'a'), (2, 20, 'b');
insert into axc_l values (10, 'one'), (20, 'two');
create view axc_v as
    select c.cid as cid, c.note as note,
        (select lbl from axc_l where axc_l.pid = p.pid) as lbl
    from axc_c c join axc_p p on p.pid = c.pref;
select cid, lbl from axc_v order by cid;
→ [{"cid":1,"lbl":"one"},{"cid":2,"lbl":"two"}]
update axc_v set note = 'CORR' where lbl = 'two';
select cid, note from axc_c order by cid;
→ [{"cid":1,"note":"a"},{"cid":2,"note":"CORR"}]

-- Explicit view column-list override (create view v (a,b) as ...): writes route by
-- the override name to the underlying lineage.
create view ax_named (k, n) as select c.cid, c.note from ax_child c join ax_parent p on p.pid = c.pref;
update ax_named set n = 'g' where k = 7;
select cid, note from ax_child order by cid;
→ [{"cid":2,"note":"F"},{"cid":7,"note":"g"}]

-- Composite-PK side -> accepted: the identity capture projects one column per PK
-- column (k0_0=x.c1, k0_1=x.c2) and the base op's EXISTS correlates on both, so a
-- predicate on part of the key (c1 = 1) binds every composite-keyed row it matches.
create table ax_comp (c1 integer, c2 integer, pref integer, note text,
    primary key (c1, c2), foreign key (pref) references ax_parent(pid));
insert into ax_comp values (1, 1, 10, 'a'), (1, 2, 20, 'b'), (2, 1, 10, 'c');
create view ax_jv_comp as
    select x.c1 as c1, x.note as note, p.pv as pv from ax_comp x join ax_parent p on p.pid = x.pref;
update ax_jv_comp set note = 'q' where c1 = 1;
select c1, c2, note from ax_comp order by c1, c2;
→ [{"c1":1,"c2":1,"note":"q"},{"c1":1,"c2":2,"note":"q"},{"c1":2,"c2":1,"note":"c"}]

-- ...but a single-side write to the SIMPLE-PK side is allowed even when the OTHER
-- (untouched) side has a COMPOSITE PK: only the touched side's PK backs the identity
-- capture, so the composite key on the untouched side is never required. (Parity the
-- per-side key capture preserves — view-mutation-retire-ast-roundtrip.)
create table ax_cpk_p (a integer, b integer, lbl text, primary key (a, b));
create table ax_spk_c (sid integer primary key, pa integer, note text);
insert into ax_cpk_p values (1, 1, 'L1');
insert into ax_spk_c values (10, 1, 'n');
create view ax_jv_simpside as
    select c.sid as sid, c.note as note, p.lbl as lbl
    from ax_spk_c c join ax_cpk_p p on p.a = c.pa;
-- run
update ax_jv_simpside set note = 'm' where sid = 10;
-- run
select sid, note, lbl from ax_jv_simpside order by sid;
→ [{"sid":10,"note":"m","lbl":"L1"}]

-- Cross-source SET value (assign from the OTHER base table) -> now ACCEPTED when the
-- read column has `base` lineage: the partner value (p.pv) is projected into the
-- __vmupd_keys capture under a `srcN` alias and read back correlated by the owning
-- side's PK (the child's cid). For cid=2 (joined parent pid=20, pv=200) the child's
-- cval becomes 200; cid=1 is untouched, and the parent base is unchanged.
create table ax_xs_p (pid integer primary key, pv integer);
create table ax_xs_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references ax_xs_p(pid));
insert into ax_xs_p values (10, 100), (20, 200);
insert into ax_xs_c values (1, 10, 1), (2, 20, 2);
create view ax_jv_x as
    select c.cid as cid, c.cval as cval, p.pv as pv from ax_xs_c c join ax_xs_p p on p.pid = c.pref;
update ax_jv_x set cval = pv where cid = 2;
select cid, cval from ax_xs_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":200}]
select pid, pv from ax_xs_p order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- TWO distinct cross-source leaves in one statement -> each partner column mints its own
-- `srcN` projection, but all reads of `__vmupd_keys` within a base op resolve to ONE
-- injected key-ref instance (a shared DAG). Both shapes must lower correctly: a two-leaf
-- expression (`cw = pv + pw`) and two single-leaf assignments (`cv = pv, cw = pw`). For
-- cid=2 (joined parent pid=20: pv=200, pw=2000) the expression case sets cw=2200; the
-- two-assignment case sets cv=200, cw=2000.
create table ax_xs2_p (pid integer primary key, pv integer, pw integer);
create table ax_xs2_c (cid integer primary key, pref integer, cv integer, cw integer,
    foreign key (pref) references ax_xs2_p(pid));
insert into ax_xs2_p values (10, 100, 1000), (20, 200, 2000);
insert into ax_xs2_c values (1, 10, 0, 0), (2, 20, 0, 0);
create view ax_jv_x2 as
    select c.cid as cid, c.cv as cv, c.cw as cw, p.pv as pv, p.pw as pw
    from ax_xs2_c c join ax_xs2_p p on p.pid = c.pref;
update ax_jv_x2 set cw = pv + pw where cid = 1;
update ax_jv_x2 set cv = pv, cw = pw where cid = 2;
select cid, cv, cw from ax_xs2_c order by cid;
→ [{"cid":1,"cv":0,"cw":1100},{"cid":2,"cv":200,"cw":2000}]

-- Cross-source `set` on a COMPOSITE-PK owning side: the correlated read-back conjoins one
-- equality per owning PK column (k.k0_0 = k1 and k.k0_1 = k2), so only the identified row
-- (k1=1, k2=2 — joined parent pp=20, pv=200) takes the partner value; (1,1) is untouched.
create table ax_xscpk_k (k1 integer, k2 integer, v integer, ref integer, primary key (k1, k2));
create table ax_xscpk_p (pp integer primary key, pv integer);
insert into ax_xscpk_p values (10, 100), (20, 200);
insert into ax_xscpk_k values (1, 1, 0, 10), (1, 2, 0, 20);
create view ax_xscpk_v as
    select k.k1 as k1, k.k2 as k2, k.v as v, p.pv as pv from ax_xscpk_k k join ax_xscpk_p p on p.pp = k.ref;
update ax_xscpk_v set v = pv where k1 = 1 and k2 = 2;
select k1, k2, v from ax_xscpk_k order by k1, k2;
→ [{"k1":1,"k2":1,"v":0},{"k1":1,"k2":2,"v":200}]

-- Cross-source `set` on a SELF-JOIN owning side: the owning side is alias-keyed, so the
-- read-back correlates on the assigned alias's PK. `set sal = msal where id = 1` copies the
-- manager (alias `m`, id=2) salary onto employee id=1; id=2 is untouched.
create table ax_xs_emp (id integer primary key, mgr integer, sal integer);
insert into ax_xs_emp values (1, 2, 50), (2, 2, 90);
create view ax_xs_self as select e.id as id, e.sal as sal, m.sal as msal from ax_xs_emp e join ax_xs_emp m on e.mgr = m.id;
update ax_xs_self set sal = msal where id = 1;
select id, sal from ax_xs_emp order by id;
→ [{"id":1,"sal":90},{"id":2,"sal":90}]

-- ...but a cross-source value reading a COMPUTED (non-base) partner column stays
-- rejected: its value is not recoverable from a captured base column.
create view ax_jv_xc as
    select c.cid as cid, c.cval as cval, (p.pv * 2) as pvc from ax_xs_c c join ax_xs_p p on p.pid = c.pref;
update ax_jv_xc set cval = pvc where cid = 1;
-- error: cannot write through

-- Cross-source `set` in the 1:MANY direction -> REJECTED at PLAN TIME with a diagnostic
-- naming the cross-source ambiguity. The owning (assigned) side `p` joins TWO children, so
-- the partner read `cv` is multi-valued: __vmupd_keys holds one srcN row per joined pair and
-- the correlated read-back (keyed by the owner's pid) returns >1 row. The cardinality proof
-- fails — the join only pins xs1n_c.pref, which is no unique key of xs1n_c — so the write is
-- rejected here rather than failing at runtime with "Scalar subquery returned more than one
-- row".
create table xs1n_p (pid integer primary key, pv integer);
create table xs1n_c (cid integer primary key, pref integer, cv integer,
    foreign key (pref) references xs1n_p(pid));
insert into xs1n_p values (10, 100);
insert into xs1n_c values (1, 10, 11), (2, 10, 22);
create view xs1n_v as
    select p.pid as pid, p.pv as pv, c.cv as cv
    from xs1n_p p join xs1n_c c on c.pref = p.pid;
update xs1n_v set pv = cv where pid = 10;
-- error: assigned side joins more than one
-- ...and the statement never executed (plan-time reject): xs1n_p is unchanged.
select pid, pv from xs1n_p order by pid;
→ [{"pid":10,"pv":100}]

-- Cross-source `set` where the partner's join column is UNIQUE but NOT the primary key ->
-- ACCEPTED via the unique-constraint branch of the cardinality proof. The join pins
-- xs1u_p.pcode (a non-partial UNIQUE), so the owning child joins at most one parent. For
-- cid=1 (joined parent pcode=500, pv=100) cv becomes 100; cid=2 (pcode=600, pv=200) untouched.
create table xs1u_p (pid integer primary key, pcode integer unique, pv integer);
create table xs1u_c (cid integer primary key, cref integer, cv integer);
insert into xs1u_p values (10, 500, 100), (20, 600, 200);
insert into xs1u_c values (1, 500, 1), (2, 600, 2);
create view xs1u_v as
    select c.cid as cid, c.cv as cv, p.pv as pv
    from xs1u_c c join xs1u_p p on p.pcode = c.cref;
update xs1u_v set cv = pv where cid = 1;
select cid, cv from xs1u_c order by cid;
→ [{"cid":1,"cv":100},{"cid":2,"cv":2}]

-- The PARTIAL-unique-key variant of this cardinality proof (rejected, because a partial
-- unique key bounds uniqueness only inside its predicate) lives in
-- 93.4.1-view-mutation-partial-unique-cardinality — a partial UNIQUE can only be spelled
-- as `create unique index … where …`, so it needs standalone index DDL.

-- Self-join body -> accepted: `e` and `m` are distinct aliases of one base table,
-- routed by alias (the column's producing scan), not table name. `mv = m.v` routes to
-- alias `m`'s side; `where id = 1` identifies via alias `e` (id = e.id). For id=1
-- (mgr=2) the joined `m` is the row id=2, so its `v` is set to 9.
create table ax_emp (id integer primary key, mgr integer, v integer);
insert into ax_emp values (1, 2, 10), (2, 9, 20), (9, 9, 30);
create view ax_self as select e.id as id, m.v as mv from ax_emp e join ax_emp m on m.id = e.mgr;
update ax_self set mv = 9 where id = 1;
select id, mgr, v from ax_emp order by id;
→ [{"id":1,"mgr":2,"v":10},{"id":2,"mgr":9,"v":9},{"id":9,"mgr":9,"v":30}]

-- Three-table inner join -> accepted: the n-way fan-out routes each assignment to its
-- owning base. `bv = b.v` routes to the third side (ax_b); `where cid = 2` identifies
-- through the join (b.bid = c.cid = 2).
create table ax_b (bid integer primary key, v integer);
insert into ax_b values (2, 50), (7, 60);
create view ax_three as
    select c.cid as cid, p.pv as pv, b.v as bv
    from ax_child c join ax_parent p on p.pid = c.pref join ax_b b on b.bid = c.cid;
update ax_three set bv = 9 where cid = 2;
select bid, v from ax_b order by bid;
→ [{"bid":2,"v":9},{"bid":7,"v":60}]

-- MULTI-HOP cross-source `set` -> conservatively REJECTED. In ax_three the owner `p`
-- (ax_parent) and the partner `b` (ax_b) are not directly equi-joined — they connect only
-- transitively through `c` (ax_child). The cardinality proof pins no ax_b column to a
-- per-owner value, so it cannot prove the owner joins at most one ax_b row and rejects at
-- plan time (this only over-rejects; a transitive value-determinacy proof is a follow-up).
update ax_three set pv = bv where cid = 2;
-- error: assigned side joins more than one
-- ...plan-time reject: ax_parent is unchanged.
select pid, pv from ax_parent order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- ========================================================================
-- Cross-source SET / authored read of a partner column projected UNQUALIFIED.
-- A join-view body may project a partner-side column BARE when the name is
-- unambiguous across the sides. The bare base-term lineage leaf is resolved to its
-- owning side by UNIQUE column ownership (the exact rule join-condition operands
-- use) and SIDE-ALIAS-QUALIFIED when the view-column substitution injects it
-- (makeSideQualifyScope — the multi-source analog of the single-source
-- correlation-qualification of substituted terms), so a partner-owned read rides
-- the IDENTICAL `__vmupd_keys` capture machinery as a qualified `b.y` read — at ANY
-- nesting depth — instead of mis-routing into the owning side's base UPDATE with a
-- generic `Column not found`. The qualifier strip itself stays purely syntactic
-- (qualifier-driven); a bare leaf reaching it is only ever a user-authored
-- local/unknown name. The qualified analogues above (ax_jv_x, ax_xscpk_v, xs1n_v,
-- xs1u_v, mj_v) all still hold.
-- See docs/vu-operators.md § Inner Join, docs/view-updateability.md cross-source `set`.
-- ========================================================================

-- (uq-1) headline: partner column `pv` projected UNQUALIFIED, read cross-source into the
-- child's `cval` (the unqualified analogue of ax_jv_x). For cid=2 (joined parent pid=20,
-- pv=200) cval becomes 200; cid=1 untouched; the parent base is unchanged.
create table uq_p (pid integer primary key, pv integer);
create table uq_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq_p(pid));
insert into uq_p values (10, 100), (20, 200);
insert into uq_c values (1, 10, 1), (2, 20, 2);
create view uq_jv as
    select c.cid as cid, cval, pv from uq_c c join uq_p p on p.pid = c.pref;
update uq_jv set cval = pv where cid = 2;
select cid, cval from uq_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":200}]
select pid, pv from uq_p order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- (uq-2) owning-side UNQUALIFIED read: both the assigned and the read column live on the
-- SAME (owning) side, the read projected bare → resolves to the owning side → PLAIN
-- qualifier strip, value lands locally with NO `srcN` capture minted (byte-identical to a
-- qualified same-side read). `cval2 = cval` copies within the child.
create table uq2_p (pid integer primary key, pv integer);
create table uq2_c (cid integer primary key, pref integer, cval integer, cval2 integer,
    foreign key (pref) references uq2_p(pid));
insert into uq2_p values (10, 100);
insert into uq2_c values (1, 10, 7, 0);
create view uq2_jv as
    select c.cid as cid, cval, cval2, pv from uq2_c c join uq2_p p on p.pid = c.pref;
update uq2_jv set cval2 = cval where cid = 1;
select cid, cval, cval2 from uq2_c order by cid;
→ [{"cid":1,"cval":7,"cval2":7}]

-- (uq-3) COMPOSITE-PK owning side, partner `pv` projected UNQUALIFIED: the correlated
-- read-back still conjoins one equality per owning PK column (k.k0_0 = k1 and k.k0_1 = k2),
-- so only the identified row (k1=1, k2=2 — joined parent pp=20, pv=200) takes the value.
create table uq3_k (k1 integer, k2 integer, v integer, ref integer, primary key (k1, k2));
create table uq3_p (pp integer primary key, pv integer);
insert into uq3_p values (10, 100), (20, 200);
insert into uq3_k values (1, 1, 0, 10), (1, 2, 0, 20);
create view uq3_v as
    select k.k1 as k1, k.k2 as k2, v, pv from uq3_k k join uq3_p p on p.pp = k.ref;
update uq3_v set v = pv where k1 = 1 and k2 = 2;
select k1, k2, v from uq3_k order by k1, k2;
→ [{"k1":1,"k2":1,"v":0},{"k1":1,"k2":2,"v":200}]

-- (uq-4) partner's join column is UNIQUE but NOT the PK, partner `pv` projected UNQUALIFIED
-- → accepted via the unique-constraint branch of the cardinality proof. For cid=1 (joined
-- parent pcode=500, pv=100) cv becomes 100; cid=2 (pcode=600, pv=200) untouched.
create table uq4_p (pid integer primary key, pcode integer unique, pv integer);
create table uq4_c (cid integer primary key, cref integer, cv integer);
insert into uq4_p values (10, 500, 100), (20, 600, 200);
insert into uq4_c values (1, 500, 1), (2, 600, 2);
create view uq4_v as
    select c.cid as cid, cv, pv from uq4_c c join uq4_p p on p.pcode = c.cref;
update uq4_v set cv = pv where cid = 1;
select cid, cv from uq4_c order by cid;
→ [{"cid":1,"cv":100},{"cid":2,"cv":2}]

-- (uq-5) `srcN` dedup: one statement reads the SAME partner column both QUALIFIED
-- (`pvq` ← p.pv) and UNQUALIFIED (`pvu` ← bare pv). Qualifying the bare ref with the
-- partner alias BEFORE registering the capture maps both to the dedup key `p.pv`, so a
-- single `srcN` projection backs both reads. (The result is value-equivalent whether one
-- or two srcN are minted — the assertion proves both routes resolve consistently in one
-- statement; the count itself is an internal invariant, not separately asserted.) For
-- cid=1 (joined parent pid=20, pv=200) both cv and cw read 200.
create table uq5_p (pid integer primary key, pv integer);
create table uq5_c (cid integer primary key, pref integer, cv integer, cw integer,
    foreign key (pref) references uq5_p(pid));
insert into uq5_p values (10, 100), (20, 200);
insert into uq5_c values (1, 20, 0, 0);
create view uq5_v as
    select c.cid as cid, c.cv as cv, c.cw as cw, p.pv as pvq, pv as pvu
    from uq5_c c join uq5_p p on p.pid = c.pref;
update uq5_v set cv = pvq, cw = pvu where cid = 1;
select cid, cv, cw from uq5_c order by cid;
→ [{"cid":1,"cv":200,"cw":200}]

-- (uq-6) authored-inverse put reading `new.<UNQUALIFIED partner column>`: the put on side
-- `b` (writing bv) reads `new.av` where `av` is projected BARE off side `a`. The forward
-- read of the non-assigned partner column rides the same capture as a cross-source SET
-- value (the unqualified analogue of mj_v). `bv2 = 'y'` ⇒ bv = 'y' || '+' || (captured av).
create table uq6_a (pid integer primary key, av text);
create table uq6_b (bid integer primary key, pid integer not null references uq6_a(pid), bv text);
insert into uq6_a values (1, 'a');
insert into uq6_b values (10, 1, 'b');
create view uq6_v as select uq6_a.pid as pid, uq6_b.bid as bid, av,
        bv || '' as bv2 with inverse (bv = new.bv2 || '+' || new.av)
    from uq6_a join uq6_b on uq6_a.pid = uq6_b.pid;
update uq6_v set bv2 = 'y' where pid = 1;
select bv from uq6_b;
→ [{"bv":"y+a"}]

-- (uq-7) 1:MANY direction with an UNQUALIFIED partner read → still rejected at PLAN TIME
-- (the cardinality gate resolves the bare ref via resolveColumnSide). Owning side `p` joins
-- TWO children, so the partner read `cv` is multi-valued and the proof fails.
create table uq7_p (pid integer primary key, pv integer);
create table uq7_c (cid integer primary key, pref integer, cv integer,
    foreign key (pref) references uq7_p(pid));
insert into uq7_p values (10, 100);
insert into uq7_c values (1, 10, 11), (2, 10, 22);
create view uq7_v as
    select p.pid as pid, pv, cv from uq7_p p join uq7_c c on c.pref = p.pid;
update uq7_v set pv = cv where pid = 10;
-- error: assigned side joins more than one
-- ...plan-time reject: uq7_p is unchanged.
select pid, pv from uq7_p order by pid;
→ [{"pid":10,"pv":100}]

-- (uq-8) COMPUTED (non-base) partner column read UNQUALIFIED → still rejected `no-inverse`
-- (gateCrossSourceReads walks the pre-substitution view term, so it sees the computed view
-- column regardless of qualification). The capture cannot carry a non-base value.
create table uq8_p (pid integer primary key, pv integer);
create table uq8_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq8_p(pid));
insert into uq8_p values (10, 100);
insert into uq8_c values (1, 10, 1);
create view uq8_v as
    select c.cid as cid, cval, (pv * 2) as pvc from uq8_c c join uq8_p p on p.pid = c.pref;
update uq8_v set cval = pvc where cid = 1;
-- error: cannot write through

-- (uq-9) SCOPE GUARD: a USER-AUTHORED bare column inside a NESTED value subquery binds to
-- that subquery's OWN from-source, NOT the view sides — even when its name collides with a
-- partner base column. `psecret` is deliberately NOT exposed by the view, so the scope-aware
-- view-column substitution never touches it; and the qualifier strip is purely syntactic —
-- it strips/routes only ALIAS-QUALIFIED refs (every base-term lineage leaf arrives
-- side-alias-qualified from the substitution) and never resolves a bare name against the
-- sides. Here `psecret` belongs to uq9_t (the subquery FROM), so cval must read 777
-- (uq9_t.psecret), NOT 888 (the joined parent uq9_p.psecret). Regression for the
-- cross-source-unqualified-body-projection review (a name-resolution decision must not ride
-- the scope-unaware descent at depth).
create table uq9_p (pid integer primary key, pv integer, psecret integer);
create table uq9_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq9_p(pid));
create table uq9_t (tid integer primary key, psecret integer);
insert into uq9_p values (10, 100, 999), (20, 200, 888);
insert into uq9_c values (1, 10, 1), (2, 20, 2);
insert into uq9_t values (1, 777);
create view uq9_v as
    select c.cid as cid, cval, pv from uq9_c c join uq9_p p on p.pid = c.pref;
update uq9_v set cval = (select psecret from uq9_t where tid = 1) where cid = 2;
select cid, cval from uq9_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":777}]

-- (uq-10) NESTED cross-source read of a BARE-projected partner column: the view column
-- `pv` (lineage: bare `pv`, owned by the partner side p) is referenced inside a value
-- SUBQUERY. The substitution injects the lineage side-alias-qualified (`p.pv`), so the
-- strip's qualified routing carries it through the `__vmupd_keys` capture at depth —
-- exactly like a top-level read (uq-1). For cid=2 (joined parent pv=200):
-- sum(tv) where tv <= 200 → 100 + 150 = 250. Other rows and the partner base unchanged.
create table uq10_p (pid integer primary key, pv integer);
create table uq10_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq10_p(pid));
create table uq10_t (tid integer primary key, tv integer);
insert into uq10_p values (10, 100), (20, 200);
insert into uq10_c values (1, 10, 1), (2, 20, 2);
insert into uq10_t values (1, 100), (2, 150), (3, 300);
create view uq10_v as
    select c.cid as cid, cval, pv from uq10_c c join uq10_p p on p.pid = c.pref;
update uq10_v set cval = (select sum(tv) from uq10_t where tv <= pv) where cid = 2;
select cid, cval from uq10_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":250}]
select pid, pv from uq10_p order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- (uq-11) COMPUTED-lineage collision — the silent-wrong injection-qualification fixes:
-- the view computes `pc = pv * 2` with `pv` written BARE in the body, and the value
-- subquery's FROM (uq11_t) ALSO has a `pv` column. The reference `pc` is substituted
-- (uq11_t has no `pc`, so it is not shadowed), and the injected lineage leaf `pv` must
-- evaluate against the JOINED PARTNER row (qualified `p.pv`), not re-bind to uq11_t.pv
-- by innermost-scope rules. For cid=2: pv=200 → pc=400 → max(tv) where tv < 400 = 350.
-- (A re-bind to uq11_t.pv=5 would compute pc=10 → max(tv) where tv < 10 = null.)
create table uq11_p (pid integer primary key, pv integer);
create table uq11_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq11_p(pid));
create table uq11_t (tid integer primary key, tv integer, pv integer);
insert into uq11_p values (10, 100), (20, 200);
insert into uq11_c values (1, 10, 1), (2, 20, 2);
insert into uq11_t values (1, 50, 5), (2, 350, 5), (3, 500, 5);
create view uq11_v as
    select c.cid as cid, cval, (pv * 2) as pc from uq11_c c join uq11_p p on p.pid = c.pref;
update uq11_v set cval = (select max(tv) from uq11_t where tv < pc) where cid = 2;
select cid, cval from uq11_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":350}]

-- (uq-12) TAINTED scope: a nested bare reference to a BARE-projected partner view column
-- under a `select *` subquery source cannot be proven correlated (the source's columns are
-- not statically resolvable, so shadowing is unprovable) → structured reject, the
-- multi-source analog of single-source case (f). Previously this shape fell out as a
-- confusing generic resolution error; the scope-aware substitution now owns the decision.
create table uq12_p (pid integer primary key, pv integer);
create table uq12_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq12_p(pid));
create table uq12_aux (x integer);
insert into uq12_p values (10, 100);
insert into uq12_c values (1, 10, 1);
insert into uq12_aux values (100);
create view uq12_v as
    select c.cid as cid, cval, pv from uq12_c c join uq12_p p on p.pid = c.pref;
update uq12_v set cval = (select s.x from (select * from uq12_aux) as s where s.x = pv) where cid = 1;
-- error: cannot be proven correlated

-- (uq-13) NESTED 1:many cardinality: the uq-7 shape (owning side p joins TWO children)
-- with the bare partner read `cv` moved INSIDE a value subquery → still rejected at PLAN
-- time. The injected lineage arrives qualified (`c.cv`), so the strip's rewrite site —
-- where the cardinality gate fires — covers the nested read identically to a top-level one.
create table uq13_p (pid integer primary key, pv integer);
create table uq13_c (cid integer primary key, pref integer, cv integer,
    foreign key (pref) references uq13_p(pid));
create table uq13_t (tid integer primary key, tv integer);
insert into uq13_p values (10, 100);
insert into uq13_c values (1, 10, 11), (2, 10, 22);
insert into uq13_t values (1, 5);
create view uq13_v as
    select p.pid as pid, pv, cv from uq13_p p join uq13_c c on c.pref = p.pid;
update uq13_v set pv = (select tv from uq13_t where tv < cv) where pid = 10;
-- error: assigned side joins more than one
-- ...plan-time reject: uq13_p is unchanged.
select pid, pv from uq13_p order by pid;
→ [{"pid":10,"pv":100}]

-- (uq-14) ENCAPSULATION GUARD at depth: a nested bare reference to a partner base column
-- the view does NOT project (`psecret`), absent from the inner FROM too, must FAIL loudly
-- at build — never silently resolve against the view sides and route through the capture
-- (that would read a concealed base column through the view). The substitution skips it
-- (not a view column) and the qualifier-only strip leaves it bare → unresolvable.
create table uq14_p (pid integer primary key, pv integer, psecret integer);
create table uq14_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq14_p(pid));
create table uq14_t (tid integer primary key, tv integer);
insert into uq14_p values (10, 100, 999);
insert into uq14_c values (1, 10, 1);
insert into uq14_t values (1, 5);
create view uq14_v as
    select c.cid as cid, cval, pv from uq14_c c join uq14_p p on p.pid = c.pref;
update uq14_v set cval = (select tv from uq14_t where tv = psecret) where cid = 1;
-- error: Column not found

-- (uq-15) WHERE-path free fix (the bare-projection analog of single-source case (g)):
-- the view RENAMES a BARE-projected partner column (`plabel` → `label`), and the user
-- WHERE references the view column inside an EXISTS whose FROM (uq15_allow) ALSO has a
-- `plabel` column. The reference `label` is substituted (not shadowed — uq15_allow has no
-- `label`), and the injected lineage leaf must correlate to the join body (`p.plabel`),
-- not re-bind to uq15_allow.plabel (which would make the predicate the uncorrelated
-- 'P10' = 'ZZZ' → no rows updated). Rows joined to parent P10 (cid 1, 3) take the write.
create table uq15_p (pid integer primary key, plabel text);
create table uq15_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references uq15_p(pid));
create table uq15_allow (k text, plabel text);
insert into uq15_p values (10, 'P10'), (20, 'P20');
insert into uq15_c values (1, 10, 'a'), (2, 20, 'b'), (3, 10, 'c');
insert into uq15_allow values ('P10', 'ZZZ');
create view uq15_v as
    select c.cid as cid, note, plabel as label from uq15_c c join uq15_p p on p.pid = c.pref;
update uq15_v set note = 'HIT' where exists (select 1 from uq15_allow where k = label);
select cid, note from uq15_c order by cid;
→ [{"cid":1,"note":"HIT"},{"cid":2,"note":"b"},{"cid":3,"note":"HIT"}]

-- (uq-16) LOUD-OVER-SILENT on an unresolvable lineage scope: a SAME-side computed view
-- column (`pc`, reading only `cval`) whose base-term lineage contains a correlated
-- subquery over a `select *` source — its columns are not statically resolvable, so the
-- injection-time side-qualify (makeSideQualifyScope, unresolvableScope: 'reject') cannot
-- prove the lineage's correlation refs are not shadowed by that source and REJECTS rather
-- than substitute unqualified (a potential silent mis-bind, the prior behaviour). Mirrors
-- the single-source makeBaseQualifyScope reject policy. Same-side keeps the cross-source
-- computed-read gate from firing first, isolating the substitution-time reject.
create table uq16_p (pid integer primary key, pv integer);
create table uq16_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq16_p(pid));
create table uq16_aux (a integer);
insert into uq16_p values (10, 100);
insert into uq16_c values (1, 10, 1);
insert into uq16_aux values (1);
create view uq16_v as
    select c.cid as cid, cval,
           (select a from (select * from uq16_aux) s where s.a = cval) as pc
    from uq16_c c join uq16_p p on p.pid = c.pref;
update uq16_v set cval = pc where cid = 1;
-- error: source columns are not statically resolvable

-- ========================================================================
-- ALIAS-SCOPE-AWARE cross-source SET strip: a USER-AUTHORED *alias-qualified* ref whose
-- qualifier collides with a join side's alias/table name but is SHADOWED by an inner
-- value-subquery's own FROM alias binds locally (innermost-scope SQL rules), NOT routed by
-- the qualifier strip. The converse of uq-9 (a BARE user name colliding with a partner base
-- column — handled by the injection-pass column-name shadowing): here the collision is on an
-- alias QUALIFIER, handled by the strip's alias-shadow descent (transformAliasScopedExpr).
-- See docs/vu-operators.md § Inner Join, docs/view-updateability.md cross-source `set`.
-- ========================================================================

-- (uq-17) PARTNER-alias collision (headline): the view exposes `pv` off side `p`; an
-- unrelated `uq17_points p` is the inner value-subquery FROM. The user ref `p.score` denotes
-- uq17_points.score by innermost-scope rules, NOT the join side `p` (uq17_p has no `score`).
-- The qualifier-only strip would route `p.score` through the `parent` capture → the partner
-- base has no `score` → build error; the alias-shadow descent leaves it local. For cid=2:
-- max(score) where k=2 → max(50,70) = 70; cid=1 untouched.
create table uq17_p (pid integer primary key, pv integer);
create table uq17_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq17_p(pid));
create table uq17_points (k integer, score integer);
insert into uq17_p values (10, 100), (20, 200);
insert into uq17_c values (1, 10, 1), (2, 20, 2);
insert into uq17_points values (2, 50), (2, 70), (1, 999);
create view uq17_v as
    select c.cid as cid, cval, pv from uq17_c c join uq17_p p on p.pid = c.pref;
update uq17_v set cval = (select max(p.score) from uq17_points p where p.k = cid) where cid = 2;
select cid, cval from uq17_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":70}]

-- (uq-18) OWNING-alias collision: the owning side (child, assigns `cval`) is aliased `c`; the
-- inner value-subquery FROM aliases `uq18_things` as `c`. The user ref `c.v` denotes
-- uq18_things.v by innermost-scope rules. The qualifier-only strip would match `c` ∈ owning
-- and strip `c.v` to BARE `v` — ambiguous across the inner cross-join's two sources (a
-- plan-time error), and even resolved leftmost it would read uq18_first.v=99, not 42. The
-- alias-shadow check fires BEFORE the owning set, so `c.v` stays local → uq18_things.v = 42.
create table uq18_p (pid integer primary key, pv integer);
create table uq18_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq18_p(pid));
create table uq18_first (fid integer primary key, v integer);
create table uq18_things (tid integer primary key, v integer);
insert into uq18_p values (10, 100);
insert into uq18_c values (1, 10, 0);
insert into uq18_first values (1, 99);
insert into uq18_things values (1, 42);
create view uq18_v as
    select c.cid as cid, cval, pv from uq18_c c join uq18_p p on p.pid = c.pref;
update uq18_v set cval = (select c.v from uq18_first f cross join uq18_things c) where cid = 1;
select cid, cval from uq18_c order by cid;
→ [{"cid":1,"cval":42}]

-- (uq-19) TABLE-NAME collision: the partner side is `uq19_parent p`, so the strip's other-side
-- qualifier set carries the table NAME `uq19_parent` as well as the alias `p`. The inner
-- value-subquery aliases `uq19_aux` AS `uq19_parent`, so the user ref `uq19_parent.av` denotes
-- uq19_aux.av. The qualifier-only strip would match the table-name qualifier and route `av`
-- through the parent capture (the parent base has no `av` → build error); the alias-shadow
-- descent records the INNER alias and leaves it local. For cid=2: aux.aid=2 → av=666.
create table uq19_parent (pid integer primary key, pv integer);
create table uq19_child (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq19_parent(pid));
create table uq19_aux (aid integer primary key, av integer);
insert into uq19_parent values (10, 100), (20, 200);
insert into uq19_child values (1, 10, 1), (2, 20, 2);
insert into uq19_aux values (1, 555), (2, 666);
create view uq19_v as
    select c.cid as cid, cval, pv from uq19_child c join uq19_parent p on p.pid = c.pref;
update uq19_v set cval = (select av from uq19_aux uq19_parent where uq19_parent.aid = cid) where cid = 2;
select cid, cval from uq19_child order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":666}]

-- (uq-20) COMPOUND-LEG negative scoping: a `union` value subquery whose FIRST leg's FROM binds
-- alias `p` (colliding with the partner side `p`) must NOT shadow the GENUINE partner read in
-- the SIBLING leg — the legs correlate to the same enclosing scope, not to each other's FROM
-- (mirror transformScopedQuery's onLeg: a leg keeps the incoming alias set, not the first
-- leg's `inner`). Leg A (`uq20_aux p`, filtered to no rows) must not leak its `p` into leg B,
-- whose `pv` (view column → side-qualified `p.pv`) still routes through the capture. For cid=2
-- (joined parent pv=200): leg A empty ∪ leg B {200} → 200; cid=1 untouched.
create table uq20_parent (pid integer primary key, pv integer);
create table uq20_child (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq20_parent(pid));
create table uq20_aux (k integer, av integer);
create table uq20_one (dummy integer);
insert into uq20_parent values (10, 100), (20, 200);
insert into uq20_child values (1, 10, 1), (2, 20, 2);
insert into uq20_aux values (1, 7);
insert into uq20_one values (0);
create view uq20_v as
    select c.cid as cid, cval, pv from uq20_child c join uq20_parent p on p.pid = c.pref;
update uq20_v set cval = (
        select av from uq20_aux p where p.k = 999
        union
        select pv from uq20_one) where cid = 2;
select cid, cval from uq20_child order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":200}]

-- (uq-21) NON-COLLIDING regression: a genuine partner read (`pv` → side-qualified `p.pv`)
-- nested in a value subquery whose inner FROM uses a *different* alias (`q`) still routes
-- through the `__vmupd_keys` capture — proving the alias-shadow fix NARROWS routing to true
-- collisions, never removes it. For cid=2 (joined parent pv=200): sum(tv) where tv <= 200 =
-- 100 + 150 = 250; cid=1 untouched; the partner base unchanged.
create table uq21_p (pid integer primary key, pv integer);
create table uq21_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq21_p(pid));
create table uq21_t (tid integer primary key, tv integer);
insert into uq21_p values (10, 100), (20, 200);
insert into uq21_c values (1, 10, 1), (2, 20, 2);
insert into uq21_t values (1, 100), (2, 150), (3, 300);
create view uq21_v as
    select c.cid as cid, cval, pv from uq21_c c join uq21_p p on p.pid = c.pref;
update uq21_v set cval = (select sum(tv) from uq21_t q where q.tv <= pv) where cid = 2;
select cid, cval from uq21_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":250}]
select pid, pv from uq21_p order by pid;
→ [{"pid":10,"pv":100},{"pid":20,"pv":200}]

-- (uq-22) DEPTH-2 shadow accumulation: a partner-alias collision (`p`) shadowed by an inner
-- FROM *two* levels down — a derived-table double nesting `(... from (select … from points p) q)`.
-- The shadow set must accumulate across BOTH nestings: `p` is bound only by the innermost
-- subquery's FROM, while the genuine correlated `cid` (bare, owning target) stays untouched at
-- every depth. The qualifier-only strip would route `p.score` (depth 2) through the `parent`
-- capture (no `score` → build error); the alias-shadow descent threads `{q}` then `{q,p}` down
-- the `subquerySource` body so `p.score` binds local. For cid=2: max(50,70)=70; cid=1 untouched.
create table uq22_p (pid integer primary key, pv integer);
create table uq22_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq22_p(pid));
create table uq22_points (k integer, score integer);
insert into uq22_p values (10, 100), (20, 200);
insert into uq22_c values (1, 10, 1), (2, 20, 2);
insert into uq22_points values (2, 50), (2, 70), (1, 999);
create view uq22_v as
    select c.cid as cid, cval, pv from uq22_c c join uq22_p p on p.pid = c.pref;
update uq22_v set cval = (
        select s from (select max(p.score) as s from uq22_points p where p.k = cid) q
    ) where cid = 2;
select cid, cval from uq22_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":70}]

-- (uq-23) CAPTURE READ-BACK owning-PK collision (cross-source `set`): the value subquery's
-- FROM (`uq23_t`) has a `cid` column colliding with the owning side's PK. The partner read of
-- `pv` (off side `p`) lowers to a correlated read of the `__vmupd_keys` capture, NESTED inside
-- that user subquery; its owning-PK operand must correlate `cid` to the lowered per-side UPDATE
-- target (uq23_c, cid=2) via the synthesised `__vm_self` alias — not re-bind to `uq23_t.cid`
-- (=99, matching no capture row → mis-keyed read-back; unfixed, this surfaces as a loud
-- `NOT NULL constraint failed: uq23_c.cval`, not a silent wrong value). For cid=2 (joined pv=200):
-- max(x) where x < 200 = max(50,150) = 150; cid=1 untouched.
create table uq23_p (pid integer primary key, pv integer);
create table uq23_c (cid integer primary key, pref integer, cval integer,
    foreign key (pref) references uq23_p(pid));
create table uq23_t (tid integer primary key, x integer, cid integer);  -- `cid` collides
insert into uq23_p values (10, 100), (20, 200);
insert into uq23_c values (1, 10, 1), (2, 20, 2);
insert into uq23_t values (1, 50, 99), (2, 150, 99), (3, 250, 99);      -- cid=99 ≠ any target cid
create view uq23_v as
    select c.cid as cid, cval, pv from uq23_c c join uq23_p p on p.pid = c.pref;
update uq23_v set cval = (select max(x) from uq23_t where x < pv) where cid = 2;
select cid, cval from uq23_c order by cid;
→ [{"cid":1,"cval":1},{"cid":2,"cval":150}]

-- (uq-24) OWNING STRIP-TO-BARE collision: a RENAMED owning column (`c.realval as cval`) read
-- inside a same-side value subquery whose FROM (`uq24_t`) has the BASE column name `realval`.
-- The view name `cval` is not a `uq24_t` column, so the descent substitutes it to owning
-- `c.realval`; the qualifier strip must yield `__vm_self.realval` (correlating to the target
-- row uq24_c, realval=7) — not bare `realval`, which would re-bind to `uq24_t.realval` (=999).
-- For cid=2: max(x) where x < 7 = max(3,6) = 6; cid=1 untouched.
create table uq24_p (pid integer primary key, pv integer);
create table uq24_c (cid integer primary key, pref integer, realval integer,
    foreign key (pref) references uq24_p(pid));
create table uq24_t (tid integer primary key, x integer, realval integer);  -- base name `realval` collides
insert into uq24_p values (10, 100), (20, 200);
insert into uq24_c values (1, 10, 5), (2, 20, 7);
insert into uq24_t values (1, 3, 999), (2, 6, 999), (3, 9, 999);
create view uq24_v as
    select c.cid as cid, c.realval as cval, pv from uq24_c c join uq24_p p on p.pid = c.pref;
update uq24_v set cval = (select max(x) from uq24_t where x < cval) where cid = 2;
select cid, realval from uq24_c order by cid;
→ [{"cid":1,"realval":5},{"cid":2,"realval":6}]

-- (uq-25) COMPOSITE owning-PK variant of uq-23: a two-column owning PK (c1, c2), so the capture
-- read-back conjoins TWO equalities — each right operand must carry `__vm_self` (else c1/c2
-- re-bind to the colliding uq25_t.c1/c2 = 99). For (c1=2,c2=2) (joined pv=200): max(x) where
-- x < 200 = max(50,150) = 150; (c1=1,c2=1) untouched.
create table uq25_p (pid integer primary key, pv integer);
create table uq25_c (c1 integer, c2 integer, pref integer, cval integer,
    primary key (c1, c2),
    foreign key (pref) references uq25_p(pid));
create table uq25_t (tid integer primary key, x integer, c1 integer, c2 integer);  -- c1,c2 collide
insert into uq25_p values (10, 100), (20, 200);
insert into uq25_c values (1, 1, 10, 1), (2, 2, 20, 2);
insert into uq25_t values (1, 50, 99, 99), (2, 150, 99, 99), (3, 250, 99, 99);
create view uq25_v as
    select c.c1 as c1, c.c2 as c2, cval, pv from uq25_c c join uq25_p p on p.pid = c.pref;
update uq25_v set cval = (select max(x) from uq25_t where x < pv) where c1 = 2 and c2 = 2;
select c1, c2, cval from uq25_c order by c1;
→ [{"c1":1,"c2":1,"cval":1},{"c1":2,"c2":2,"cval":150}]

-- n-way DELETE under FK enforcement (foreign_keys defaults ON here) -> the fan-out must
-- order the base deletes FK-child before FK-parent. The canonical columnar split has each
-- member's PK reference the anchor's (axd_b, axd_c -> axd_a); deleting the parent first
-- would trip the children's inbound RESTRICT/NO-ACTION and abort. The reverse FK-topo
-- order deletes the children first, so the joined row clears from every member cleanly.
create table axd_a (k integer primary key, av integer);
create table axd_b (k integer primary key references axd_a(k), bv integer);
create table axd_c (k integer primary key references axd_a(k), cv integer);
insert into axd_a values (1, 10), (2, 20);
insert into axd_b values (1, 11), (2, 21);
insert into axd_c values (1, 12), (2, 22);
create view axd_v as
    select a.k as k, a.av as av, b.bv as bv, c.cv as cv
    from axd_a a join axd_b b on b.k = a.k join axd_c c on c.k = a.k;
delete from axd_v where k = 1;
select k from axd_a order by k;
→ [{"k":2}]
select k from axd_b order by k;
→ [{"k":2}]
select k from axd_c order by k;
→ [{"k":2}]

-- n-way (3-table star) INSERT under FK enforcement -> the shared key is minted once from
-- the anchor's declared default (axi_a), FK-parent first, then threaded into the two
-- children's key columns via the equivalence class. One insert through the view produces
-- one row per member, all sharing the minted key.
create table axi_a (k integer primary key default (coalesce((select max(k) from axi_a), 0) + 1), av integer);
create table axi_b (k integer primary key references axi_a(k), bv integer);
create table axi_c (k integer primary key references axi_a(k), cv integer);
create view axi_v as
    select a.k as k, a.av as av, b.bv as bv, c.cv as cv
    from axi_a a join axi_b b on b.k = a.k join axi_c c on c.k = a.k;
insert into axi_v (av, bv, cv) values (100, 200, 300);
select k, av from axi_a order by k;
→ [{"k":1,"av":100}]
select k, bv from axi_b order by k;
→ [{"k":1,"bv":200}]
select k, cv from axi_c order by k;
→ [{"k":1,"cv":300}]
select k, av, bv, cv from axi_v order by k;
→ [{"k":1,"av":100,"bv":200,"cv":300}]

-- DELETE through a join with no provable FK -> the hardwired-lenient predicate-honest
-- multi-side fan-out deletes the joined row's contribution from EVERY candidate side
-- ("make this joined row not exist"). The two base deletes address rows via the
-- up-front captured key set, so the first side's delete cannot empty the join out from
-- under the second side's. (To pin a single non-default side, expose it as a per-row
-- presence/existence column and write it — routing is no longer a tag.)
create table ax_x (xid integer primary key, v integer);
create table ax_y (yid integer primary key, v integer);
insert into ax_x values (1, 5);
insert into ax_y values (5, 9);
create view ax_nofk as select x.xid as xid, y.yid as yid from ax_x x join ax_y y on y.yid = x.v;
delete from ax_nofk where xid = 1;
-- the joined row (xid=1 ⋈ yid=5) is gone from both base tables.
select xid from ax_x order by xid;
→ []
select yid from ax_y order by yid;
→ []

-- ===================================
-- Lenient multi-side DELETE fan-out (the predicate-honest "make this joined row not
-- exist"). An ambiguous join delete with two residual candidate sides, no provable
-- FK, and no resolving tag deletes from BOTH sides. The two base deletes read an
-- up-front captured key set, so the first side's delete cannot empty the join out
-- from under the second side's identifying subquery. The inner join hides unmatched
-- base rows on either side, which the fan-out must leave untouched.
-- See docs/vu-operators.md § Inner Join — Deletes.
-- ===================================

-- --- (fo-a) unmatched base rows on either side survive the fan-out ---
create table fo_x (xid integer primary key, v integer, lx text);
create table fo_y (yid integer primary key, ly text);
insert into fo_x values (1, 50, 'x1'), (2, 60, 'x2'), (3, 99, 'x3');  -- xid=3 (v=99) joins nothing
insert into fo_y values (50, 'a'), (60, 'b'), (70, 'c');             -- yid=70 joins nothing
create view fo_jv as select x.xid as xid, x.lx as lx, y.ly as ly
    from fo_x x join fo_y y on y.yid = x.v;
select xid, lx, ly from fo_jv order by xid;
→ [{"xid":1,"lx":"x1","ly":"a"},{"xid":2,"lx":"x2","ly":"b"}]

-- fan out: deletes fo_x xid=1 AND fo_y yid=50 (the joined row), leaving every other
-- base row — including the two unmatched rows the inner join hides — in place.
delete from fo_jv where xid = 1;
select xid, v from fo_x order by xid;
→ [{"xid":2,"v":60},{"xid":3,"v":99}]
select yid, ly from fo_y order by yid;
→ [{"yid":60,"ly":"b"},{"yid":70,"ly":"c"}]
select xid, lx, ly from fo_jv order by xid;
→ [{"xid":2,"lx":"x2","ly":"b"}]

-- --- (fo-b) fan-out + ON DELETE CASCADE: a cascade-removed row is a silent no-op ---
-- A MUTUAL FK pair (each side references the other) leaves no single provable FK
-- direction, so the delete fans out. Both edges are ON DELETE CASCADE: deleting the
-- first side (cas_a) cascades away the matching row on the second side (cas_b) BEFORE
-- the fan-out's own second base delete runs. Because each base delete is a
-- predicate-SCAN over the live table (not a key-addressed delete that errors on a
-- missing key), the already-removed row simply falls out of the scan — a no-op, not a
-- double-delete error. Seeded with nullable FK columns + back-fill updates so the
-- mutual FK can be created under enforcement (foreign_keys defaults on).
create table cas_a (aid integer primary key, bref integer null,
    foreign key (bref) references cas_b(bid) on delete cascade);
create table cas_b (bid integer primary key, aref integer null,
    foreign key (aref) references cas_a(aid) on delete cascade);
insert into cas_a (aid, bref) values (1, null), (2, null);
insert into cas_b (bid, aref) values (10, null), (20, null);
update cas_a set bref = 10 where aid = 1;
update cas_a set bref = 20 where aid = 2;
update cas_b set aref = 1 where bid = 10;
update cas_b set aref = 2 where bid = 20;
create view cas_jv as select a.aid as aid, b.bid as bid from cas_a a join cas_b b on b.aref = a.aid;
select aid, bid from cas_jv order by aid;
→ [{"aid":1,"bid":10},{"aid":2,"bid":20}]

-- delete the (aid=1 ⋈ bid=10) row: side cas_a deletes aid=1 first, whose ON DELETE
-- CASCADE removes cas_b bid=10; the fan-out's cas_b delete (bid in {10}) then no-ops.
delete from cas_jv where aid = 1;
select aid, bref from cas_a order by aid;
→ [{"aid":2,"bref":20}]
select bid, aref from cas_b order by bid;
→ [{"bid":20,"aref":2}]
select aid, bid from cas_jv order by aid;
→ [{"aid":2,"bid":20}]

-- --- (fo-c) fan-out + RETURNING: returns the deleted view rows AND deletes both sides ---
-- The delete RETURNING re-queries the view captured BEFORE the base ops fire (the
-- pre-delete image), independent of the captured key set the base ops read.
create table fr_x (xid integer primary key, v integer, lx text);
create table fr_y (yid integer primary key, ly text);
insert into fr_x values (1, 5, 'x1'), (2, 6, 'x2');
insert into fr_y values (5, 'y5'), (6, 'y6');
create view fr_jv as select x.xid as xid, x.lx as lx, y.ly as ly
    from fr_x x join fr_y y on y.yid = x.v;
delete from fr_jv where xid = 1 returning xid, lx, ly;
→ [{"xid":1,"lx":"x1","ly":"y5"}]
select xid, lx from fr_x order by xid;
→ [{"xid":2,"lx":"x2"}]
select yid, ly from fr_y order by yid;
→ [{"yid":6,"ly":"y6"}]
select xid, lx, ly from fr_jv order by xid;
→ [{"xid":2,"lx":"x2","ly":"y6"}]

-- --- (fo-d) fan-out + body WHERE: the capture honors (user WHERE ∧ body WHERE) ---
-- A view body WHERE further restricts which joined rows exist. The fan-out's
-- up-front capture identifies rows by (user WHERE ∧ body WHERE) via the shared
-- buildMultiSourceKeyCapture, so a delete only removes base rows whose joined row is
-- actually visible through the view; a row excluded by the body predicate is spared
-- on BOTH sides even when the user WHERE alone would match it.
create table bw_x (xid integer primary key, v integer, keep integer);
create table bw_y (yid integer primary key, ly text);
insert into bw_x values (1, 5, 1), (2, 6, 0);   -- xid=2 has keep=0 -> hidden by body WHERE
insert into bw_y values (5, 'y5'), (6, 'y6');
create view bw_dv as select x.xid as xid, y.ly as ly
    from bw_x x join bw_y y on y.yid = x.v where x.keep = 1;
-- xid=2 (keep=0) is not in the view, so deleting where xid in (1,2) only removes the
-- xid=1 joined row from both sides; xid=2 / yid=6 survive.
delete from bw_dv where xid in (1, 2);
select xid, v, keep from bw_x order by xid;
→ [{"xid":2,"v":6,"keep":0}]
select yid, ly from bw_y order by yid;
→ [{"yid":6,"ly":"y6"}]

-- --- (fo-e) a second no-FK two-candidate join still fans out (no tag needed) ---
-- With two candidate sides and no provable FK, the hardwired-lenient delete fans out
-- to both — the same maximal-lenient behavior the routing tags used to leave in place.
create table te_x (xid integer primary key, v integer);
create table te_y (yid integer primary key, w integer);
insert into te_x values (1, 5), (2, 7);
insert into te_y values (5, 50), (7, 70);
create view te_jv as select x.xid as xid, y.yid as yid from te_x x join te_y y on y.yid = x.v;
delete from te_jv where xid = 1;
select xid from te_x order by xid;
→ [{"xid":2}]
select yid from te_y order by yid;
→ [{"yid":7}]

-- --- (fo-f) no-FK ambiguous delete fans out (lenient is now unconditional) ---
-- The removed `quereus.update.policy='strict'` knob could once reject an ambiguous
-- multi-side delete; lenient fan-out is now the only behavior, so the same no-FK shape
-- deletes from BOTH sides rather than rejecting. To forbid a side, do not expose it.
create table ps_x (xid integer primary key, v integer);
create table ps_y (yid integer primary key, w integer);
insert into ps_x values (1, 5);
insert into ps_y values (5, 50);
create view ps_jv as select x.xid as xid, y.yid as yid
    from ps_x x join ps_y y on y.yid = x.v;
delete from ps_jv where xid = 1;
-- the joined row is gone from both base tables (lenient fan-out).
select xid from ps_x order by xid;
→ []
select yid from ps_y order by yid;
→ []

-- ===================================
-- ON-DELETE-aware fan-out ordering over an ASYMMETRIC mutual FK. The two-side
-- fan-out is reached only when no single-direction FK is provable — a mutual FK
-- (each side references the other) is one such case. When the two edges' ON DELETE
-- actions differ, a fixed [0,1] order is no longer order-independent: the fan-out
-- now orders the base deletes by ON DELETE action (delete the side whose removal
-- clears the other's reference first), and rejects at plan time the mutual shapes no
-- order can satisfy under immediate enforcement + the transitive RESTRICT pre-walk
-- (runtime/foreign-key-actions.ts). Seeded like (fo-b): nullable FK columns +
-- back-fill updates so the mutual FK can be established under enforcement.
-- See docs/vu-operators.md § Inner Join — Deletes.
-- ===================================

-- --- (fo-g) restrict+cascade mutual FK -> structured diagnostic (the headline) ---
-- Edge g_a.bref -> g_b ON DELETE CASCADE governs deleting side1 (g_b); edge
-- g_b.aref -> g_a ON DELETE RESTRICT governs deleting side0 (g_a). So inbound0 =
-- restrict, inbound1 = cascade. Deleting g_a first trips g_b's RESTRICT; deleting
-- g_b first cascades into g_a, whose deletion then trips g_b's RESTRICT via the
-- transitive pre-walk. Neither order is satisfiable, so the plan is rejected up front
-- with `mutual-fk-restrict-delete` instead of the raw transitive-FK runtime error.
create table g_a (aid integer primary key, bref integer null,
    foreign key (bref) references g_b(bid) on delete cascade);
create table g_b (bid integer primary key, aref integer null,
    foreign key (aref) references g_a(aid) on delete restrict);
insert into g_a (aid, bref) values (1, null), (2, null);
insert into g_b (bid, aref) values (10, null), (20, null);
update g_a set bref = 10 where aid = 1;
update g_a set bref = 20 where aid = 2;
update g_b set aref = 1 where bid = 10;
update g_b set aref = 2 where bid = 20;
create view g_jv as select a.aid as aid, b.bid as bid from g_a a join g_b b on b.aref = a.aid;
select aid, bid from g_jv order by aid;
→ [{"aid":1,"bid":10},{"aid":2,"bid":20}]
delete from g_jv where aid = 1;
-- error: mutual foreign key
-- the rejected delete is a no-op: both base rows survive.
select aid, bref from g_a order by aid;
→ [{"aid":1,"bref":10},{"aid":2,"bref":20}]
select bid, aref from g_b order by bid;
→ [{"bid":10,"aref":1},{"bid":20,"aref":2}]

-- --- (fo-h) restrict/restrict mutual FK -> same diagnostic ---
-- Both edges ON DELETE RESTRICT (inbound0 = inbound1 = restrict): deleting either
-- side trips the other's RESTRICT directly. Unsatisfiable in any order; rejected.
create table h_a (aid integer primary key, bref integer null,
    foreign key (bref) references h_b(bid) on delete restrict);
create table h_b (bid integer primary key, aref integer null,
    foreign key (aref) references h_a(aid) on delete restrict);
insert into h_a (aid, bref) values (1, null), (2, null);
insert into h_b (bid, aref) values (10, null), (20, null);
update h_a set bref = 10 where aid = 1;
update h_a set bref = 20 where aid = 2;
update h_b set aref = 1 where bid = 10;
update h_b set aref = 2 where bid = 20;
create view h_jv as select a.aid as aid, b.bid as bid from h_a a join h_b b on b.aref = a.aid;
delete from h_jv where aid = 1;
-- error: mutual foreign key
-- both base rows survive the rejected delete.
select aid, bref from h_a order by aid;
→ [{"aid":1,"bref":10},{"aid":2,"bref":20}]
select bid, aref from h_b order by bid;
→ [{"bid":10,"aref":1},{"bid":20,"aref":2}]

-- --- (fo-i) restrict+setNull mutual FK -> SUCCESS via reordering (the rescued case) ---
-- This is the ONLY asymmetric mutual shape a side ordering can satisfy. Edge
-- i_a.bref -> i_b ON DELETE SET NULL governs deleting side1 (i_b); edge i_b.aref ->
-- i_a ON DELETE RESTRICT governs deleting side0 (i_a). So inbound0 = restrict,
-- inbound1 = setNull. The as-is [0,1] order would delete i_a first and trip i_b's
-- RESTRICT (a latent runtime error before this change); the ON-DELETE-aware order
-- deletes i_b FIRST — its SET NULL clears i_a.bref, removing no row but no RESTRICT —
-- then deletes i_a cleanly (no i_b row references it any longer). Both base rows for
-- the joined identity end up gone.
create table i_a (aid integer primary key, bref integer null,
    foreign key (bref) references i_b(bid) on delete set null);
create table i_b (bid integer primary key, aref integer null,
    foreign key (aref) references i_a(aid) on delete restrict);
insert into i_a (aid, bref) values (1, null), (2, null);
insert into i_b (bid, aref) values (10, null), (20, null);
update i_a set bref = 10 where aid = 1;
update i_a set bref = 20 where aid = 2;
update i_b set aref = 1 where bid = 10;
update i_b set aref = 2 where bid = 20;
create view i_jv as select a.aid as aid, b.bid as bid from i_a a join i_b b on b.aref = a.aid;
select aid, bid from i_jv order by aid;
→ [{"aid":1,"bid":10},{"aid":2,"bid":20}]
-- reordered fan-out: i_b bid=10 deleted first (SET NULL clears i_a aid=1's bref),
-- then i_a aid=1 deleted cleanly. The aid=2 / bid=20 joined identity is untouched.
delete from i_jv where aid = 1;
select aid, bref from i_a order by aid;
→ [{"aid":2,"bref":20}]
select bid, aref from i_b order by bid;
→ [{"bid":20,"aref":2}]
select aid, bid from i_jv order by aid;
→ [{"aid":2,"bid":20}]

-- --- (fo-j) setNull+restrict mutual FK -> SUCCESS via the MIRRORED [0,1] order ---
-- The mirror of (fo-i): the SET NULL edge governs deleting side0 (not side1), so the
-- rescuing order is [0,1] rather than [1,0]. Edge j_a.bref -> j_b ON DELETE RESTRICT
-- governs deleting side1 (j_b); edge j_b.aref -> j_a ON DELETE SET NULL governs
-- deleting side0 (j_a). So inbound0 = setNull, inbound1 = restrict — `deletableFirst`
-- takes its SET NULL clause (not its cascade clause, which (fo-b) already pins), so the
-- fan-out keeps [0,1]: delete j_a FIRST (its SET NULL clears j_b's aref, tripping no
-- RESTRICT), then j_b cleanly (no j_a row references it any longer). This pins the
-- order-selection symmetry — the SET NULL-first rescue works in BOTH directions.
create table j_a (aid integer primary key, bref integer null,
    foreign key (bref) references j_b(bid) on delete restrict);
create table j_b (bid integer primary key, aref integer null,
    foreign key (aref) references j_a(aid) on delete set null);
insert into j_a (aid, bref) values (1, null), (2, null);
insert into j_b (bid, aref) values (10, null), (20, null);
update j_a set bref = 10 where aid = 1;
update j_a set bref = 20 where aid = 2;
update j_b set aref = 1 where bid = 10;
update j_b set aref = 2 where bid = 20;
create view j_jv as select a.aid as aid, b.bid as bid from j_a a join j_b b on b.aref = a.aid;
select aid, bid from j_jv order by aid;
→ [{"aid":1,"bid":10},{"aid":2,"bid":20}]
-- reordered fan-out keeps [0,1]: j_a aid=1 deleted first (SET NULL clears j_b bid=10's
-- aref), then j_b bid=10 deleted cleanly. The aid=2 / bid=20 identity is untouched.
delete from j_jv where aid = 1;
select aid, bref from j_a order by aid;
→ [{"aid":2,"bref":20}]
select bid, aref from j_b order by bid;
→ [{"bid":20,"aref":2}]
select aid, bid from j_jv order by aid;
→ [{"aid":2,"bid":20}]

-- --- (fo-k) restrict/restrict mutual FK, join on a NON-FK column -> SUCCESS ---
-- The plan-time `mutual-fk-restrict-delete` reject is gated on the join provably
-- correlating a mutual FK edge (the joined rows necessarily cross-reference). Here the
-- join is on a non-FK column (a.lbl = b.lbl) — it correlates NEITHER edge — and the FK
-- columns (bref/aref) are left NULL, so the joined rows do not in fact cross-reference.
-- The schema-only `orderDeleteFanout` still returns undefined (both edges RESTRICT), but
-- the reject no longer fires; the planner falls back to the fixed [0,1] fan-out and
-- defers to the runtime RESTRICT pre-check, which (FK columns NULL ⇒ MATCH SIMPLE: no
-- referencing row) finds nothing and lets the delete succeed. This is the over-rejection
-- repro from the ticket. See docs/vu-operators.md § Inner Join — Deletes.
create table k_a (aid integer primary key, lbl text, bref integer null,
    foreign key (bref) references k_b(bid) on delete restrict);
create table k_b (bid integer primary key, lbl text, aref integer null,
    foreign key (aref) references k_a(aid) on delete restrict);
insert into k_a (aid, lbl, bref) values (1, 'x', null), (2, 'y', null);
insert into k_b (bid, lbl, aref) values (10, 'x', null), (20, 'z', null);
create view k_jv as select a.aid as aid, b.bid as bid from k_a a join k_b b on a.lbl = b.lbl;
select aid, bid from k_jv order by aid;
→ [{"aid":1,"bid":10}]
-- fans out [0,1]; both base deletes succeed (FK columns NULL — runtime RESTRICT pre-check
-- finds no referencing row), removing both base rows of the joined identity (aid=1/bid=10).
delete from k_jv where aid = 1;
select aid from k_a order by aid;
→ [{"aid":2}]
select bid from k_b order by bid;
→ [{"bid":20}]

-- --- (fo-l) same non-FK join, but FK columns back-filled -> RAW FK-constraint error ---
-- The accepted residual tradeoff of (fo-k)'s relaxation: when the join correlates neither
-- edge, the actionable plan-time `mutual-fk-restrict-delete` diagnostic is no longer
-- raised — so when the joined rows *do* cross-reference in data (FK columns back-filled
-- here), the deferred [0,1] fan-out's per-base-op parent-side FK check raises the raw
-- FK-constraint error instead. A non-correlated join trades the actionable plan-time
-- diagnostic for the raw FK error when the data happens to reference.
-- See docs/vu-operators.md § Inner Join — Deletes.
create table l_a (aid integer primary key, lbl text, bref integer null,
    foreign key (bref) references l_b(bid) on delete restrict);
create table l_b (bid integer primary key, lbl text, aref integer null,
    foreign key (aref) references l_a(aid) on delete restrict);
insert into l_a (aid, lbl, bref) values (1, 'x', null), (2, 'y', null);
insert into l_b (bid, lbl, aref) values (10, 'x', null), (20, 'z', null);
-- back-fill so the joined identity (aid=1/bid=10) cross-references in data
update l_a set bref = 10 where aid = 1;
update l_b set aref = 1 where bid = 10;
create view l_jv as select a.aid as aid, b.bid as bid from l_a a join l_b b on a.lbl = b.lbl;
select aid, bid from l_jv order by aid;
→ [{"aid":1,"bid":10}]
-- deferred [0,1] fan-out: deleting l_a aid=1 first trips l_b's reference to aid=1 (l_b
-- bid=10 has aref=1) at the per-base-op parent-side FK check.
delete from l_jv where aid = 1;
-- error: violates RESTRICT from 'l_b'
-- the rejected delete is a no-op: both base rows survive.
select aid, bref from l_a order by aid;
→ [{"aid":1,"bref":10},{"aid":2,"bref":null}]
select bid, aref from l_b order by bid;
→ [{"bid":10,"aref":1},{"bid":20,"aref":null}]

-- --- (fo-m) correlation expressed in the body WHERE (not the ON) -> REJECT ---
-- The correlation gate folds the view body's own WHERE conjuncts into the cross-side
-- equality set, not just the ON condition. Here the join's ON is on a non-FK column
-- (a.lbl = b.lbl, correlating neither edge), but the body WHERE `b.aref = a.aid`
-- correlates edge B (m_b.aref -> m_a.aid). So `joinCorrelatesMutualFk` still proves a
-- mutual FK edge correlated and the plan-time `mutual-fk-restrict-delete` reject fires
-- — the actionable diagnostic, not the deferred raw FK error. This pins the WHERE-fold
-- branch of the gate. See docs/vu-operators.md § Inner Join — Deletes.
create table m_a (aid integer primary key, lbl text, bref integer null,
    foreign key (bref) references m_b(bid) on delete restrict);
create table m_b (bid integer primary key, lbl text, aref integer null,
    foreign key (aref) references m_a(aid) on delete restrict);
insert into m_a (aid, lbl, bref) values (1, 'x', null), (2, 'y', null);
insert into m_b (bid, lbl, aref) values (10, 'x', null), (20, 'z', null);
-- back-fill after both rows exist (avoids the circular insert-time FK chicken-and-egg)
update m_a set bref = 10 where aid = 1;
update m_b set aref = 1 where bid = 10;
create view m_jv as select a.aid as aid, b.bid as bid from m_a a join m_b b on a.lbl = b.lbl where b.aref = a.aid;
select aid, bid from m_jv order by aid;
→ [{"aid":1,"bid":10}]
delete from m_jv where aid = 1;
-- error: mutual foreign key
-- both base rows survive the rejected delete.
select aid, bref from m_a order by aid;
→ [{"aid":1,"bref":10},{"aid":2,"bref":null}]
select bid, aref from m_b order by bid;
→ [{"bid":10,"aref":1},{"bid":20,"aref":null}]

-- ===================================
-- Phase 3.4: view with defaults + per-row routing. A view's omitted-insert
-- defaults are declared first-class — `create view v as <body> with defaults
-- (col = expr, …)` — the construct that replaced the removed
-- `quereus.update.default_for.<column>` tag (view-DDL and statement-level alike;
-- a per-statement default is expressible as an explicit insert value). Write
-- *routing* is no longer a tag either — predicates rule, and a per-row writable
-- presence/existence column states a non-default side explicitly. Every retired
-- `quereus.update.*` key is now an `unknown-reserved-tag` error. See
-- docs/vu-inverses.md § View defaults.
-- ===================================

-- --- the retired statement-level default_for tag is unknown at the dml-stmt site ---
create table df_t (id integer primary key, created integer, note text);
create view df_v as select id, note from df_t;

insert into df_v with tags ("quereus.update.default_for.created" = '777') values (5, 'x');
-- error: unknown reserved tag
-- the rejected insert wrote nothing.
select * from df_t order by id;
→ []

-- --- with defaults: the view-declared omitted-insert default ---
create table df2 (id integer primary key, created integer);
create view df2_v as select id from df2 with defaults (created = 111);

-- the clause supplies the omitted projected-away column (the expression is a
-- first-class AST value, not re-parsed tag text).
insert into df2_v values (1);
select * from df2 order by id;
→ [{"id":1,"created":111}]

-- --- with defaults: non-literal expression, alongside a renamed column list ---
create table df3 (id integer primary key, created integer, label text);
create view df3_v (ident, tag) as select id, label from df3 with defaults (created = 100 + 11);
insert into df3_v values (1, 'x');
select * from df3 order by id;
→ [{"id":1,"created":111,"label":"x"}]

-- --- a duplicate target WITHIN one `with defaults` clause is a parse error ---
create view df3_dup_v as select id from df3 with defaults (created = 1, created = 2);
-- error: Duplicate column 'created' in WITH DEFAULTS

-- --- the retired view-DDL default_for tag is unknown at mutation ---
-- View-DDL tag validation is lazy (the create succeeds; the rename hints are the
-- only reserved keys still legal there, inert on a direct create) — the
-- write-through fails, and nothing is written.
create table df5 (id integer primary key, created integer);
create view df5_v as select id from df5 with tags ("quereus.update.default_for.created" = '555');
insert into df5_v values (1);
-- error: unknown reserved tag
select * from df5 order by id;
→ []

-- --- with defaults naming an unknown column: hard sited error at write ---
create table df6 (id integer primary key, created integer);
create view df6_v as select id from df6 with defaults (nope = 1);
insert into df6_v values (1);
-- error: not a column

-- --- materialized view: with defaults write through to the source and are
-- transparent to row-time backing maintenance (maintenance projects the
-- resulting source row, defaulted column included-then-projected-away).
create table mvd_t (id integer primary key, name text, created integer not null);
create materialized view mvd_v as select id, name from mvd_t with defaults (created = 777);
insert into mvd_v values (1, 'a');
-- the source row carries the defaulted not-null column...
select * from mvd_t order by id;
→ [{"id":1,"name":"a","created":777}]
-- ...and the backing reads back the projected row (reads-own-writes).
select * from mvd_v order by id;
→ [{"id":1,"name":"a"}]

-- --- predicate/FK default: an FK join delete auto-routes to the FK-child side ---
-- A declared foreign key proves the child (FK-many) side, so the join delete routes
-- there unconditionally (deleting the child leaves the parent) — the predicate/FK
-- truth, no tag involved.
create table pol_parent (pid integer primary key, lbl text);
create table pol_child (cid integer primary key, pref integer, note text,
    foreign key (pref) references pol_parent(pid));
insert into pol_parent values (10, 'P10'), (20, 'P20');
insert into pol_child values (1, 10, 'a'), (2, 20, 'b');
create view pol_fk as
    select c.cid as cid, c.note as note, p.lbl as lbl
    from pol_child c join pol_parent p on p.pid = c.pref;
delete from pol_fk where cid = 1;
select cid from pol_child order by cid;
→ [{"cid":2}]
select pid from pol_parent order by pid;
→ [{"pid":10},{"pid":20}]

-- --- explicit non-default side via a per-row existence column ---
-- The removed `delete_via='parent'` / `target` routing is now expressed per-row: a LEFT
-- join exposes the parent's match as `exists right as hasP`, and writing `hasP = false`
-- deletes the matched parent for the targeted rows (ON DELETE CASCADE clears the
-- now-orphaned child). This is the strictly-more-precise replacement — the side is named
-- in the data shape, per row, not by a statement-level tag.
create table er_parent (pid integer primary key, lbl text);
create table er_child (cid integer primary key, pref integer, note text,
    foreign key (pref) references er_parent(pid) on delete cascade);
insert into er_parent values (10, 'P10'), (20, 'P20');
insert into er_child values (1, 10, 'a'), (2, 20, 'b');
create view er_jv as
    select c.cid as cid, c.note as note, p.lbl as lbl, hasP
    from er_child c left join er_parent p on p.pid = c.pref exists right as hasP;
update er_jv set hasP = false where cid = 1;
select pid from er_parent order by pid;
→ [{"pid":20}]
select cid from er_child order by cid;
→ [{"cid":2}]

-- --- the removed routing keys are now unknown-reserved-tag errors ---
-- target / exclude / delete_via / policy on a DML statement -> unknown reserved tag.
create table tg_x (xid integer primary key, v integer, lx text);
create table tg_y (yid integer primary key, ly text);
insert into tg_x values (1, 5, 'x1'), (2, 6, 'x2');
insert into tg_y values (5, 'y5'), (6, 'y6');
create view tg_jv as
    select x.xid as xid, x.lx as lx, y.ly as ly from tg_x x join tg_y y on y.yid = x.v;

delete from tg_jv with tags ("quereus.update.target" = 'tg_x') where xid = 1;
-- error: unknown reserved tag

delete from tg_jv with tags ("quereus.update.delete_via" = 'left_delete') where xid = 1;
-- error: unknown reserved tag

-- the rejected deletes left every base row intact.
select xid from tg_x order by xid;
→ [{"xid":1},{"xid":2}]
select yid from tg_y order by yid;
→ [{"yid":5},{"yid":6}]

-- --- sited diagnostics for malformed reserved tags ---

-- unknown reserved key (a typo) on a DML statement -> sited error.
update tg_jv with tags ("quereus.update.taget" = 'tg_x') set lx = 'z' where xid = 2;
-- error: unknown reserved tag

-- a removed routing key declared on the VIEW DDL -> surfaces (sited) at mutation.
create table bvu (id integer primary key, x integer);
create view bvu_v as select id from bvu with tags ("quereus.update.policy" = 'strict');
insert into bvu_v values (1);
-- error: unknown reserved tag

-- ===================================
-- Phase 3.4: adversarial edges (review pass) — insert-default precedence and a
-- single-source view carrying a removed routing tag.
-- ===================================

-- --- insert-default precedence: an explicit supply of the column wins ---
-- The view projects `created`, so a statement that supplies it explicitly must
-- override the view's declared insert default; an omitted insert still takes it.
create table dfp (id integer primary key, created integer);
create view dfp_v as select id, created from dfp with defaults (created = 111);
insert into dfp_v (id, created) values (1, 999);
insert into dfp_v (id) values (2);
select * from dfp order by id;
→ [{"id":1,"created":999},{"id":2,"created":111}]

-- --- single-source view carrying a removed routing tag -> error at mutation ---
-- target/exclude/delete_via/policy were removed; a stray one declared on a view (single-
-- or multi-source) is no longer an inert no-op — it is an unknown-reserved-tag error
-- surfaced when the view is mutated, so nothing is written.
create table sst (id integer primary key, note text);
create view sst_v as select id, note from sst
    with tags ("quereus.update.target" = 'not_a_real_table');
insert into sst_v values (1, 'a');
-- error: unknown reserved tag
select * from sst order by id;
→ []

-- ===================================
-- View-column references nested inside subquery / exists / in-subquery operands
-- of a user predicate (or assigned value) are descended into and rewritten to
-- their base-term lineage — scope-aware, so a subquery-local same-named column is
-- left alone and an unresolvable source is rejected, not silently mis-bound.
-- See docs/vu-operators.md § Selection.
-- ===================================

-- --- (a) the repro: a correlated view-column ref inside EXISTS, over a base table
--         that ALSO has a same-named base column (silent-mis-bind hazard) ---
-- The view renames base `lbl` to the view column `note`; base table `sq_c` also
-- has a literal `note` column. `note` inside EXISTS is the VIEW column (= base
-- `lbl`); it must substitute to `lbl`, not re-bind to base `sq_c.note`.
create table sq_c (cid integer primary key, note text, lbl text);
create table sq_other (k text);
insert into sq_c values (1, 'BASE_NOTE_1', 'LBL_1'), (2, 'BASE_NOTE_2', 'LBL_2');
insert into sq_other values ('LBL_1');
create view sq_jv as select cid as cid, lbl as note from sq_c;

update sq_jv set note = 'CHANGED' where exists (select 1 from sq_other o where o.k = note);
-- cid=1 (lbl LBL_1, present in sq_other) matches -> its `lbl` becomes CHANGED; the
-- silent no-op (predicate re-bound to base `note`, absent from sq_other) is gone.
select cid, note, lbl from sq_c order by cid;
→ [{"cid":1,"note":"BASE_NOTE_1","lbl":"CHANGED"},{"cid":2,"note":"BASE_NOTE_2","lbl":"LBL_2"}]
-- the write is visible through the view (view col `note` = base `lbl`).
select * from sq_jv order by cid;
→ [{"cid":1,"note":"CHANGED"},{"cid":2,"note":"LBL_2"}]

-- --- (b) a correlated view-column ref inside an in (select ... where ...) ---
-- `category` is the view column (= base `grp`); referenced unqualified inside the
-- IN-subquery (whose source has no same-named column) it correlates to the outer
-- view row and must substitute to `grp`.
create table sq_in_t (id integer primary key, grp text, mark text);
create table sq_in_map (g text, allowed_id integer);
insert into sq_in_t values (1, 'G1', 'x'), (2, 'G2', 'y'), (3, 'G1', 'z');
insert into sq_in_map values ('G1', 1), ('G1', 3);
create view sq_in_v as select id as id, grp as category, mark as mark from sq_in_t;

update sq_in_v set mark = 'M' where id in (select allowed_id from sq_in_map where g = category);
select id, grp, mark from sq_in_t order by id;
→ [{"id":1,"grp":"G1","mark":"M"},{"id":2,"grp":"G2","mark":"y"},{"id":3,"grp":"G1","mark":"M"}]

-- --- (c) a subquery-local same-named column must NOT be substituted ---
-- The view renames base `lbl` to view column `note`; the subquery source
-- `sq_sh_src` has its OWN `note` column. `note` inside `in (select note from
-- sq_sh_src)` is sq_sh_src.note (subquery-local) — it must stay local. The
-- top-level left `note` is the view column and substitutes to base `lbl`.
create table sq_sh_t (tid integer primary key, lbl text, payload text);
create table sq_sh_src (note text);
insert into sq_sh_t values (1, 'L1', 'p1'), (2, 'L2', 'p2');
insert into sq_sh_src values ('L1');
create view sq_sh_v as select tid as tid, lbl as note, payload as payload from sq_sh_t;

update sq_sh_v set payload = 'P' where note in (select note from sq_sh_src);
-- lbl in ('L1') -> only tid=1; if the inner `note` had been wrongly substituted to
-- `lbl`, the subquery `select lbl from sq_sh_src` would not even resolve.
select tid, lbl, payload from sq_sh_t order by tid;
→ [{"tid":1,"lbl":"L1","payload":"P"},{"tid":2,"lbl":"L2","payload":"p2"}]

-- --- (d) a subquery referencing NO view column still works unchanged ---
create table sq_none_t (id integer primary key, val text);
create table sq_none_ok (flag integer);
insert into sq_none_t values (1, 'a'), (2, 'b');
insert into sq_none_ok values (1);
create view sq_none_v as select id as id, val as val from sq_none_t;

update sq_none_v set val = 'Z' where exists (select 1 from sq_none_ok where flag = 1);
select id, val from sq_none_t order by id;
→ [{"id":1,"val":"Z"},{"id":2,"val":"Z"}]

-- --- (e1) multi-source join view: a view-col ref nested in a subquery in the user
--          WHERE is routed to the correct (alias-qualified) base term ---
-- `label` is owned by the parent side (p.label); referenced inside EXISTS it
-- substitutes to `p.label`, correlating to the join body of the identifying
-- subquery. The update routes to the child side (note is owned by sq_jc).
create table sq_jp (pid integer primary key, label text);
create table sq_jc (cid integer primary key, pref integer, note text,
    foreign key (pref) references sq_jp(pid));
create table sq_allow (lab text);
insert into sq_jp values (10, 'P10'), (20, 'P20');
insert into sq_jc values (1, 10, 'a'), (2, 20, 'b'), (3, 10, 'c');
insert into sq_allow values ('P10');
create view sq_jv2 as select c.cid as cid, c.note as note, p.label as label
    from sq_jc c join sq_jp p on p.pid = c.pref;

update sq_jv2 set note = 'HIT' where exists (select 1 from sq_allow where lab = label);
select cid, note from sq_jc order by cid;
→ [{"cid":1,"note":"HIT"},{"cid":2,"note":"b"},{"cid":3,"note":"HIT"}]

-- --- (e2) multi-source: a view-col ref nested in a subquery in a SET value ---
-- The SET value is a correlated scalar subquery keyed on the OWNING-side view
-- column `note` (= c.note); the descent substitutes it to `c.note` and the
-- owning-side qualifier is stripped through the subquery so it correlates to the
-- target row of the lowered child UPDATE.
create table sq_e2_jp (pid integer primary key, label text);
create table sq_e2_jc (cid integer primary key, pref integer, note text,
    foreign key (pref) references sq_e2_jp(pid));
create table sq_e2_lookup (k text, v text);
insert into sq_e2_jp values (10, 'P10');
insert into sq_e2_jc values (1, 10, 'a'), (2, 10, 'b');
insert into sq_e2_lookup values ('a', 'AA'), ('b', 'BB');
create view sq_e2_v as select c.cid as cid, c.note as note, p.label as label
    from sq_e2_jc c join sq_e2_jp p on p.pid = c.pref;

update sq_e2_v set note = (select v from sq_e2_lookup where k = note) where cid = 1;
select cid, note from sq_e2_jc order by cid;
→ [{"cid":1,"note":"AA"},{"cid":2,"note":"b"}]

-- --- (f) conservative reject: a `select *` subquery source can't be resolved, so
--         a nested unqualified view-column ref can't be proven correlated ---
create table sq_f_t (id integer primary key, lbl text);
create table sq_f_aux (x text);
insert into sq_f_t values (1, 'L1');
insert into sq_f_aux values ('L1');
create view sq_f_v as select id as id, lbl as tag from sq_f_t;

update sq_f_v set tag = 'X' where exists (select 1 from (select * from sq_f_aux) as s where s.x = tag);
-- error: cannot write through

-- a table-valued-function subquery source is unresolvable the same way -> reject.
update sq_f_v set tag = 'X' where exists (select 1 from table_info('sq_f_t') as ti where ti.name = tag);
-- error: cannot write through

-- --- (g) the substituted base term itself collides with a subquery-local source ---
-- The view renames base `lbl` to view column `note`; the EXISTS subquery FROM
-- (p1_aux) ALSO has an `lbl` column. `note` is the view column = base `p1_t.lbl`,
-- correlated to the outer row. An UNQUALIFIED substituted base term (`lbl`) inside
-- the subquery would re-bind to p1_aux.lbl (innermost scope) — making the predicate
-- the uncorrelated p1_aux.k = p1_aux.lbl, true for the ('Q','Q') row, so EVERY outer
-- row would pass (a silent wrong write). Single-source descent qualifies the term to
-- p1_t.lbl, so the predicate is the correlated p1_aux.k = p1_t.lbl -> only row 1.
create table p1_t (id integer primary key, lbl text);
create table p1_aux (k text, lbl text);
insert into p1_t values (1, 'A'), (2, 'B');
insert into p1_aux values ('A', 'X'), ('Q', 'Q');
create view p1_v as select id as id, lbl as note from p1_t;

update p1_v set note = 'CHANGED' where exists (select 1 from p1_aux where k = note);
select id, lbl from p1_t order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"B"}]

-- --- (h) `where lbl = note`: the literal base name AND the substituted term collide ---
-- The literal `lbl` binds the subquery-local p2_aux.lbl (it is not a view column,
-- so it is never substituted); `note` is the view column = base p2_t.lbl. Without
-- qualification both collapse to p2_aux.lbl, making the predicate the always-true
-- p2_aux.lbl = p2_aux.lbl. The substituted term is qualified to p2_t.lbl, so the
-- predicate is the correlated p2_aux.lbl = p2_t.lbl -> only the matching row 1.
create table p2_t (id integer primary key, lbl text);
create table p2_aux (k text, lbl text);
insert into p2_t values (1, 'A'), (2, 'B');
insert into p2_aux values ('A', 'A'), ('Q', 'Q');
create view p2_v as select id as id, lbl as note from p2_t;

update p2_v set note = 'CHANGED' where exists (select 1 from p2_aux where lbl = note);
select id, lbl from p2_t order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"B"}]

-- --- (i) negative control on the BASE name: a bare base-name ref to a genuine
--          subquery-local source must STAY local (analogous to (c), on `lbl`) ---
-- The view renames base `lbl` to `note`; the EXISTS subquery source nc_src has its
-- OWN `lbl` column plus an `ok` flag. The bare `lbl` inside the subquery is
-- nc_src.lbl (never a view column, so never substituted/qualified); `note` is the
-- view column, qualified to the correlated nc_t.lbl. The predicate selects the
-- nc_src row whose lbl matches the outer row and tests its `ok`. If the fix wrongly
-- qualified the local `lbl` to nc_t.lbl, the predicate would degrade to the
-- always-true nc_t.lbl = nc_t.lbl and ok = 1 -> both rows would write.
create table nc_t (id integer primary key, lbl text);
create table nc_src (lbl text, ok integer);
insert into nc_t values (1, 'L1'), (2, 'L2');
insert into nc_src values ('L1', 1), ('L2', 0);
create view nc_v as select id as id, lbl as note from nc_t;

update nc_v set note = 'CHANGED' where exists (select 1 from nc_src where lbl = note and ok = 1);
select id, lbl from nc_t order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"L2"}]

-- --- (j) DELETE descent: same collision as (g) on the delete path ---
-- The delete rewrite threads the same base-table qualifier into the subquery
-- descent. The view renames base `lbl` to `note`; the EXISTS subquery FROM
-- (d1_aux) ALSO has an `lbl`. Without qualification the substituted base term
-- would re-bind to d1_aux.lbl, making the predicate the uncorrelated
-- d1_aux.k = d1_aux.lbl (true for ('Q','Q')) -> every outer row would delete (a
-- silent wrong delete). Qualified to d1_t.lbl, only the matching row 1 deletes.
create table d1_t (id integer primary key, lbl text);
create table d1_aux (k text, lbl text);
insert into d1_t values (1, 'A'), (2, 'B');
insert into d1_aux values ('A', 'X'), ('Q', 'Q');
create view d1_v as select id as id, lbl as note from d1_t;

delete from d1_v where exists (select 1 from d1_aux where k = note);
select id, lbl from d1_t order by id;
→ [{"id":2,"lbl":"B"}]

-- --- (k) computed lineage whose base term is itself a correlated subquery, the
--          DEEP collision: the substituted term must be qualified INSIDE its own
--          nested subquery, not just at its top level ---
-- The view column `note` is a correlated scalar subquery (select x from cv_oth
-- where fk = id); its correlation `id` is cv_base.id. Referenced inside the user
-- EXISTS subquery `select 1 from cv_src where tag = note` — and cv_src ALSO has an
-- `id` column — a SHALLOW qualifier left the lineage's own inner `id` unqualified,
-- so it re-bound to cv_src.id (=99); cv_oth has no fk=99, so `note` evaluated NULL
-- and the EXISTS was false for EVERY outer row (a silent no-op). The deep
-- scope-aware qualifier rewrites the term to (select x from cv_oth where fk =
-- cv_base.id), correlating to the outer target row regardless of the user FROM.
create table cv_base (id integer primary key, lbl text);
create table cv_oth (fk integer, x text);
create table cv_src (id integer, tag text);
insert into cv_base values (1, 'A'), (2, 'B');
insert into cv_oth values (1, 'AA'), (2, 'BB');
insert into cv_src values (99, 'AA');
create view cv_v as select id as id, lbl as lbl,
    (select x from cv_oth where fk = id) as note from cv_base;

update cv_v set lbl = 'CHANGED' where exists (select 1 from cv_src where tag = note);
-- cv_base.id=1 -> note 'AA', present in cv_src -> row 1 writes; id=2 -> 'BB', absent -> kept.
select id, lbl from cv_base order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"B"}]

-- --- (l) negative control — no over-qualify ---
-- Same computed-lineage view, but the user subquery FROM (cv_ok) has NO `id`, so
-- it does NOT shadow the lineage's correlation column. The lineage's genuinely-
-- local `fk` / `x` (owned by cv_oth, shadowed inside the lineage subquery, and not
-- base columns of cv_base) must stay local — if the deep qualifier wrongly
-- qualified them to cv_base.fk / cv_base.x the subquery would error (cv_base has no
-- such columns) or mis-evaluate. cv_ok holds only the row-1 match.
create table cv_ok (tag text);
insert into cv_ok values ('AA');

update cv_v set lbl = 'OK' where exists (select 1 from cv_ok where tag = note);
select id, lbl from cv_base order by id;
→ [{"id":1,"lbl":"OK"},{"id":2,"lbl":"B"}]

-- --- (m) DELETE variant — the delete path threads the same deep qualifier ---
-- The (k) repro shape on the delete path, confirming rewriteViewDelete threads the
-- correlation qualifier through its subquery descent too. Only row 1 deletes.
create table cd_base (id integer primary key, lbl text);
create table cd_oth (fk integer, x text);
create table cd_src (id integer, tag text);
insert into cd_base values (1, 'A'), (2, 'B');
insert into cd_oth values (1, 'AA'), (2, 'BB');
insert into cd_src values (99, 'AA');
create view cd_v as select id as id, lbl as lbl,
    (select x from cd_oth where fk = id) as note from cd_base;

delete from cd_v where exists (select 1 from cd_src where tag = note);
select id, lbl from cd_base order by id;
→ [{"id":2,"lbl":"B"}]

-- --- (n) lineage-subquery taint reject — unresolvable FROM *inside* the computed
--          lineage term itself (distinct from blocks (e)/(f), which taint the USER
--          subquery). The deep correlation-qualifier descends into the lineage
--          subquery and must prove which names its own FROM introduces in order to
--          know whether the bare `id` correlates out to the base row. The nested
--          `(select * from cn_oth)` source makes those names unresolvable, so the
--          qualifier rejects loudly rather than guess and risk a silent wrong write.
create table cn_base (id integer primary key, lbl text);
create table cn_oth (fk integer, x text);
create table cn_src (id integer, tag text);
insert into cn_base values (1, 'A');
create view cn_v as select id as id, lbl as lbl,
    (select x from (select * from cn_oth) as s where s.fk = id) as note from cn_base;

update cn_v set lbl = 'X' where exists (select 1 from cn_src where tag = note);
-- error: not statically resolvable

-- --- (o) the deep qualifier's SHADOW logic, isolated from its base-column gate ---
-- Blocks (k)/(l)/(m) only ever exercise the base-column GATE: the lineage subquery's
-- local columns (cv_oth.fk / .x) are excluded from qualification simply because they
-- are not columns of the base table. The `shadowed`-set accumulation inside
-- the base-qualify scope-transform descent (makeBaseQualifyScope) is only the *deciding* factor when a lineage-local
-- column name ALSO collides with a base column name — then the base-column gate alone
-- would wrongly qualify it, and only the shadow set keeps it local. Here so_oth has
-- its own `id` (colliding with so_base.id). The lineage `(select x from so_oth where
-- link = k and id = 1)` filters so_oth by its OWN id (=1) and correlates on `k`. The
-- shadow set must keep `id` bound to so_oth.id (local literal filter) while qualifying
-- only `k` to so_base.k. If `id` were (wrongly) qualified to so_base.id, the local
-- `id = 1` filter would vanish (becoming the per-row-constant so_base.id = 1) and the
-- subquery would select so_oth's first link-matching row ('Q') instead of 'P' — so the
-- EXISTS against so_src(tag='P') would no longer match row 1. Asserts row 1 writes.
create table so_base (id integer primary key, k integer, lbl text);
create table so_oth (id integer, link integer, x text);
create table so_src (id integer, tag text);
insert into so_base values (1, 10, 'A'), (2, 20, 'B');
insert into so_oth values (3, 10, 'Q'), (1, 10, 'P');
insert into so_src values (99, 'P');
create view so_v as select id as id, k as k, lbl as lbl,
    (select x from so_oth where link = k and id = 1) as note from so_base;

update so_v set lbl = 'HIT' where exists (select 1 from so_src where tag = note);
-- base row1 (k=10): so_oth where link=10 and so_oth.id=1 -> 'P', matches so_src -> writes.
-- base row2 (k=20): no link=20 row -> note NULL -> kept.
select id, lbl from so_base order by id;
→ [{"id":1,"lbl":"HIT"},{"id":2,"lbl":"B"}]

-- --- (p) same-base-table self-reference: the user subquery FROM names the view's OWN
--          base table — the corner the synthesised target alias closes ---
-- The view renames base `lbl` to view column `note`; the EXISTS subquery FROM is the
-- SAME base table (sr_t). `note` substitutes to base `lbl` and is correlation-qualified.
-- A bare-base-table-name qualifier (sr_t.lbl) would bind the INNER sr_t (innermost
-- scope) -> the predicate degrades to the uncorrelated sr_t.k = sr_t.lbl, true for the
-- ('A','A') row, so EVERY outer row would pass (a silent wrong write). The synthesised
-- alias on the lowered target (__vm_self.lbl) binds the OUTER row -> the predicate is
-- the correlated sr_t.k = <outer>.lbl -> only outer row 1 (lbl 'A', matched by k='A').
create table sr_t (id integer primary key, k text, lbl text);
insert into sr_t values (1, 'A', 'A'), (2, 'Z', 'B');
create view sr_v as select id as id, lbl as note from sr_t;

update sr_v set note = 'CHANGED' where exists (select 1 from sr_t where sr_t.k = note);
select id, lbl from sr_t order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"B"}]

-- --- (q) DELETE variant — the delete path threads the same synthesised alias ---
create table dr_t (id integer primary key, k text, lbl text);
insert into dr_t values (1, 'A', 'A'), (2, 'Z', 'B');
create view dr_v as select id as id, lbl as note from dr_t;

delete from dr_v where exists (select 1 from dr_t where dr_t.k = note);
select id, lbl from dr_t order by id;
→ [{"id":2,"lbl":"B"}]

-- --- (r) RETURNING-subquery same-base-table correlation: SELF_ALIAS is threaded into
--          rewriteViewReturning, so a correlated subquery INSIDE a RETURNING clause binds
--          the outer target row the same way a WHERE subquery does. We mutate a NON-
--          correlation column (g) so the correlation column (note=lbl) is stable across
--          the write — the asserted count is unambiguous. The view renames base `lbl` to
--          `note`; the RETURNING subquery FROM is the SAME base table (rt_t). A bare-base-
--          table qualifier would de-correlate to `rt_t.k = rt_t.lbl` (matches only the one
--          ('A','A') row -> count 1); the synthesised alias binds the OUTER row's lbl 'A'
--          -> the correlated `rt_t.k = 'A'` matches BOTH rows -> count 2.
create table rt_t (id integer primary key, k text, grp text, lbl text);
insert into rt_t values (1, 'A', 'G', 'A'), (2, 'A', 'G', 'B');
create view rt_v as select id as id, grp as g, lbl as note from rt_t;

update rt_v set g = 'X' where id = 1 returning (select count(*) from rt_t where rt_t.k = note) as c;
→ [{"c":2}]

-- --- (s) `in (select ... where <correlation>)` descent form (the (p)/(q) blocks use
--          `exists`). The correlated `note` lives inside the IN-subquery's WHERE and FROMs
--          the SAME base table (ir_t). Correlated: for outer lbl 'A' the subquery yields
--          {'A'} so `'A' in (...)` holds (row 1 changes); for outer lbl 'B' it yields {} so
--          the row is untouched. De-correlated (`ir_t.k = ir_t.lbl`) would yield {'A'} for
--          EVERY outer row -> both rows would change (a silent wrong write).
create table ir_t (id integer primary key, k text, lbl text);
insert into ir_t values (1, 'A', 'A'), (2, 'Z', 'B');
create view ir_v as select id as id, lbl as note from ir_t;

update ir_v set note = 'CHANGED' where 'A' in (select ir_t.k from ir_t where ir_t.k = note);
select id, lbl from ir_t order by id;
→ [{"id":1,"lbl":"CHANGED"},{"id":2,"lbl":"B"}]

-- --- (t) a definition fragment's OWN subquery correlates by the BASE TABLE NAME ---
-- Blocks (k)-(s) all correlate the lineage subquery *unqualified* (`where fk = id`).
-- Here the view body spells the correlation with the base table's name
-- (`qt_l.id = qt_t.id`). The lowered UPDATE targets the base table under the
-- synthesised correlation name (__vm_self), so a verbatim `qt_t.` qualifier inside
-- the copied fragment resolves against nothing and the statement errored with
-- "gt.id isn't a column". normalizeBaseRefs now re-points such a nested qualifier at
-- the lowered correlation name (top-level qualifiers still strip to bare).
create table qt_t (id integer primary key, x integer);
create table qt_l (id integer primary key, lbl text);
insert into qt_t values (1, 10), (2, 20);
insert into qt_l values (1, 'one'), (2, 'two');
create view qt_v as select id as id, x as x,
    (select lbl from qt_l where qt_l.id = qt_t.id) as lbl from qt_t;

select id, x, lbl from qt_v order by id;
→ [{"id":1,"x":10,"lbl":"one"},{"id":2,"x":20,"lbl":"two"}]

update qt_v set x = 77 where lbl = 'one';
select id, x from qt_t order by id;
→ [{"id":1,"x":77},{"id":2,"x":20}]

-- --- (u) DELETE variant of (t) — the delete lowering threads the same name ---
create table qd_t (id integer primary key, x integer);
create table qd_l (id integer primary key, lbl text);
insert into qd_t values (1, 10), (2, 20);
insert into qd_l values (1, 'one'), (2, 'two');
create view qd_v as select id as id, x as x,
    (select lbl from qd_l where qd_l.id = qd_t.id) as lbl from qd_t;

delete from qd_v where lbl = 'one';
select id, x from qd_t order by id;
→ [{"id":2,"x":20}]

-- --- (v) the body ALIASES its source and correlates by the alias ---
-- Same shape as (t) but the correlation is spelled `a.id` against `from qa_t a`.
-- The alias is in the same base-qualifier set as the table name, so it re-points
-- identically; before the fix this failed with "a.id isn't a column".
create table qa_t (id integer primary key, x integer);
create table qa_l (id integer primary key, lbl text);
insert into qa_t values (1, 10), (2, 20);
insert into qa_l values (1, 'one'), (2, 'two');
create view qa_v as select a.id as id, a.x as x,
    (select lbl from qa_l where qa_l.id = a.id) as lbl from qa_t a;

update qa_v set x = 88 where lbl = 'two';
select id, x from qa_t order by id;
→ [{"id":1,"x":10},{"id":2,"x":88}]

-- --- (w) COLLISION negative control — why the nested qualifier is re-pointed and
--          not simply stripped like a top-level one ---
-- The lineage subquery's own FROM (qk_l) carries a column named `k`, the same name
-- as the correlated base column. Stripping `qk_t.k` to a bare `k` would re-bind it
-- to qk_l.k by innermost-scope rules, degrading the correlation to the tautology
-- `qk_l.k = qk_l.k`. qk_l holds exactly ONE row, so that degenerate subquery still
-- returns a single value ('one') for EVERY outer row instead of erroring: `lbl =
-- 'one'` would write BOTH rows — a silent wrong write. (Verified: with the nested
-- rewrite reduced to a strip, this block writes {"id":2,"x":9}.) Re-pointing to the
-- lowered correlation name keeps the correlation, so only the k=10 row is written.
create table qk_t (id integer primary key, k integer, x integer);
create table qk_l (k integer primary key, lbl text);
insert into qk_t values (1, 10, 100), (2, 20, 200);
insert into qk_l values (10, 'one');
create view qk_v as select id as id, x as x,
    (select lbl from qk_l where qk_l.k = qk_t.k) as lbl from qk_t;

update qk_v set x = 9 where lbl = 'one';
select id, x from qk_t order by id;
→ [{"id":1,"x":9},{"id":2,"x":200}]

-- --- (x) SHADOW negative control — a qualifier the fragment's OWN subquery FROM
--          binds stays local ---
-- The lineage subquery FROMs the view's own base table (qs_t), so `qs_t.` inside it
-- names the INNER source by innermost-scope rules and must NOT be re-pointed. The
-- pin column is therefore the same constant ('B', the id=2 row's lbl) for every
-- outer row, and `pin = 'B'` writes all three rows. Had the qualifier been
-- re-pointed, `__vm_self.id = 2` would filter per outer row instead: row 2 would see
-- an unfiltered scan (first lbl 'A') and rows 1/3 an empty one (NULL), so nothing
-- would match.
create table qs_t (id integer primary key, x integer, lbl text);
insert into qs_t values (1, 10, 'A'), (2, 20, 'B'), (3, 30, 'C');
create view qs_v as select id as id, x as x,
    (select qs_t.lbl from qs_t where qs_t.id = 2) as pin from qs_t;

update qs_v set x = 55 where pin = 'B';
select id, x from qs_t order by id;
→ [{"id":1,"x":55},{"id":2,"x":55},{"id":3,"x":55}]

-- --- (y) the base-qualified correlation lives in the VIEW'S OWN WHERE ---
-- The body's selection predicate is copied into the lowered statement the same way a
-- computed column's lineage is, so it hits the identical rewrite. Row 3 is outside
-- the view (500 < 100 is false) and must not be touched. A naive strip would collapse
-- `qw_lim.id = qw_t.id` to the tautology `qw_lim.id = qw_lim.id`, pinning lim to
-- qw_lim's first row (10) for every base row — row 2 (50 < 10) would then be wrongly
-- excluded.
create table qw_t (id integer primary key, x integer);
create table qw_lim (id integer primary key, lim integer);
insert into qw_t values (1, 5), (2, 50), (3, 500);
insert into qw_lim values (1, 10), (2, 100), (3, 100);
create view qw_v as select id as id, x as x from qw_t
    where x < (select lim from qw_lim where qw_lim.id = qw_t.id);

select id, x from qw_v order by id;
→ [{"id":1,"x":5},{"id":2,"x":50}]

update qw_v set x = 9;
select id, x from qw_t order by id;
→ [{"id":1,"x":9},{"id":2,"x":9},{"id":3,"x":500}]

-- --- (z) RETURNING re-projects the computed column through the same rewrite ---
-- The RETURNING clause re-evaluates the lineage against the post-mutation row, so
-- its nested base-qualified correlation must resolve too. Correlated, row 2 returns
-- its own label 'two'; a de-correlated fragment would return qr_l's first row ('one').
create table qr_t (id integer primary key, x integer);
create table qr_l (id integer primary key, lbl text);
insert into qr_t values (1, 10), (2, 20);
insert into qr_l values (1, 'one'), (2, 'two');
create view qr_v as select id as id, x as x,
    (select lbl from qr_l where qr_l.id = qr_t.id) as lbl from qr_t;

update qr_v set x = 33 where id = 2 returning id, x, lbl;
→ [{"id":2,"x":33,"lbl":"two"}]

-- --- (aa) INSERT … RETURNING through an ALIAS-qualified lineage correlation ---
-- INSERT lowers onto the BARE base-table name (its base statement has no target-row
-- scan for a subquery to correlate to), so it keeps the default correlation name
-- rather than __vm_self. For a body that aliases its source (`from qi_t a … a.id`)
-- the nested qualifier is therefore re-pointed `a.` → `qi_t.`, which is what makes
-- it resolvable at all — verbatim `a.` bound to nothing. RETURNING re-projects the
-- computed column, so this is the INSERT-side dual of (z).
create table qi_t (id integer primary key, x integer);
create table qi_l (id integer primary key, lbl text);
insert into qi_l values (1, 'one'), (2, 'two');
create view qi_v as select a.id as id, a.x as x,
    (select lbl from qi_l where qi_l.id = a.id) as lbl from qi_t a;

insert into qi_v (id, x) values (2, 20) returning id, x, lbl;
→ [{"id":2,"x":20,"lbl":"two"}]

select id, x, lbl from qi_v order by id;
→ [{"id":2,"x":20,"lbl":"two"}]

-- --- (ab) the correlation sits TWO subquery levels down ---
-- The nested rewrite recurses, so a qualifier inside a subquery of the lineage
-- subquery is re-pointed the same way. De-correlated at either level, `qn_m` would
-- pin to its first row (mid 100) for every base row and `lbl = 'two'` would match
-- nothing — the update would be a no-op.
create table qn_t (id integer primary key, x integer);
create table qn_m (id integer primary key, mid integer);
create table qn_l (mid integer primary key, lbl text);
insert into qn_t values (1, 10), (2, 20);
insert into qn_m values (1, 100), (2, 200);
insert into qn_l values (100, 'one'), (200, 'two');
create view qn_v as select id as id, x as x,
    (select lbl from qn_l where qn_l.mid = (select mid from qn_m where qn_m.id = qn_t.id)) as lbl
    from qn_t;

select id, lbl from qn_v order by id;
→ [{"id":1,"lbl":"one"},{"id":2,"lbl":"two"}]

update qn_v set x = 44 where lbl = 'two';
select id, x from qn_t order by id;
→ [{"id":1,"x":10},{"id":2,"x":44}]

-- --- (ac) COMPOUND legs inside a lineage subquery scope independently ---
-- A union leg is a SIBLING of its neighbour, not nested inside it: it sees the
-- ENCLOSING alias-shadow set, not the other leg's FROM. Leg 1 FROMs the view's own
-- base table, so its `qc_t.` refs are local (and its `id = 99` filter makes it
-- return nothing); leg 2 FROMs qc_l, so its `qc_t.id` is an outer correlation and
-- must be re-pointed. Had leg 2 inherited leg 1's alias set, `qc_t.id` would have
-- been left local and the statement would fail to resolve it.
create table qc_t (id integer primary key, x integer, lbl2 text);
create table qc_l (id integer primary key, lbl text);
insert into qc_t values (1, 10, 'never'), (2, 20, 'never');
insert into qc_l values (2, 'two');
create view qc_v as select id as id, x as x,
    (select qc_t.lbl2 from qc_t where qc_t.id = 99
     union all
     select lbl from qc_l where qc_l.id = qc_t.id) as lbl from qc_t;

select id, lbl from qc_v order by id;
→ [{"id":1,"lbl":null},{"id":2,"lbl":"two"}]

update qc_v set x = 77 where lbl = 'two';
select id, x from qc_t order by id;
→ [{"id":1,"x":10},{"id":2,"x":77}]

-- ===================================
-- Phase 2b: Multi-source key-preserving inner-join INSERT (shared-surrogate
-- envelope). The shared join key is not a view column, so its value is sourced
-- from the ANCHOR key column's declared `default`, evaluated ONCE per produced
-- logical row at the envelope (with `mutation_ordinal()` resolving to the row's
-- 1-based ordinal) and threaded into both base inserts via the equivalence class
-- (so the branches agree on identity). The engine itself mints nothing — the
-- basis author declares the policy. Sides are written FK-parent-before-FK-child.
-- See docs/vu-operators.md § Inner Join (Inserts), docs/vu-mutation-context.md § Mutation Context.
-- ===================================

-- --- (a) the worked example: two base tables sharing a surrogate `rid` the view
--         hides. The anchor (u_core) declares a high-water-mark allocator default
--         `coalesce(max(rid),0) + mutation_ordinal()` — the user-authored
--         reconstruction of the retired zero-config `integer-auto` mint. One value
--         per row, shared across both base inserts ---
create table u_core (rid integer primary key default (coalesce((select max(rid) from u_core), 0) + mutation_ordinal()), name text);
create table u_contact (rid integer primary key, email text);
create view appuser as
    select c.name as name, k.email as email
    from u_core c join u_contact k on k.rid = c.rid;

insert into appuser (name, email) values ('Ada', 'ada@x.io'), ('Lin', 'lin@x.io');

-- Exactly one evaluation per row (rid 1 then 2), shared across both base inserts
-- of that row, distinct across rows.
select rid, name from u_core order by rid;
→ [{"rid":1,"name":"Ada"},{"rid":2,"name":"Lin"}]
select rid, email from u_contact order by rid;
→ [{"rid":1,"email":"ada@x.io"},{"rid":2,"email":"lin@x.io"}]

-- The write is visible through the view.
select name, email from appuser order by name;
→ [{"name":"Ada","email":"ada@x.io"},{"name":"Lin","email":"lin@x.io"}]

-- A second insert allocates ABOVE the existing keys (max(rid)=2 observed before any
-- base write), and the ordinal distinguishes rows of the multi-row insert.
insert into appuser (name, email) values ('Bo', 'bo@x.io'), ('Cy', 'cy@x.io');
select rid, name from u_core order by rid;
→ [{"rid":1,"name":"Ada"},{"rid":2,"name":"Lin"},{"rid":3,"name":"Bo"},{"rid":4,"name":"Cy"}]
select rid, email from u_contact order by rid;
→ [{"rid":1,"email":"ada@x.io"},{"rid":2,"email":"lin@x.io"},{"rid":3,"email":"bo@x.io"},{"rid":4,"email":"cy@x.io"}]

-- --- (b) the allocator default sits on the FK-PARENT anchor; FK-parent inserted
--         before the FK-child, so the child's shared key references a parent that
--         exists. The default allocates above a pre-existing parent row ---
create table msi_parent (pp integer primary key default (coalesce((select max(pp) from msi_parent), 0) + mutation_ordinal()), pv integer null);
create table msi_child (cc integer primary key, pr integer null, cv integer null,
    foreign key (pr) references msi_parent(pp));
create view msi_jv as
    select c.cc as cc, c.cv as cv, p.pv as pv
    from msi_child c join msi_parent p on p.pp = c.pr;
-- a pre-existing parent (explicit pp overrides the default) so the allocator must clear it.
insert into msi_parent values (40, 400);

insert into msi_jv (cc, cv, pv) values (1, 11, 111);
-- allocated pp = max(40)+1 = 41, shared as msi_child.pr.
select pp, pv from msi_parent order by pp;
→ [{"pp":40,"pv":400},{"pp":41,"pv":111}]
select cc, pr, cv from msi_child order by cc;
→ [{"cc":1,"pr":41,"cv":11}]
select cc, cv, pv from msi_jv order by cc;
→ [{"cc":1,"cv":11,"pv":111}]

-- a multi-row insert allocates a DISTINCT key per row, each shared across both sides.
insert into msi_jv (cc, cv, pv) values (2, 22, 222), (3, 33, 333);
select pp, pv from msi_parent order by pp;
→ [{"pp":40,"pv":400},{"pp":41,"pv":111},{"pp":42,"pv":222},{"pp":43,"pv":333}]
select cc, pr, cv from msi_child order by cc;
→ [{"cc":1,"pr":41,"cv":11},{"cc":2,"pr":42,"cv":22},{"cc":3,"pr":43,"cv":33}]

-- --- (c) a NON-DETERMINISTIC anchor default (a clock read) under
--         `nondeterministic_schema`: the value is captured ONCE per row at the
--         envelope and threaded to BOTH members, so they always agree on the key
--         (the load-bearing evaluate-once-and-thread guarantee). The ordinal makes
--         each row distinct even within one clock tick ---
pragma nondeterministic_schema = true;
create table ndc_core (rid integer primary key default (epoch_ms('now') * 1000 + mutation_ordinal()), name text);
create table ndc_contact (rid integer primary key, email text);
create view ndc_v as select c.name as name, k.email as email
    from ndc_core c join ndc_contact k on k.rid = c.rid;
insert into ndc_v (name, email) values ('Ada', 'a@x'), ('Lin', 'l@x');
-- The surrogate was evaluated once per row and threaded to both members, so every
-- core row joins exactly one contact row on the identical key, and the two rows are
-- distinct. (Exact rid values are non-deterministic, so we assert the invariants.)
select (select count(*) from ndc_core) as core_rows,
       (select count(*) from ndc_core c join ndc_contact k on k.rid = c.rid) as joined_rows,
       (select count(distinct rid) from ndc_core) as distinct_rids;
→ [{"core_rows":2,"joined_rows":2,"distinct_rids":2}]
pragma nondeterministic_schema = false;

-- --- (d) directly-supplied shared key: the view exposes the join key, so no
--         default is evaluated — the supplied value threads to both sides ---
create table dk_a (k integer primary key, av text);
create table dk_b (k integer primary key, bv text);
create view dk_v as select m.k as k, m.av as av, s.bv as bv
    from dk_a m join dk_b s on s.k = m.k;

insert into dk_v (k, av, bv) values (7, 'a7', 'b7'), (9, 'a9', 'b9');
select k, av from dk_a order by k;
→ [{"k":7,"av":"a7"},{"k":9,"av":"a9"}]
select k, bv from dk_b order by k;
→ [{"k":7,"bv":"b7"},{"k":9,"bv":"b9"}]
select k, av, bv from dk_v order by k;
→ [{"k":7,"av":"a7","bv":"b7"},{"k":9,"av":"a9","bv":"b9"}]

-- --- (e) a NOT NULL NON-KEY base column with no default and no supplied value ->
--         rejected (the anchor key has a default, so this isolates the must_have check) ---
create table nd_main (k integer primary key default (coalesce((select max(k) from nd_main), 0) + mutation_ordinal()), must_have text not null);
create table nd_side (k integer primary key, sv text);
create view nd_v as select m.k as k, s.sv as sv from nd_main m join nd_side s on s.k = m.k;
insert into nd_v (sv) values ('x');
-- error: no default

-- --- (f) a surrogate shared key whose anchor column declares NO default and is not
--         supplied -> rejected (the engine no longer invents one) ---
create table skn_core (rid integer primary key, name text);
create table skn_contact (rid integer primary key, email text);
create view skn_v as select c.name as name, k.email as email
    from skn_core c join skn_contact k on k.rid = c.rid;
insert into skn_v (name, email) values ('x', 'y');
-- error: neither supplied nor declares a default

-- --- (g) a computed (non-invertible) target column is not insertable ---
create table cm_a (k integer primary key, av integer);
create table cm_b (k integer primary key, bv integer);
create view cm_v as select a.k as k, a.av + 1 as bumped, b.bv as bv
    from cm_a a join cm_b b on b.k = a.k;
insert into cm_v (bumped, bv) values (5, 6);
-- error: non-invertible

-- --- (h) the shared key exposed by more than one view column (both sides of the
--         equi-join key) is over-specified: an insert cannot honor divergent
--         supplied values, so it is rejected rather than silently dropping one ---
create table bk_a (k integer primary key, av text);
create table bk_b (k integer primary key, bv text);
create view bk_v as select a.k as ka, b.k as kb, a.av as av, b.bv as bv
    from bk_a a join bk_b b on b.k = a.k;
insert into bk_v (ka, kb, av, bv) values (5, 7, 'a', 'b');
-- error: shared join key is exposed by more than one view column

-- --- (i) `mutation_ordinal()` is a general mutation-context primitive — it reaches
--         an ordinary single-source multi-row INSERT column default (not just the
--         decomposition envelope) and resolves to each row's 1-based ordinal, where a
--         window function cannot reach (the column-default position). NB: unlike the
--         envelope (which freezes a pre-mutation snapshot), a plain insert writes
--         incrementally, so compose the ordinal with deterministic state directly
--         rather than `max()` (which would already include prior rows of the same
--         statement) ---
create table mo_seq (id integer primary key, ord integer default (mutation_ordinal()), label text);
insert into mo_seq (id, label) values (10, 'a'), (20, 'b'), (30, 'c');
select id, ord, label from mo_seq order by id;
→ [{"id":10,"ord":1,"label":"a"},{"id":20,"ord":2,"label":"b"},{"id":30,"ord":3,"label":"c"}]

-- --- (j) the anchor key default reads a SUPPLIED sibling via `new.<col>`: the
--         minted shared key derives from a value the INSERT provides through the view
--         (the same `new.<col>` surface as the single-source insert path), evaluated
--         once per row at the envelope and threaded to both members. A member-table
--         column default also reads a supplied sibling via `new.<col>`. ---
create table ek_core (
    rid integer primary key default (coalesce((select max(rid) from ek_core), 0) + new.seq),
    seq integer,
    name text
);
create table ek_contact (rid integer primary key, email text, label text default (lower(new.email)));
create view ek_v as select c.seq as seq, c.name as name, k.email as email
    from ek_core c join ek_contact k on k.rid = c.rid;

-- max(rid)=0 frozen pre-mutation for BOTH rows, so each row's shared key derives from
-- its supplied `new.seq` (5 and 3 — distinct, and unequal to the 1,2 ordinals, proving
-- the value comes from the supplied sibling rather than the ordinal).
insert into ek_v (seq, name, email) values (5, 'Ada', 'A@X'), (3, 'Lin', 'L@X');
select rid, seq, name from ek_core order by rid;
→ [{"rid":3,"seq":3,"name":"Lin"},{"rid":5,"seq":5,"name":"Ada"}]
-- the member-table `label` default read its supplied sibling `new.email`.
select rid, email, label from ek_contact order by rid;
→ [{"rid":3,"email":"L@X","label":"l@x"},{"rid":5,"email":"A@X","label":"a@x"}]
-- every member agrees on the threaded identity (the join recovers both rows).
select seq, name, email from ek_v order by seq;
→ [{"seq":3,"name":"Lin","email":"L@X"},{"seq":5,"name":"Ada","email":"A@X"}]

-- a second insert observes the post-first-insert max(rid)=5 (pre-mutation for THIS
-- statement) and adds the supplied `new.seq`: 5 + 100 = 105.
insert into ek_v (seq, name, email) values (100, 'Bo', 'B@X');
select rid, seq from ek_core order by rid;
→ [{"rid":3,"seq":3},{"rid":5,"seq":5},{"rid":105,"seq":100}]

-- --- (k) a side default reads a CROSS-SIDE supplied sibling via `new.<col>`: the
--         FK-CHILD side carries a NOT NULL column `derived` whose default references a
--         logical column (`new.pv`) the PARENT side supplies — one the child's own base
--         table does NOT carry. This resolves only because the produced row's NEW
--         context threads into every side insert's default scope (the multi-source dual
--         of the decomposition cross-member case in 03.4-defaults). Before that threading
--         it raised `new.pv isn't a column` from the per-side buildInsertStmt. ---
create table msx_parent (pp integer primary key default (coalesce((select max(pp) from msx_parent), 0) + mutation_ordinal()), pv integer null);
create table msx_child (cc integer primary key, pr integer null, cv integer null,
    derived integer not null default (new.pv * 10),
    foreign key (pr) references msx_parent(pp));
create view msx_jv as
    select c.cc as cc, c.cv as cv, p.pv as pv
    from msx_child c join msx_parent p on p.pp = c.pr;

-- pv=7 is a PARENT-side supplied column; the CHILD's `derived` default reads it
-- (new.pv * 10 = 70), proving the cross-side resolution through the threaded NEW context.
insert into msx_jv (cc, cv, pv) values (1, 11, 7);
select cc, cv, derived from msx_child order by cc;
→ [{"cc":1,"cv":11,"derived":70}]
select pp, pv from msx_parent order by pp;
→ [{"pp":1,"pv":7}]

-- multi-row: each produced row resolves its OWN parent-side pv (per-row NEW context).
insert into msx_jv (cc, cv, pv) values (2, 22, 3), (3, 33, 9);
select cc, cv, derived from msx_child order by cc;
→ [{"cc":1,"cv":11,"derived":70},{"cc":2,"cv":22,"derived":30},{"cc":3,"cv":33,"derived":90}]

-- ===================================
-- RETURNING through a view (single- and multi-source).
-- The returned rows are projected through the VIEW's column list (not the base
-- tables'), evaluated against the post-mutation base state — so a computed view
-- column re-evaluates against the post-mutation base values, a renamed view column
-- keeps its view-spelling, and `returning *` expands to the view's columns.
-- See docs/view-updateability.md § `returning` Clauses.
-- ===================================

-- --- (a) single-source: rename + computed view columns, all three ops ---
-- empv renames base id/name, exposes base rate, and a computed `doubled` = rate*2.
create table emp (id integer primary key, name text, rate integer);
create view empv as
    select id as eid, name as ename, rate as ratev, rate * 2 as doubled from emp;
-- run

-- INSERT returns the view-projected NEW row; `doubled` evaluated post-insert.
insert into empv (eid, ename, ratev) values (1, 'Ann', 5)
    returning eid, ename, ratev, doubled;
→ [{"eid":1,"ename":"Ann","ratev":5,"doubled":10}]

-- UPDATE returns the view-projected NEW row; `doubled` re-evaluated post-update.
update empv set ratev = 7 where eid = 1 returning eid, ratev, doubled;
→ [{"eid":1,"ratev":7,"doubled":14}]

-- a multi-row insert returns one row per inserted row, in insertion order; the
-- computed column is evaluated per row.
insert into empv (eid, ename, ratev) values (2, 'B', 1), (3, 'C', 2)
    returning eid, doubled;
→ [{"eid":2,"doubled":2},{"eid":3,"doubled":4}]

-- DELETE returns the view-projected OLD (deleted) row; computed col over OLD vals.
delete from empv where eid = 1 returning eid, ename, doubled;
→ [{"eid":1,"ename":"Ann","doubled":14}]

-- the surviving rows confirm only eid=1 was deleted.
select eid, ratev from empv order by eid;
→ [{"eid":2,"ratev":1},{"eid":3,"ratev":2}]

-- --- (b) single-source filter view: a selection-predicate default appears in
--         RETURNING (the appended constant is part of the inserted NEW row), and
--         `returning *` expands to the view's column list ---
create table men2 (name text primary key, color text);
create view greenmen2 as select name, color from men2 where color = 'green';
-- run

insert into greenmen2 (name) values ('Bob') returning name, color;
→ [{"name":"Bob","color":"green"}]

insert into greenmen2 (name) values ('Carl') returning *;
→ [{"name":"Carl","color":"green"}]

-- --- (c) multi-source inner-join: update/delete RETURNING returns the joined,
--         view-projected rows (a re-query of the view; update reads the
--         post-mutation image, delete captures the pre-delete image) ---
create table rp (pid integer primary key, label text);
create table rc (cid integer primary key, pref integer, note text,
    foreign key (pref) references rp(pid));
insert into rp values (10, 'P10'), (20, 'P20');
insert into rc values (1, 10, 'a'), (2, 20, 'b');
create view rjoin as
    select c.cid as cid, c.note as note, p.label as label
    from rc c join rp p on p.pid = c.pref;
-- run

-- update a child-owned column; the returned view row reflects the post-update note.
update rjoin set note = 'A' where cid = 1 returning cid, note, label;
→ [{"cid":1,"note":"A","label":"P10"}]

-- update a parent-owned column (label is owned by rp, whose PK the projection
-- hides — identified through the join); the returned view row reflects it.
update rjoin set label = 'P20x' where cid = 2 returning cid, note, label;
→ [{"cid":2,"note":"b","label":"P20x"}]

-- update touching BOTH sides; the re-query returns the fully post-mutation row.
update rjoin set note = 'Z', label = 'P10z' where cid = 1 returning cid, note, label;
→ [{"cid":1,"note":"Z","label":"P10z"}]

-- `returning *` through the join view expands to the view's columns.
delete from rjoin where cid = 1 returning *;
→ [{"cid":1,"note":"Z","label":"P10z"}]

-- the delete removed only the cid=1 child row; cid=2 remains.
select cid, note, label from rjoin order by cid;
→ [{"cid":2,"note":"b","label":"P20x"}]

-- A multi-source DELETE RETURNING of a body-COMPUTED view column (`banner`) is
-- recomputed in base terms over the planned join (NOT referenced by the optimizer-
-- eliminated intermediate output attribute id), mirroring the UPDATE RETURNING path.
create table dr_p (pid integer primary key, label text);
create table dr_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references dr_p(pid));
insert into dr_p values (10, 'P10'), (20, 'P20');
insert into dr_c values (1, 10, 'a'), (2, 20, 'b'), (3, 10, 'c');
create view dr_jv as
    select c.cid as cid, c.note as note, p.label as label, c.note || '!' as banner
    from dr_c c join dr_p p on p.pid = c.pref;
-- run

-- a RETURNING ref to a hidden base column (`pref`, not a dr_jv output) is rejected by
-- the encapsulation guard — at plan time, before any row is deleted.
delete from dr_jv where cid = 1 returning pref;
-- error: is not a column of the view

-- the body-computed column `banner` (= note || '!') is recomputed for the deleted row.
delete from dr_jv where cid = 1 returning cid, banner;
→ [{"cid":1,"banner":"a!"}]

-- `returning *` over the join view with a computed column expands and computes it.
delete from dr_jv where cid = 2 returning *;
→ [{"cid":2,"note":"b","label":"P20","banner":"b!"}]

-- a computed RETURNING expression over a base-routed column stays green.
delete from dr_jv where cid = 3 returning note || '+' as notex;
→ [{"notex":"c+"}]

-- --- (d) multi-source update RETURNING where the predicate filters on a column the
--         update rewrites — per-row identity capture (the changed rows no longer
--         match the predicate, but their captured identity does). Two children per
--         parent so a parent-side update returns multiple rows; ascending memory-vtab
--         scan order is deterministic. See docs/view-updateability.md § `returning`.
create table rp2 (pid integer primary key, label text);
create table rc2 (cid integer primary key, pref integer, note text,
    foreign key (pref) references rp2(pid));
insert into rp2 values (10, 'P10'), (20, 'P20');
insert into rc2 values (1, 10, 'a'), (2, 10, 'a'), (3, 20, 'b');
create view rjoin2 as
    select c.cid as cid, c.note as note, p.label as label
    from rc2 c join rp2 p on p.pid = c.pref;
-- run

-- predicate on the rewritten CHILD column: cid 1,2 (note='a') update to 'A'.
update rjoin2 set note = 'A' where note = 'a' returning cid, note, label;
→ [{"cid":1,"note":"A","label":"P10"},{"cid":2,"note":"A","label":"P10"}]

-- predicate on a PARENT-owned column the update rewrites (label='P10' → pid 10 →
-- children 1,2). Parent label becomes 'PX'; both children re-projected.
update rjoin2 set label = 'PX' where label = 'P10' returning cid, note, label;
→ [{"cid":1,"note":"A","label":"PX"},{"cid":2,"note":"A","label":"PX"}]

-- BOTH sides assigned (child note + parent label), predicated on the PARENT's
-- reassigned column (`label = 'PX'`) — the clash this fix closes. The FK-parent op
-- rewrites `label` before the FK-child op's identifying subquery runs, so a live
-- re-query of the join body would find no 'PX' row and the child side would silently
-- no-op; the up-front identity capture pins cid 1,2 so BOTH per-side ops land and the
-- re-query re-projects the fully post-mutation rows.
update rjoin2 set note = 'B', label = 'PY' where label = 'PX' returning cid, note, label;
→ [{"cid":1,"note":"B","label":"PY"},{"cid":2,"note":"B","label":"PY"}]

-- BOTH sides assigned, predicated on the CHILD column the update also rewrites
-- (`note = 'B'` → 'C', plus `label` → 'PZ'); the capture identifies cid 1,2 the same
-- way regardless of which side's reassigned column the predicate filters on.
update rjoin2 set note = 'C', label = 'PZ' where note = 'B' returning cid, note, label;
→ [{"cid":1,"note":"C","label":"PZ"},{"cid":2,"note":"C","label":"PZ"}]

-- `returning *` through the join update, single-side child predicate-clash.
update rjoin2 set note = 'E' where note = 'C' returning *;
→ [{"cid":1,"note":"E","label":"PZ"},{"cid":2,"note":"E","label":"PZ"}]

-- --- (e) multi-source update RETURNING edge cases: push-out-of-filter (NEW
--         semantics), renamed + computed view columns through the capture re-query
--         projection, and empty-match. See docs/view-updateability.md § `returning`.

-- push-out-of-filter: a join view WITH a body WHERE. An update that moves a row
-- OUT of the view's filter must still be RETURNED (the capture matches by identity,
-- not the now-stale predicate — single-source NEW semantics; the re-query does not
-- re-apply the body/user WHERE).
create table fp (pid integer primary key, label text);
create table fc (cid integer primary key, pref integer, note text,
    foreign key (pref) references fp(pid));
insert into fp values (10, 'P10');
insert into fc values (1, 10, 'keep'), (2, 10, 'keep');
create view fjoin as
    select c.cid as cid, c.note as note, p.label as label
    from fc c join fp p on p.pid = c.pref where c.note = 'keep';
-- run

update fjoin set note = 'gone' where cid = 1 returning cid, note, label;
→ [{"cid":1,"note":"gone","label":"P10"}]

-- the row is now out of the view filter (note != 'keep'), so the view no longer
-- shows it; only cid=2 remains visible.
select cid, note, label from fjoin order by cid;
→ [{"cid":2,"note":"keep","label":"P10"}]

-- renamed (id←cid, quantity←qty, label←lbl) + computed (dbl = qty*2) view columns
-- surface under their VIEW spelling, computed re-evaluated post-update.
create table rp3 (pid integer primary key, lbl text);
create table rc3 (cid integer primary key, pref integer, qty integer,
    foreign key (pref) references rp3(pid));
insert into rp3 values (10, 'P10');
insert into rc3 values (1, 10, 5);
create view rjoin3 as
    select c.cid as id, c.qty as quantity, c.qty * 2 as dbl, p.lbl as label
    from rc3 c join rp3 p on p.pid = c.pref;
-- run

update rjoin3 set quantity = 8 where id = 1 returning id, quantity, dbl, label;
→ [{"id":1,"quantity":8,"dbl":16,"label":"P10"}]

-- empty match returns an empty set (EXISTS over zero captured identities), not an error.
update rjoin3 set quantity = 9 where id = 999 returning id, quantity;
→ []

-- ===================================
-- Top-level view-column scope (the encapsulation guard). A `where` / `set` /
-- `returning` reference must name a column the VIEW exposes; a base-only column the
-- view projects away must NOT silently resolve against the underlying base table.
-- See docs/vu-operators.md § Selection (Top-level view-column scope).
-- ===================================

create table t3 (id integer primary key, shown text not null, secret text);
create view sv as select id, shown from t3;          -- view exposes only (id, shown)
insert into t3 values (1, 'a', 'classified-1'), (2, 'b', 'classified-2');

-- (1) RETURNING leak: `secret` is a base-only column projected away by the view.
insert into sv (id, shown) values (3, 'g') returning id, secret;
-- error: not a column of the view

-- (2) WHERE leak: filtering on the projected-away base column.
update sv set shown = 'X' where secret = 'classified-2';
-- error: not a column of the view

-- (3) SET leak: assigning the projected-away base column.
update sv set secret = 'leaked' where id = 1;
-- error: not a column of the view

-- view-qualified reference to a projected-away base column rejects too.
update sv set shown = 'X' where sv.secret = 'classified-2';
-- error: not a column of the view

-- positive: where / set / returning on real view columns still succeed.
update sv set shown = 'Y' where id = 1 returning id, shown;
→ [{"id":1,"shown":"Y"}]
delete from sv where shown = 'b' returning id;
→ [{"id":2}]
select id, shown, secret from t3 order by id;
→ [{"id":1,"shown":"Y","secret":"classified-1"}]

-- (4) SET-value leak: the assigned RHS must also name view columns. A base-only
-- column on the RHS would otherwise READ a value the view hides (`secret`) into a
-- visible column — the same encapsulation leak as the where/set-target guard.
update sv set shown = secret where id = 1;
-- error: not a column of the view
update sv set shown = upper(secret) where id = 1;
-- error: not a column of the view

-- positive: a SET value that references a real view column (here inside a function)
-- passes the guard and is evaluated normally.
update sv set shown = 'lo' where id = 1;
update sv set shown = upper(shown) where id = 1 returning id, shown;
→ [{"id":1,"shown":"LO"}]

-- INSERT target-column list is guarded the same way (a base-only column is not an
-- insertable view column).
insert into sv (id, secret) values (5, 'x');
-- error: not a column of the view

-- renamed view column: the view spelling `note` is accepted; the base spelling
-- `label` is rejected (a renamed column's only legal reference is its view name).
create table pt (id integer primary key, label text);
insert into pt values (1, 'L1'), (2, 'L2');
create view pv as select id, label as note from pt;

update pv set note = 'L1x' where note = 'L1' returning id, note;
→ [{"id":1,"note":"L1x"}]

-- the base spelling `label` is not a view column -> rejected on where / set / returning.
update pv set note = 'Z' where label = 'L2';
-- error: not a column of the view
update pv set label = 'Z' where id = 2;
-- error: not a column of the view
-- the base spelling on the SET value (RHS) is rejected too.
update pv set note = label where id = 2;
-- error: not a column of the view
insert into pv (id, note) values (3, 'L3') returning id, label;
-- error: not a column of the view

-- computed view column: it IS a view column, so `returning <computed>` reads it,
-- but `set <computed>` still yields the existing no-inverse diagnostic (NOT the
-- unknown-view-column guard — the column exists, it is just not writable).
create table ct (id integer primary key, qty integer);
insert into ct values (1, 5);
create view cvv as select id, qty, qty * 2 as dbl from ct;

update cvv set qty = 7 where id = 1 returning id, qty, dbl;
→ [{"id":1,"qty":7,"dbl":14}]
update cvv set dbl = 99 where id = 1;
-- error: read-only

-- parity: a multi-source (join) view rejects an unknown top-level column the SAME
-- way (the guard reads consistently across the single- and multi-source spines).
create table jp (pid integer primary key, plabel text);
create table jc (cid integer primary key, pref integer, note text,
    foreign key (pref) references jp(pid));
insert into jp values (10, 'P10');
insert into jc values (1, 10, 'a');
create view jvscope as select c.cid as cid, c.note as note from jc c join jp p on p.pid = c.pref;

-- `plabel` is a base column owned by jp but projected away — not a view column.
update jvscope set note = 'X' where plabel = 'P10';
-- error: not a column of the view
delete from jvscope where plabel = 'P10';
-- error: not a column of the view
-- a SET target that is not a view column at all (vs. a computed one) -> same guard.
update jvscope set plabel = 'Z' where cid = 1;
-- error: not a column of the view
-- a SET value referencing a base-only column rejects the same way (previously a
-- generic "column not found"; now the structured unknown-view-column guard).
update jvscope set note = plabel where cid = 1;
-- error: not a column of the view

-- a multi-source UPDATE … RETURNING projecting a base-only column (the re-query
-- projects over the join body) is guarded the same way.
update jvscope set note = 'Y' where cid = 1 returning cid, plabel;
-- error: not a column of the view

-- ===================================
-- Outer joins (§ Outer Joins). A LEFT outer join is partially write-through-able:
-- the PRESERVED side (left) updates / deletes / inserts normally; the NON-preserved
-- (right) side is a presence-gated optional member of the insert fan-out and is
-- read-only on UPDATE (deferred — `unsupported-outer-join-update`). The preserved
-- anchor's partner declares the high-water-mark default the both-side insert mints
-- the shared key from. FK off so an unmatched (null-extended) child can exist.
-- ===================================
pragma foreign_keys = false;
create table ojp (pp integer primary key default (coalesce((select max(pp) from ojp), 0) + mutation_ordinal()), pv integer null);
create table ojc (cc integer primary key, pr integer null references ojp(pp), cv integer null);
create view ojv as select c.cc as cc, c.cv as cv, p.pv as pv from ojc c left join ojp p on p.pp = c.pr;

insert into ojp values (1, 10);
insert into ojc values (1, 1, 100);     -- joined row
insert into ojc values (2, null, 200);  -- null-extended row (no parent)

-- The view null-extends the unmatched child.
select cc, cv, pv from ojv order by cc;
→ [{"cc":1,"cv":100,"pv":10},{"cc":2,"cv":200,"pv":null}]

-- Preserved-side update propagates to the child — including the null-extended row,
-- which stays null-extended (the parent side is untouched).
update ojv set cv = 111 where cc = 1;
update ojv set cv = 222 where cc = 2;
select cc, cv, pv from ojv order by cc;
→ [{"cc":1,"cv":111,"pv":10},{"cc":2,"cv":222,"pv":null}]

-- A non-preserved-side update now propagates per row (the matched-update / null-extended-
-- insert materialization). cc=1 is matched (parent pp=1), so this updates the parent in
-- place; the null-extended materialization branch is exercised in property.spec.ts.
update ojv set pv = 99 where cc = 1;
select cc, pv from ojv where cc = 1;
→ [{"cc":1,"pv":99}]

-- Both-side insert: child + (presence-gated) parent under the minted shared key.
insert into ojv (cc, cv, pv) values (3, 33, 333);
select cc, cv, pv from ojv where cc = 3;
→ [{"cc":3,"cv":33,"pv":333}]

-- Preserved-only insert: a single child insert; the row reads back null-extended.
insert into ojv (cc, cv) values (4, 44);
select cc, cv, pv from ojv where cc = 4;
→ [{"cc":4,"cv":44,"pv":null}]

-- A non-preserved-only insert has no preserved anchor row to attach to — rejected.
insert into ojv (pv) values (999);
-- error: only non-preserved-side columns

-- Delete routes to the preserved (child) side: the joined row disappears and the
-- parent survives; a null-extended row is deletable too.
delete from ojv where cc = 1;
delete from ojv where cc = 2;
select cc, cv, pv from ojv order by cc;
→ [{"cc":3,"cv":33,"pv":333},{"cc":4,"cv":44,"pv":null}]
-- the parent of the deleted joined row survives (the delete hit only the child side),
-- carrying the pv=99 written through the matched non-preserved-side update above.
select pv from ojp order by pv;
→ [{"pv":99},{"pv":333}]

-- ===================================
-- Outer (LEFT) join NON-PRESERVED-side UPDATE … RETURNING. The post-mutation re-query
-- re-keys its identity off the stable PRESERVED-side (child) PK via a matched-OR-null
-- disjunction (preserved side = exact PK equality; non-preserved side = matched OR
-- captured-null), so a MATCHED row (parent updated in place), a freshly-MATERIALIZED
-- null-extended row (parent minted), and a NULL-key no-op row all surface with their
-- correct post-mutation NEW image. See docs/view-updateability.md § `returning` Clauses.
-- ===================================
pragma foreign_keys = false;
create table ojrp (pp integer primary key, pv integer null);
create table ojrc (cc integer primary key, pr integer null, cv integer null);
create view ojrv as select c.cc as cc, c.cv as cv, p.pv as pv from ojrc c left join ojrp p on p.pp = c.pr;

insert into ojrp values (10, 100);
insert into ojrc values (1, 10, 1000);    -- matched
insert into ojrc values (2, 99, 2000);    -- null-extended, dangling key (materializes)
insert into ojrc values (3, null, 3000);  -- null-extended, NULL key (no-op)

-- Matched-only: parent pp=10 updated in place; the matched disjunction branch identifies it.
update ojrv set pv = 444 where cc = 1 returning cc, pv;
→ [{"cc":1,"pv":444}]

-- Null-extended materialization: the non-preserved PK was captured NULL, so the row is
-- identified by the child PK alone (the null branch) and the freshly minted parent surfaces.
update ojrv set pv = 555 where cc = 2 returning cc, cv, pv;
→ [{"cc":2,"cv":2000,"pv":555}]

-- Null-key no-op: nothing materializes, but the affected child row still returns its
-- unchanged post-mutation image (pv null).
update ojrv set pv = 666 where cc = 3 returning cc, pv;
→ [{"cc":3,"pv":null}]

-- Mixed batch over the (now all-joined cc=1,2 + null-key cc=3) view; `returning *` expands
-- to the view's columns. Ascending child-PK scan order is deterministic.
update ojrv set pv = 777 where cc in (1, 2, 3) returning *;
→ [{"cc":1,"cv":1000,"pv":777},{"cc":2,"cv":2000,"pv":777},{"cc":3,"cv":3000,"pv":null}]

-- Preserved-side (child cv) update over a batch including the null-extended cc=3 row +
-- RETURNING: the captured-null parent PK is recovered by the null branch, so cc=3 is NOT
-- dropped (the latent partial-set bug this fix closes).
update ojrv set cv = 99 where cc in (1, 3) returning cc, cv, pv;
→ [{"cc":1,"cv":99,"pv":777},{"cc":3,"cv":99,"pv":null}]

-- ===================================
-- Outer (LEFT) join NON-PRESERVED-side UPDATE — FAN-OUT (one non-preserved partner shared by
-- several preserved rows). The matched read-back and the materialization INSERT de-dup per
-- non-preserved partner (`min` read-back / `group by` join key), so a shared-partner write
-- applies ONCE: a matched fan-out no longer errors `Scalar subquery returned more than one
-- row`, and a materialization fan-out no longer double-inserts the partner PK
-- (view-write-outer-join-nonpreserved-fanout-update). See docs/vu-operators.md § Outer Joins.
-- ===================================
pragma foreign_keys = false;
create table fof_parent (pp integer primary key, pv integer null);
create table fof_child (cc integer primary key, pr integer null, cv integer null);
create view fofv as select c.cc as cc, c.cv as cv, p.pv as pv from fof_child c left join fof_parent p on p.pp = c.pr;

-- Matched fan-out: cc=1 and cc=4 share ONE existing parent (pp = 10). The non-preserved pv
-- update applies once to that parent; the unselected sibling cc=5 (also on parent 10) reads it.
insert into fof_parent values (10, 100);
insert into fof_child values (1, 10, 1000);
insert into fof_child values (4, 10, 4000);
insert into fof_child values (5, 10, 5000);
update fofv set pv = 5 where cc in (1, 4);
select cc, cv, pv from fofv order by cc;
→ [{"cc":1,"cv":1000,"pv":5},{"cc":4,"cv":4000,"pv":5},{"cc":5,"cv":5000,"pv":5}]
-- Exactly one parent row, updated once.
select pp, pv from fof_parent order by pp;
→ [{"pp":10,"pv":5}]

-- Materialization fan-out: cc=6 and cc=7 null-extend on the SAME dangling key (pr = 99, no
-- parent 99). The partner materializes ONCE (group by the join key); both children join it.
delete from fof_parent;
delete from fof_child;
insert into fof_child values (6, 99, 6000);
insert into fof_child values (7, 99, 7000);
update fofv set pv = 7 where cc in (6, 7);
select cc, cv, pv from fofv order by cc;
→ [{"cc":6,"cv":6000,"pv":7},{"cc":7,"cv":7000,"pv":7}]
select pp, pv from fof_parent order by pp;
→ [{"pp":99,"pv":7}]

-- Both fan-outs with RETURNING: each affected child surfaces once (ascending child-PK order).
delete from fof_parent;
delete from fof_child;
insert into fof_parent values (10, 100);
insert into fof_child values (1, 10, 1000);
insert into fof_child values (4, 10, 4000);
update fofv set pv = 5 where cc in (1, 4) returning cc, cv, pv;
→ [{"cc":1,"cv":1000,"pv":5},{"cc":4,"cv":4000,"pv":5}]

delete from fof_parent;
delete from fof_child;
insert into fof_child values (6, 99, 6000);
insert into fof_child values (7, 99, 7000);
update fofv set pv = 7 where cc in (6, 7) returning cc, cv, pv;
→ [{"cc":6,"cv":6000,"pv":7},{"cc":7,"cv":7000,"pv":7}]

-- ===================================
-- Outer-join INSERT with FK ENFORCEMENT ON (the per-row conditional key thread).
-- The preserved (FK-child) side threads the minted shared key into its join column
-- only for rows whose non-preserved (FK-parent) partner is present that row; for a row
-- whose pv is null the partner insert is dropped and the child's `pr` is nulled — so
-- the FK never dangles (previously: `CHECK constraint failed: _fk_ojc2_pr`).
-- ===================================
pragma foreign_keys = true;
create table ojp2 (pp integer primary key default (coalesce((select max(pp) from ojp2), 0) + mutation_ordinal()), pv integer null);
create table ojc2 (cc integer primary key, pr integer null references ojp2(pp), cv integer null);
create view ojv2 as select c.cc as cc, c.cv as cv, p.pv as pv from ojc2 c left join ojp2 p on p.pp = c.pr;

-- Both-side insert with a NULL pv: the parent is dropped (presence-gated absent), the
-- child's `pr` is nulled, no FK violation. The row reads back null-extended.
insert into ojv2 (cc, cv, pv) values (5, 55, null);
select cc, cv, pv from ojv2 where cc = 5;
→ [{"cc":5,"cv":55,"pv":null}]
-- ojc2.pr is null (no dangling FK), and no parent was minted.
select pr from ojc2 where cc = 5;
→ [{"pr":null}]
select count(*) as n from ojp2;
→ [{"n":0}]

-- A non-null pv in the SAME multi-row insert still mints + threads the real key: the
-- parent materializes and the child's `pr` points at it (null pv row stays null-extended).
insert into ojv2 (cc, cv, pv) values (6, 66, null), (7, 77, 777);
select cc, cv, pv from ojv2 where cc in (6, 7) order by cc;
→ [{"cc":6,"cv":66,"pv":null},{"cc":7,"cv":77,"pv":777}]
-- cc=6 (null pv) reads pr null; cc=7 (non-null pv) reads pr = the minted parent key.
select cc, case when pr is null then 'null' else 'fk' end as pr_kind from ojc2 where cc in (6, 7) order by cc;
→ [{"cc":6,"pr_kind":"null"},{"cc":7,"pr_kind":"fk"}]
-- exactly one parent row materialized (for cc=7); its pv is the supplied 777.
select pv from ojp2 order by pv;
→ [{"pv":777}]

-- ===================================
-- Multi-parent shared key (one child FK column references TWO optional parents on the
-- same value). A single shared key cannot reference one parent but not the other, so a
-- partial-supply row would silently lose the supplied value AND orphan the present
-- parent. The shape is rejected at plan time (was: silent wrong result + orphan).
-- Two inline `references` clauses on one column parse into two FK constraints.
-- ===================================
pragma foreign_keys = true;
create table mpp1 (pp integer primary key default (coalesce((select max(pp) from mpp1), 0) + mutation_ordinal()), pv integer null);
create table mpp2 (qq integer primary key default (coalesce((select max(qq) from mpp2), 0) + mutation_ordinal()), qv integer null);
create table mpc (c integer primary key, pr integer null references mpp1(pp) references mpp2(qq), cv integer null);
create view mpv as
    select x.c as c, x.cv as cv, a.pv as pv, b.qv as qv
    from mpc x left join mpp1 a on a.pp = x.pr
               left join mpp2 b on b.qq = x.pr;

-- Partial supply (p1 only): rejected at plan time — no orphan, no silent loss.
insert into mpv (c, cv, pv, qv) values (2, 200, 30, null);
-- error: single shared key

-- The reject is static (shape-based), so even an all-parents-supplied row still rejects
-- (the v1 over-rejection tradeoff; per-parent key columns are the future generalization).
insert into mpv (c, cv, pv, qv) values (3, 300, 30, 40);
-- error: single shared key

-- The single-PARENT-columns shape (qv omitted) is NOT caught by this reject — p2 is
-- statically inactive, so groups.length == 1. Under FK enforcement the child's pr then
-- dangles the (untouched) mpp2 FK and errors loudly at runtime — pre-existing behavior,
-- out of scope here (it is not the silent-loss bug). Left unasserted to avoid coupling to
-- the exact FK-violation message text.

-- No parent (or child) row leaked from the rejected inserts above.
select count(*) as n from mpp1;
→ [{"n":0}]
select count(*) as n from mpp2;
→ [{"n":0}]
select count(*) as n from mpc;
→ [{"n":0}]
pragma foreign_keys = false;

-- The reject is shape-based (driven by the FK *schema*, via sideDeclaresFkOnto), not by
-- runtime enforcement, so it still fires with foreign_keys OFF — pragma-independent.
insert into mpv (c, cv, pv, qv) values (4, 400, 30, 40);
-- error: single shared key

-- ===================================
-- σ (where-clause) constant-FD insert defaulting (multisource-insert-sigma-fd-defaulting):
-- the join body's `where col = literal` equality constants are lifted as per-side insert
-- defaults — the multi-source analog of the single-source § Selection rule (line ~118).
-- An omitted σ-constrained column is supplied its σ constant, so the inserted row satisfies
-- the view predicate and is visible THROUGH the view (matching the single-source path); an
-- explicit value that contradicts the σ constant rejects at plan time (predicate-contradiction).
-- ===================================
pragma foreign_keys = false;

-- --- (σ1) the repro: σ on a PROJECTED-AWAY column (`color` is not an SV output column), key
--          supplied. The omitted `color` is defaulted to 'red' on sdj1, so the row is visible
--          through SV — without the lift it would land NULL and be invisible. ---
create table sdj1 (id integer primary key, x integer, color text null);
create table sdj2 (id integer primary key, y integer null);
insert into sdj1 values (1,10,'red');
insert into sdj2 values (1,100);
create view sdjv as select sdj1.id as id, sdj1.x as x from sdj1 join sdj2 on sdj1.id = sdj2.id where sdj1.color = 'red';

insert into sdjv (id, x) values (5, 50);
-- color DEFAULTED to the σ constant on sdj1 (the projected-away column).
select id, x, color from sdj1 order by id;
→ [{"id":1,"x":10,"color":"red"},{"id":5,"x":50,"color":"red"}]
select id, y from sdj2 order by id;
→ [{"id":1,"y":100},{"id":5,"y":null}]
-- the inserted row is VISIBLE through the view (it satisfies `where color='red'`).
select id, x from sdjv order by id;
→ [{"id":1,"x":10},{"id":5,"x":50}]

-- σ-defaulting also applies to a SELECT source — a strict capability gain over the single-
-- source path (which defers σ-defaulting for SELECT sources). The σ constant rides the side
-- projection, not the VALUES rows, so a SELECT-sourced insert defaults `color` too.
insert into sdjv (id, x) select 11, 110;
select id, x, color from sdj1 where id = 11;
→ [{"id":11,"x":110,"color":"red"}]

-- --- (σ2) σ on a PROJECTED column, supplied. The matching value succeeds (idempotent — no
--          default, no reject); a contradicting value rejects at plan time. ---
create table sdc1 (id integer primary key, x integer null, color text null);
create table sdc2 (id integer primary key, y integer null);
insert into sdc1 values (1,10,'red');
insert into sdc2 values (1,100);
create view sdjp as select sdc1.id as id, sdc1.x as x, sdc1.color as color from sdc1 join sdc2 on sdc1.id = sdc2.id where sdc1.color = 'red';

insert into sdjp (id, x, color) values (6, 60, 'red');
select id, x, color from sdc1 order by id;
→ [{"id":1,"x":10,"color":"red"},{"id":6,"x":60,"color":"red"}]

-- a supplied value CONTRADICTING the σ constant rejects at plan time.
insert into sdjp (id, x, color) values (7, 70, 'blue');
-- error: contradicts its selection predicate
-- ...the statement never executed: sdc1 unchanged.
select id, x, color from sdc1 order by id;
→ [{"id":1,"x":10,"color":"red"},{"id":6,"x":60,"color":"red"}]

-- a multi-row insert: the contradiction scan walks EVERY row and rejects if ANY cell
-- contradicts the σ constant (here the second row's color='blue').
insert into sdjp (id, x, color) values (10, 100, 'red'), (11, 110, 'blue');
-- error: contradicts its selection predicate
select count(*) as n from sdc1 where id in (10, 11);
→ [{"n":0}]

-- --- (σ2b) a RENAMED σ column (`color as hue`): the contradiction match is on the resolved
--           BASE column, not the view spelling, so supplying `hue` contradicting the σ rejects;
--           the matching value succeeds. ---
create view sdjr as select sdc1.id as id, sdc1.color as hue from sdc1 join sdc2 on sdc1.id = sdc2.id where sdc1.color = 'red';
insert into sdjr (id, hue) values (8, 'blue');
-- error: contradicts its selection predicate
insert into sdjr (id, hue) values (9, 'red');
select id, color from sdc1 where id = 9;
→ [{"id":9,"color":"red"}]

-- --- (σ3) σ over a MINTED shared key: a σ-constrained non-key, non-projected column (`tier`,
--          NOT NULL with no default) on the anchor side is defaulted while the key `rid` is
--          still minted once per row from the anchor's high-water default. The σ default also
--          legitimately covers the NOT-NULL-without-default column (no spurious `no-default`). ---
create table sdmk_core (rid integer primary key default (coalesce((select max(rid) from sdmk_core), 0) + mutation_ordinal()), name text, tier text not null);
create table sdmk_contact (rid integer primary key, email text);
create view sdmkv as select c.name as name, k.email as email
    from sdmk_core c join sdmk_contact k on k.rid = c.rid where c.tier = 'gold';

insert into sdmkv (name, email) values ('Ada', 'ada@x.io');
-- rid minted once (=1) and shared; tier defaulted to 'gold' on the anchor (sdmk_core).
select rid, name, tier from sdmk_core order by rid;
→ [{"rid":1,"name":"Ada","tier":"gold"}]
select rid, email from sdmk_contact order by rid;
→ [{"rid":1,"email":"ada@x.io"}]
select name, email from sdmkv order by name;
→ [{"name":"Ada","email":"ada@x.io"}]

-- --- (σ4) OUTER join with σ on the NON-preserved side. A both-side insert makes that side
--          active, so the σ default applies and the row is visible; a preserved-only insert
--          leaves the side inactive (no base row to default into), so the row reads back null-
--          extended/invisible — a documented structural residual — but the insert SUCCEEDS. ---
create table sdop (pp integer primary key default (coalesce((select max(pp) from sdop), 0) + mutation_ordinal()), pv integer null, kind text not null);
create table sdoc (cc integer primary key, pr integer null references sdop(pp), cv integer null);
create view sdojv as select c.cc as cc, c.cv as cv, p.pv as pv
    from sdoc c left join sdop p on p.pp = c.pr where p.kind = 'gold';

-- both-side insert: the non-preserved side (sdop) is active, so its σ column `kind` defaults to
-- 'gold' and the materialized parent satisfies `where p.kind='gold'` ⇒ the row is VISIBLE.
insert into sdojv (cc, cv, pv) values (3, 33, 333);
select cc, cv, pv from sdojv where cc = 3;
→ [{"cc":3,"cv":33,"pv":333}]
select pv, kind from sdop order by pv;
→ [{"pv":333,"kind":"gold"}]

-- preserved-only insert: sdop is INACTIVE (no base row), so no σ default — the row reads back
-- null-extended and is INVISIBLE through the σ-constrained view, yet the insert SUCCEEDS (sdoc
-- is written). This is the documented structural residual.
insert into sdojv (cc, cv) values (4, 44);
select cc, cv, pv from sdojv where cc = 4;
→ []
select cc, cv from sdoc where cc = 4;
→ [{"cc":4,"cv":44}]

-- --- (σ5) MULTIPLE σ columns on ONE side, both projected away (`color`, `region` — both NOT
--          NULL with no default). The per-side σ-default loop appends BOTH constant projections
--          (and both base columns to the side insert's column list), so both are defaulted and
--          legitimately cover their NOT-NULL-without-default columns; the row is visible. ---
create table sdm1 (id integer primary key, x integer, color text not null, region text not null);
create table sdm2 (id integer primary key, y integer null);
insert into sdm1 values (1,10,'red','west');
insert into sdm2 values (1,100);
create view sdmv as select sdm1.id as id, sdm1.x as x from sdm1 join sdm2 on sdm1.id = sdm2.id
    where sdm1.color = 'red' and sdm1.region = 'west';

insert into sdmv (id, x) values (5, 50);
-- BOTH σ columns defaulted on sdm1 (the multi-default-per-side path); neither raises no-default.
select id, x, color, region from sdm1 order by id;
→ [{"id":1,"x":10,"color":"red","region":"west"},{"id":5,"x":50,"color":"red","region":"west"}]
-- the inserted row is VISIBLE through the view (it satisfies both σ conjuncts).
select id, x from sdmv order by id;
→ [{"id":1,"x":10},{"id":5,"x":50}]
pragma foreign_keys = false;

-- ===================================
-- SUPPLIED shared key (the join key is a writable view column, not minted): the per-row
-- key gate must NOT fire. The user's explicit key may reference a PRE-EXISTING parent the
-- insert never touches (pv left null because the parent already exists); nulling it would
-- silently discard the supplied key and orphan the child. FK enforcement validates a bad
-- reference instead. (Regression: the minted-key gate wrongly nulled the supplied key.)
-- ===================================
create table skp (pp integer primary key, pv integer null);
create table skc (cc integer primary key, pr integer null references skp(pp), cv integer null);
create view skv as select c.cc as cc, c.pr as k, c.cv as cv, p.pv as pv from skc c left join skp p on p.pp = c.pr;
-- A parent that already exists; the child points at it via the supplied key with pv null.
insert into skp (pp, pv) values (42, 4242);
insert into skv (cc, k, cv, pv) values (7, 42, 99, null);
-- The supplied key is preserved (pr = 42), so the child joins the pre-existing parent.
select cc, k, cv, pv from skv where cc = 7;
→ [{"cc":7,"k":42,"cv":99,"pv":4242}]
select pr from skc where cc = 7;
→ [{"pr":42}]
pragma foreign_keys = false;

-- ===================================
-- RIGHT outer join (§ Outer Joins) — the mirror of the LEFT `ojv` block above. In
-- `A right join B`, B (the right operand) is PRESERVED and A (left) is NON-preserved. With
-- the child (the FK-key holder) on the right and the parent on the left, the preserved /
-- non-preserved routing is identical to LEFT with the operands swapped: a preserved-side
-- update / delete / insert works normally; a non-preserved-side update materializes per
-- row via the EC join key; a both-side insert mints the shared key from the FK-parent's
-- high-water default. The substrate keys off JoinSide.preserved, not source order, so the
-- swap is transparent (view-write-right-join-readmit). FK off so an unmatched parent slot
-- is fine.
-- ===================================
pragma foreign_keys = false;
create table rojp (pp integer primary key default (coalesce((select max(pp) from rojp), 0) + mutation_ordinal()), pv integer null);
create table rojc (cc integer primary key, pr integer null references rojp(pp), cv integer null);
create view rojv as select p.pv as pv, c.cc as cc, c.cv as cv from rojp p right join rojc c on p.pp = c.pr;

insert into rojp values (1, 10);
insert into rojc values (1, 1, 100);     -- joined row
insert into rojc values (2, null, 200);  -- null-extended row (no parent)
insert into rojc values (5, 5, 500);     -- null-extended row with a CONCRETE dangling key

-- The view null-extends every preserved (child) row whose parent is absent.
select cc, cv, pv from rojv order by cc;
→ [{"cc":1,"cv":100,"pv":10},{"cc":2,"cv":200,"pv":null},{"cc":5,"cv":500,"pv":null}]

-- Preserved-side (child) update propagates to rojc — including the null-extended rows,
-- which stay null-extended (the parent side is untouched).
update rojv set cv = 111 where cc = 1;
update rojv set cv = 222 where cc = 2;
select cc, cv, pv from rojv order by cc;
→ [{"cc":1,"cv":111,"pv":10},{"cc":2,"cv":222,"pv":null},{"cc":5,"cv":500,"pv":null}]

-- Non-preserved-side (parent) update, MATCHED row: cc=1 has parent pp=1, so this updates
-- the parent in place (no new parent row).
update rojv set pv = 99 where cc = 1;
select cc, pv from rojv where cc = 1;
→ [{"cc":1,"pv":99}]

-- Non-preserved-side update, NULL-EXTENDED row with a concrete dangling key (cc=5, pr=5,
-- no parent yet): materializes rojp(pp=5, pv=555) via the EC join key (pp = pr = 5), so the
-- child now joins it and the row reads back joined.
update rojv set pv = 555 where cc = 5;
select cc, pv from rojv where cc = 5;
→ [{"cc":5,"pv":555}]

-- Both-side insert: child + (presence-gated) parent under the minted shared key.
insert into rojv (cc, cv, pv) values (3, 33, 333);
select cc, cv, pv from rojv where cc = 3;
→ [{"cc":3,"cv":33,"pv":333}]

-- Preserved-only insert: a single child insert; the row reads back null-extended.
insert into rojv (cc, cv) values (4, 44);
select cc, cv, pv from rojv where cc = 4;
→ [{"cc":4,"cv":44,"pv":null}]

-- A non-preserved-only insert has no preserved anchor row to attach to — rejected.
insert into rojv (pv) values (999);
-- error: only non-preserved-side columns

-- Delete routes to the preserved (child) side: the joined row disappears.
delete from rojv where cc = 1;
delete from rojv where cc = 2;
select cc, cv, pv from rojv order by cc;
→ [{"cc":3,"cv":33,"pv":333},{"cc":4,"cv":44,"pv":null},{"cc":5,"cv":500,"pv":555}]
pragma foreign_keys = false;

-- ===================================
-- RIGHT join NON-PRESERVED-side UPDATE — FAN-OUT (the mirror of the LEFT `fofv` block). The
-- parent (left operand) is NON-preserved and shared by several preserved children; the per-
-- partner de-dup keys off JoinSide.preserved, not source order, so the swap is transparent.
-- ===================================
pragma foreign_keys = false;
create table rfof_parent (pp integer primary key, pv integer null);
create table rfof_child (cc integer primary key, pr integer null, cv integer null);
create view rfofv as select p.pv as pv, c.cc as cc, c.cv as cv from rfof_parent p right join rfof_child c on p.pp = c.pr;

-- Matched fan-out: cc=1 and cc=4 share one existing parent pp=10 — pv update applies once.
insert into rfof_parent values (10, 100);
insert into rfof_child values (1, 10, 1000);
insert into rfof_child values (4, 10, 4000);
insert into rfof_child values (5, 10, 5000);
update rfofv set pv = 5 where cc in (1, 4);
select cc, cv, pv from rfofv order by cc;
→ [{"cc":1,"cv":1000,"pv":5},{"cc":4,"cv":4000,"pv":5},{"cc":5,"cv":5000,"pv":5}]
select pp, pv from rfof_parent order by pp;
→ [{"pp":10,"pv":5}]

-- Materialization fan-out: cc=6 and cc=7 null-extend on the same dangling key pr=99 — the
-- partner materializes once.
delete from rfof_parent;
delete from rfof_child;
insert into rfof_child values (6, 99, 6000);
insert into rfof_child values (7, 99, 7000);
update rfofv set pv = 7 where cc in (6, 7);
select cc, cv, pv from rfofv order by cc;
→ [{"cc":6,"cv":6000,"pv":7},{"cc":7,"cv":7000,"pv":7}]
select pp, pv from rfof_parent order by pp;
→ [{"pp":99,"pv":7}]

-- ===================================
-- RIGHT join, PRESERVED-side PK HIDDEN by the projection: the preserved side is the right
-- operand and its PK (hp_b.bid) is not projected, yet a preserved-side update reconstructs
-- the row identity from the planned join body (the capture pins base PKs regardless of
-- projection — the mirror of the inner-join tj2.id hidden-PK case).
-- ===================================
create table hp_a (aid integer primary key, av text);
create table hp_b (bid integer primary key, bv text);
insert into hp_b values (1, 'x');
create view hpv as select a.av as av, b.bv as bv from hp_a a right join hp_b b on a.aid = b.bid;
-- hp_b(1) is preserved with no hp_a match → av null-extends.
select av, bv from hpv;
→ [{"av":null,"bv":"x"}]
-- Preserved-side update with the PK hidden: identity reconstructed from the body.
update hpv set bv = 'y' where bv = 'x';
select av, bv from hpv;
→ [{"av":null,"bv":"y"}]

-- ===================================
-- Self-join RIGHT (alias-keyed preserved/non-preserved split): `rsj l right join rsj r`
-- makes the RIGHT alias `r` preserved and the LEFT alias `l` non-preserved — routing is by
-- alias, not table name. A read-back guards the right self-join variant; a preserved-side
-- (right-alias) update routes by that alias's identity.
-- ===================================
create table rsj (id integer primary key, fk integer null, x text);
insert into rsj values (1, null, 'r1');
insert into rsj values (2, 1, 'r2');
create view rsjv as select l.x as lx, r.id as rid, r.x as rx from rsj l right join rsj r on l.id = r.fk;
-- Every r row is preserved; l matches where l.id = r.fk. r1(fk null) null-extends l; r2(fk 1)
-- matches l = r1.
select lx, rid, rx from rsjv order by rid;
→ [{"lx":null,"rid":1,"rx":"r1"},{"lx":"r1","rid":2,"rx":"r2"}]
-- Preserved-side (right-alias) update routes to rsj by the right alias's identity.
update rsjv set rx = 'r2b' where rid = 2;
select lx, rid, rx from rsjv order by rid;
→ [{"lx":null,"rid":1,"rx":"r1"},{"lx":"r1","rid":2,"rx":"r2b"}]

-- ===================================
-- CTE name as a DML write target. A leading `with t as (…)` makes the CTE name a real
-- DML target: update / insert / delete route the CTE body through the SAME view-mutation
-- substrate a named view uses (an ephemeral view-like adapter). A single-source
-- projection-and-filter CTE body produces a byte-identical base-op plan to the equivalent
-- named view, so the observable base-table state matches. No grammar change — a CTE name
-- already parses as a bare identifier; this is pure resolution + routing.
-- See docs/vu-operators.md § Common Table Expressions and the CTE-name DML target.
-- ===================================

create table cte_base (id integer primary key, color text);
insert into cte_base values (1,'red'),(2,'green'),(3,'blue');

-- CTE Round-Trip Law — UPDATE through the CTE target lands on the base table.
with t as (select id, color from cte_base) update t set color = 'x' where id = 1;
select * from cte_base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"green"},{"id":3,"color":"blue"}]

-- INSERT through the CTE target.
with t as (select id, color from cte_base) insert into t (id, color) values (4, 'k');
select * from cte_base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"green"},{"id":3,"color":"blue"},{"id":4,"color":"k"}]

-- DELETE through the CTE target.
with t as (select id, color from cte_base) delete from t where id = 2;
select * from cte_base order by id;
→ [{"id":1,"color":"x"},{"id":3,"color":"blue"},{"id":4,"color":"k"}]

-- Parity: the equivalent named view, from the same starting rows and the same DML
-- sequence, leaves byte-identical base-table state.
create table cte_view_base (id integer primary key, color text);
insert into cte_view_base values (1,'red'),(2,'green'),(3,'blue');
create view cte_tv as select id, color from cte_view_base;
update cte_tv set color = 'x' where id = 1;
insert into cte_tv (id, color) values (4, 'k');
delete from cte_tv where id = 2;
select * from cte_view_base order by id;
→ [{"id":1,"color":"x"},{"id":3,"color":"blue"},{"id":4,"color":"k"}]

-- RETURNING through the CTE target (the OLD/NEW machinery the base op embeds).
create table cte_ret (id integer primary key, color text);
insert into cte_ret values (1,'red');
with t as (select id, color from cte_ret) update t set color='ret' where id=1 returning id, color;
→ [{"id":1,"color":"ret"}]

-- RETURNING <q>.* qualifier validation against a CTE-name target: the only valid
-- qualifier is the CTE name (`resolveCteTarget` sets `view.name = cte.name`).
with t as (select id, color from cte_ret) update t set color='x2' where id=1 returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

with t as (select id, color from cte_ret) update t set color='cret' where id=1 returning t.*;
→ [{"id":1,"color":"cret"}]

-- Shadow: a CTE named the same as a real table SHADOWS it as the write TARGET, but the
-- CTE body's own `from sbase` reads the REAL table (a non-recursive CTE cannot reference
-- itself), so the lowered base op writes the real `sbase`. This is the load-bearing
-- self-reference case.
create table sbase (id integer primary key, color text);
insert into sbase values (1,'red'),(2,'green');
with sbase as (select id, color from sbase) update sbase set color='x' where id=1;
select * from sbase order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"green"}]

-- Schema-qualified target never resolves to a CTE (a CTE is never schema-qualified):
-- `main.sbase` writes the REAL table even with a same-named leading CTE in scope.
with sbase as (select id, color from sbase) update main.sbase set color='q' where id=2;
select * from sbase order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"q"}]

-- Sibling CTE read in the SET value resolves: the statement's CTEs are threaded into
-- scope; only the TARGET's own name is shadowed out of its body.
create table sib_b (id integer primary key, color text);
insert into sib_b values (1,'red');
create table sib_vals (v text);
insert into sib_vals values ('fromcte');
with vv as (select v from sib_vals), t as (select id, color from sib_b)
    update t set color = (select v from vv) where id = 1;
select * from sib_b order by id;
→ [{"id":1,"color":"fromcte"}]

-- Read-gap closure: an ORDINARY base-table UPDATE may now read a CTE in its WHERE
-- (update/delete previously ignored the leading WITH entirely).
create table rg (id integer primary key, n integer);
insert into rg values (1,10),(2,20);
with keep as (select 1 as id) update rg set n=99 where id in (select id from keep);
select * from rg order by id;
→ [{"id":1,"n":99},{"id":2,"n":20}]

-- Multi-source (join) CTE body: composes through the same join substrate as a join view.
create table cj_p (pid integer primary key, label text);
create table cj_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references cj_p(pid));
insert into cj_p values (10,'P10');
insert into cj_c values (1,10,'a');
with j as (select c.cid as cid, c.note as note, p.label as label
    from cj_c c join cj_p p on p.pid = c.pref)
    update j set note='B' where cid=1;
select cid, note from cj_c order by cid;
→ [{"cid":1,"note":"B"}]

-- A `with recursive` clause whose TARGET member is a plain non-self-referential body is
-- still writable: the reject gates on actual self-reference, not the `recursive` keyword.
create table nr (id integer primary key, color text);
insert into nr values (1,'red'),(2,'green');
with recursive r as (select 1 as n union all select n+1 from r where n<3),
    t as (select id, color from nr) update t set color='ok' where id=2;
select * from nr order by id;
→ [{"id":1,"color":"red"},{"id":2,"color":"ok"}]

-- Reject parity — a recursive CTE target raises the structured `recursive-cte` reason,
-- never a generic table-not-found miss.
with recursive r as (select 1 as n union all select n+1 from r where n<3) update r set n=0;
-- error: recursive cte has no recoverable base operation

-- Reject parity — an aggregate-bodied CTE target and the equivalent aggregate view both
-- reject with the SAME body-shape diagnostic ("not updateable in phase 1").
create table agg_t (id integer primary key, g integer, v integer);
insert into agg_t values (1,1,10),(2,1,20);
with cagg as (select g, sum(v) as s from agg_t group by g) update cagg set s=0 where g=1;
-- error: is not updateable in phase 1
create view vagg as select g, sum(v) as s from agg_t group by g;
update vagg set s=0 where g=1;
-- error: is not updateable in phase 1

-- Reject parity — a DISTINCT-bodied CTE target and the equivalent DISTINCT view both reject.
with cdist as (select distinct color from cte_base) update cdist set color='z';
-- error: is not updateable in phase 1
create view vdist as select distinct color from cte_base;
update vdist set color='z';
-- error: is not updateable in phase 1

-- ===================================
-- CTE-name DML target: transparent multi-level (CTE-over-CTE) body.
-- A single-source projection-and-filter chain whose body reads ANOTHER (prior) sibling CTE
-- is AST-flattened down to its terminal base table, so it writes through byte-identically to
-- collapsing the chain into one CTE body. The flattener does pure syntactic substitution +
-- filter conjunction; all lineage/inverse reasoning stays in the planner that re-plans the
-- flat body. A non-updateable INTERMEDIATE rejects with that intermediate's body-shape reason.
-- See docs/vu-operators.md § Common Table Expressions — multi-level CTE body.
-- ===================================

-- 2-level positive UPDATE write-through — `t` reads `a` reads base `ml`; the write lands on ml.
create table ml (id integer primary key, color text);
insert into ml values (1,'red'),(2,'green');
with a as (select id, color from ml), t as (select * from a) update t set color='z' where id=1;
select * from ml order by id;
→ [{"id":1,"color":"z"},{"id":2,"color":"green"}]

-- 3-level positive (ml3 ← a ← b ← t) — the recursion composes all three levels.
create table ml3 (id integer primary key, color text);
insert into ml3 values (1,'red'),(2,'green');
with a as (select id, color from ml3), b as (select * from a), t as (select * from b)
    update t set color='z' where id=2;
select * from ml3 order by id;
→ [{"id":1,"color":"red"},{"id":2,"color":"z"}]

-- INSERT through a 2-level chain — the row lands on the terminal base table.
create table mli (id integer primary key, color text);
with a as (select id, color from mli), t as (select * from a) insert into t values (5,'blue');
select * from mli order by id;
→ [{"id":5,"color":"blue"}]

-- DELETE through a 2-level chain.
create table mld (id integer primary key, color text);
insert into mld values (1,'red'),(2,'green'),(3,'blue');
with a as (select id, color from mld), t as (select * from a) delete from t where id=2;
select * from mld order by id;
→ [{"id":1,"color":"red"},{"id":3,"color":"blue"}]

-- RETURNING through a 2-level chain — returns the view-projected post-mutation row.
create table mlr (id integer primary key, color text);
insert into mlr values (1,'red');
with a as (select id, color from mlr), t as (select * from a)
    update t set color='z' where id=1 returning id, color;
→ [{"id":1,"color":"z"}]

-- Filter conjunction — the intermediate's WHERE survives into the flat body, so only red rows
-- update; row 2 (green) is untouched even though the user predicate alone would match it.
create table mlf (id integer primary key, color text);
insert into mlf values (1,'red'),(2,'green');
with a as (select id, color from mlf where color='red'), t as (select * from a)
    update t set color='z';
select * from mlf order by id;
→ [{"id":1,"color":"z"},{"id":2,"color":"green"}]

-- Projection narrowing — `t` exposes only `id`, so `set color=…` is unknown-view-column,
-- exactly as for the collapsed single CTE `select id from mln`.
create table mln (id integer primary key, color text);
insert into mln values (1,'red');
with a as (select id, color from mln), t as (select id from a) update t set color='z';
-- error: is not a column of the view

-- Computed / inverse across levels — `vp = v + 1` is inlined two levels; the planner recovers
-- the inverse from the FLAT body (the flattener only substitutes syntactically), so vp=9 ⇒ v=8.
create table ml2 (id integer primary key, v integer);
insert into ml2 values (1,7);
with a as (select id, v + 1 as vp from ml2), t as (select * from a) update t set vp=9 where id=1;
select id, v from ml2 order by id;
→ [{"id":1,"v":8}]

-- Column rename — the inner CTE renames its outputs over a renamed prior CTE `a(p,q)`;
-- substitution maps the renamed names back to the base columns.
create table mlren (id integer primary key, color text);
insert into mlren values (1,'red');
with a(p,q) as (select id, color from mlren), t as (select p as id, q as color from a)
    update t set color='z' where id=1;
select * from mlren order by id;
→ [{"id":1,"color":"z"}]

-- Column rename over a `select *` inner — `a(p,q)` renames the star body's columns; the
-- flattener resolves the base table's column list (the one schemaManager touch) to pair the
-- renamed names with positions, so `set q=…` lowers to the base `color` column.
create table mlrens (id integer primary key, color text);
insert into mlrens values (1,'red');
with a(p,q) as (select * from mlrens), t as (select * from a) update t set q='z' where p=1;
select * from mlrens order by id;
→ [{"id":1,"color":"z"}]

-- Combined shadow + inline — `a` (defined before the `base2` CTE) inlines, while the target's
-- own self-name `base2` does NOT (its same-named source stays the real `base2` table). The
-- flattened body is `select id, color from base2`, re-planned target-excluded onto real base2.
create table base2 (id integer primary key, color text);
insert into base2 values (1,'red'),(2,'green');
with a as (select id, color from base2), base2 as (select * from a)
    update base2 set color='z' where id=1;
select * from base2 order by id;
→ [{"id":1,"color":"z"},{"id":2,"color":"green"}]

-- Nested-subquery alias shadow of the inner CTE source name — a subquery in the consumer's WHERE
-- re-binds the inner source `x` as a LOCAL FROM alias (`from nother x`), so the `x.note` there is
-- that nested alias's column, NOT the inner CTE's renamed base term. The consumer-rewrite is now
-- alias-shadow-aware, so it leaves the nested `x.note` alone → EXISTS sees nother.note='red' (true)
-- → the write fires. (Blind substitution rewrote `x.note` → base `color`, binding nother.color
-- ='blue' → EXISTS false → no write — the silent-wrong bug this fixes.)
create table nbase (id integer primary key, color text);
insert into nbase values (1,'red');
create table nother (oid integer primary key, note text, color text);
insert into nother values (1,'red','blue');
with x as (select id, color as note from nbase),
     t as (select id, note from x where exists (select 1 from nother x where x.note = 'red'))
     update t set note='z';
select * from nbase order by id;
→ [{"id":1,"color":"z"}]

-- Over-suppression guard — a GENUINE correlation: the nested subquery does NOT re-bind `x`, so
-- `x.note` is a real outer correlation and MUST still be substituted to the inner base term.
-- cother has a matching row → EXISTS true → the write fires (the fix must not over-suppress).
create table cbase (id integer primary key, color text);
insert into cbase values (1,'red');
create table cother (oid integer primary key, color text);
insert into cother values (1,'red');
with x as (select id, color as note from cbase),
     t as (select id, note from x where exists (select 1 from cother where cother.color = x.note))
     update t set note='z';
select * from cbase order by id;
→ [{"id":1,"color":"z"}]

-- Identity-strip path (benign) — a `select *` inner with no rename: the consumer-rewrite only
-- drops the `sourceName.` qualifier, and a nested re-bind of the source name resolves the bare
-- result to the same local column either way. Shadow-awareness changes nothing here; pin no-change.
create table idbase (id integer primary key, color text);
insert into idbase values (1,'red');
create table idother (oid integer primary key, color text);
insert into idother values (1,'red');
with x as (select * from idbase),
     t as (select id, color from x where exists (select 1 from idother x where x.color = 'red'))
     update t set color='z';
select * from idbase order by id;
→ [{"id":1,"color":"z"}]

-- Deeper nesting — the re-bind of the inner source name `x` sits TWO subquery levels down; the
-- alias-shadow set must accumulate downward (the outer EXISTS adds nd_mid, the inner adds x), so
-- the depth-2 `x.note` is local to `nd_inner` and stays unsubstituted → it binds nd_inner.note='red'
-- → the EXISTS chain is true → write fires. (Blind substitution would bind nd_inner.color='blue'.)
create table nd_base (id integer primary key, color text);
insert into nd_base values (1,'red');
create table nd_mid (mid integer primary key);
insert into nd_mid values (1);
create table nd_inner (iid integer primary key, note text, color text);
insert into nd_inner values (1,'red','blue');
with x as (select id, color as note from nd_base),
     t as (select id, note from x where exists (
        select 1 from nd_mid where exists (
           select 1 from nd_inner x where x.note = 'red')))
     update t set note='z';
select * from nd_base order by id;
→ [{"id":1,"color":"z"}]

-- Compound (union) leg re-binds the inner source name — the shadowing re-bind sits in a UNION leg
-- of a nested EXISTS subquery, exercising the `onLeg` descent (a leg's OWN FROM aliases shadow
-- WITHIN that leg). `from lother2 x` re-binds `x`, so the leg's `x.note` is that local alias's
-- column → binds lother2.note='red' → leg returns a row → EXISTS true → write fires. (Blind
-- substitution rewrote `x.note` → bare base `color`, which then binds lother2.color='blue' inside
-- the leg → both legs empty → EXISTS false → no write.)
create table lbase (id integer primary key, color text);
insert into lbase values (1,'red');
create table lother (k integer primary key);
create table lother2 (oid integer primary key, note text, color text);
insert into lother2 values (1,'red','blue');
with x as (select id, color as note from lbase),
     t as (select id, note from x where exists (
        select 1 from lother
        union
        select 1 from lother2 x where x.note = 'red'))
     update t set note='z';
select * from lbase order by id;
→ [{"id":1,"color":"z"}]

-- Forward reference write-through — the target `x` is the FIRST CTE and its body reads a sibling
-- CTE `fwd` defined LATER whose name also shadows a real table. Per SQL definition-order scoping a
-- non-recursive CTE is visible only to LATER siblings, so inside `x`'s body the later `fwd` CTE is
-- out of scope and `from fwd` binds the REAL `fwd` table → write through. `contextForCteTarget`
-- strips the target + every at-or-after sibling from the re-plan context (prior-sibling prefix),
-- mirroring `buildCommonTableExpr`. The later `fwd` CTE is never the write target and is unused.
create table fwd (id integer primary key, color text);
insert into fwd values (1,'red'),(2,'green');
with x as (select id, color from fwd), fwd as (select id, color from fwd)
    update x set color='z' where id=1;
select * from fwd order by id;
→ [{"id":1,"color":"z"},{"id":2,"color":"green"}]

-- Forward reference with NO real backing — the body reads a later sibling `later` whose name is
-- NOT a real table. The later CTE is out of scope (prior-sibling-only visibility) and there is no
-- real object to fall back to, so resolution is a clean table-not-found, never silently wrong.
create table fwdr (id integer primary key, color text);
insert into fwdr values (1,'red');
with x as (select id, color from later), later as (select * from fwdr)
    update x set color='z' where id=1;
-- error: Table 'later' not found

-- v1 boundary (documented) — a USER-clause read of a LATER-defined sibling. The body re-plan and
-- the user `set` subquery share ONE re-plan context (`contextForCteTarget`), which strips later
-- siblings, so the `set` subquery's `from ysib` binds the REAL table `ysib` (v=42), NOT the later
-- CTE `ysib` (which would give 999). Per strict SQL the user clause is main-query scope and SHOULD
-- see the later CTE; v1 instead resolves a real same-named table (deterministic, never silently
-- the wrong relation). This pins the v1 behavior — if the faithful split-context fix lands, flip it.
create table ysib (id integer primary key, v integer);
insert into ysib values (1,42);
create table ucbase (id integer primary key, c integer);
insert into ucbase values (1,0);
with x as (select id, c from ucbase), ysib as (select id, 999 as v from ucbase)
    update x set c = (select v from ysib where id=1) where id=1;
select c from ucbase order by id;
→ [{"c":42}]

-- Reject parity — a non-updateable INTERMEDIATE rejects with its own body-shape reason
-- ("not updateable in phase 1"), the SAME diagnostic class a single-level CTE of that shape
-- raises. Aggregate intermediate:
create table mlagg (id integer primary key, g integer, v integer);
insert into mlagg values (1,1,10),(2,1,20);
with a as (select g, sum(v) as s from mlagg group by g), t as (select * from a)
    update t set s=0 where g=1;
-- error: is not updateable in phase 1

-- Distinct intermediate:
create table mldist (id integer primary key, color text);
insert into mldist values (1,'red');
with a as (select distinct color from mldist), t as (select * from a) update t set color='z';
-- error: is not updateable in phase 1

-- Set-op intermediate:
create table mlsop (id integer primary key);
insert into mlsop values (1),(2);
with a as (select id from mlsop union select id from mlsop), t as (select * from a)
    delete from t where id=1;
-- error: is not updateable in phase 1

-- Join intermediate:
create table mljp (pid integer primary key, label text);
create table mljc (cid integer primary key, pref integer, note text);
insert into mljp values (10,'P10');
insert into mljc values (1,10,'a');
with a as (select c.cid as cid, c.note as note, p.label as label
    from mljc c join mljp p on p.pid = c.pref), t as (select * from a)
    update t set note='B' where cid=1;
-- error: is not updateable in phase 1

-- v1 boundary — a plain set-op-bodied CTE target reaches the existing set-op reject
-- (structured, no crash).
create table so (id integer primary key);
insert into so values (1),(2);
with s as (select id from so union select id from so) delete from s where id=1;
-- error: is not updateable in phase 1

-- ===================================
-- CTE-name DML target: self-reference in the user predicate (Halloween).
-- A user-clause self-read of the target CTE name (`with t as (…) update t … where id in
-- (select id from t)`) resolves the self-read against an EAGER up-front capture of the
-- whole CTE body, producing a Halloween-safe positive write (split context + eager
-- capture via `ViewMutationNode.identityCapture`). The body is still planned
-- target-EXCLUDED (so a same-named base FROM reaches the real table — the load-bearing
-- shadow case), while the user `where`/`set`/`returning` self-read resolves `t` to the
-- frozen snapshot. Mirrors the inline-subquery Halloween behavior (isq_hw / isq_hwk).
-- See docs/vu-operators.md § Common Table Expressions — self-reference.
-- ===================================

-- Bare self-read — `from t` resolves to the captured snapshot; every captured row
-- matches, so both rows update (a positive write, NOT the old clean reject).
create table hw (id integer primary key, color text);
insert into hw values (1,'red'),(2,'green');
with t as (select id, color from hw) update t set color='x' where id in (select id from t);
select * from hw order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"x"}]

-- Key-mutating self-read (Halloween core) — the predicate column (`id`) is itself rewritten
-- by the SET. The capture is materialized BEFORE the base op, so the frozen {1,2} drives the
-- predicate and both rows mutate deterministically (1→11, 2→12) — never the feedback bug.
create table hwk (id integer primary key, color text);
insert into hwk values (1,'a'),(2,'b');
with t as (select id, color from hwk) update t set id = id + 10 where id in (select id from t);
select * from hwk order by id;
→ [{"id":11,"color":"a"},{"id":12,"color":"b"}]

-- Shadow case × self-read combo — the body FROM, the self-read FROM, and the target all
-- name `base`. Body planned target-excluded reads the REAL base; capture snapshots real base;
-- the lowered target is schema-qualified `main.base` → real base; the predicate `from base`
-- → capture. All three resolve correctly and the write lands on real `base`.
create table base (id integer primary key, color text);
insert into base values (1,'red'),(2,'green');
with base as (select id, color from base) update base set color='x' where id in (select id from base);
select * from base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"x"}]

-- Self-read × later-sibling combo — the target `t` is the FIRST CTE; its body reads `hwls`,
-- which is ALSO a later sibling that shadows the real table. `contextForCteTarget` strips the
-- target + later sibling, so BOTH the body and the eager capture are planned over the REAL
-- `hwls`; the user-clause self-read `from t` resolves to the capture. The self-capture path is
-- untouched by later-sibling stripping (it re-adds only the target name) → Halloween-safe write.
create table hwls (id integer primary key, color text);
insert into hwls values (1,'red'),(2,'green');
with t as (select id, color from hwls), hwls as (select id, color from hwls)
    update t set color='x' where id in (select id from t);
select * from hwls order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"x"}]

-- DELETE variant — a self-read with a subquery WHERE (`id<>2`) deletes only the rows whose
-- id is in the captured-and-filtered set {1,3}; row 2 survives.
create table hwd (id integer primary key, color text);
insert into hwd values (1,'red'),(2,'green'),(3,'blue');
with t as (select id, color from hwd) delete from t where id in (select id from t where id<>2);
select * from hwd order by id;
→ [{"id":2,"color":"green"}]

-- RETURNING self-read — `(select count(*) from t)` reads the frozen snapshot (2 rows)
-- materialized before the base op, so the RETURNING re-projection returns the pre-mutation
-- count even though the same statement writes a row.
create table hwret (id integer primary key, color text);
insert into hwret values (1,'red'),(2,'green');
with t as (select id, color from hwret) update t set color='r' where id=1 returning (select count(*) from t) as cnt;
→ [{"cnt":2}]
select * from hwret order by id;
→ [{"id":1,"color":"r"},{"id":2,"color":"green"}]

-- View-name-qualified self-read (`select t.id from t`) — `t.id` is the CAPTURE's column
-- (alias-shadow), NOT a de-correlated `__vm_self`-qualified base term. The discriminating
-- probe is an aggregate: `sum(t.id)` over the frozen {1,2,3} is 6 (a silent de-correlation
-- would correlate `t.id` to the single outer row and sum 1+1+1 = 3).
create table hwq (id integer primary key, color text);
insert into hwq values (1,'a'),(2,'b'),(3,'c');
with t as (select id, color from hwq) update t set color = cast((select sum(t.id) from t) as text) where id = 1;
select * from hwq order by id;
→ [{"id":1,"color":"6"},{"id":2,"color":"b"},{"id":3,"color":"c"}]

-- The same alias-shadow probe in a RETURNING subquery — `sum(t.id)` over the frozen capture
-- is 6, not the de-correlated 3.
create table hwq2 (id integer primary key, color text);
insert into hwq2 values (1,'a'),(2,'b'),(3,'c');
with t as (select id, color from hwq2) update t set color='r' where id=1 returning (select sum(t.id) from t) as s;
→ [{"s":6}]

-- Scalar-subquery self-read in the SET value — row 2 takes row 1's captured color.
create table hwsc (id integer primary key, color text);
insert into hwsc values (1,'red'),(2,'green');
with t as (select id, color from hwsc) update t set color = (select color from t where id = 1) where id = 2;
select * from hwsc order by id;
→ [{"id":1,"color":"red"},{"id":2,"color":"red"}]

-- exists-form self-read — `exists (select 1 from t where color='red')` over the capture is
-- true (a red row exists), so every row updates. The capture + descend hold for the `exists`
-- operand form, not just `in`.
create table hwe (id integer primary key, color text);
insert into hwe values (1,'red'),(2,'green');
with t as (select id, color from hwe) update t set color='E' where exists (select 1 from t where color='red');
select * from hwe order by id;
→ [{"id":1,"color":"E"},{"id":2,"color":"E"}]

-- Mixed self-read + genuine outer correlation in one statement — the WHERE `from t` is the
-- self-read (→ capture), while the SET value `(select c from oth where oth.k = id)` is a
-- genuine outer view-column correlation (the bare `id`, not shadowed by `oth`, binds to the
-- target row). The descend keeps them distinct (shadow vs. not-shadowed).
create table hwmix (id integer primary key, color text);
insert into hwmix values (1,'red'),(2,'green');
create table oth (k integer, c text);
insert into oth values (1,'M1'),(2,'M2');
with t as (select id, color from hwmix) update t set color = (select c from oth where oth.k = id) where id in (select id from t);
select * from hwmix order by id;
→ [{"id":1,"color":"M1"},{"id":2,"color":"M2"}]

-- Sibling CTE read in the predicate alongside the self-read — `t` is the self-read (capture),
-- `sv` is a sibling CTE (stays in `cteNodes` in BOTH contexts); only id in capture AND in
-- `sv` {1} matches, so row 1 updates and row 2 is left.
create table hwsib (id integer primary key, color text);
insert into hwsib values (1,'red'),(2,'green');
create table sibvals (filt integer);
insert into sibvals values (1);
with sv as (select filt from sibvals), t as (select id, color from hwsib)
    update t set color='s' where id in (select id from t) and id in (select filt from sv);
select * from hwsib order by id;
→ [{"id":1,"color":"s"},{"id":2,"color":"green"}]

-- Sibling CTE read in the SET value alongside the self-read in the WHERE — `cv` resolves
-- (sibling), `t` resolves to the capture.
create table hwss (id integer primary key, color text);
insert into hwss values (1,'red'),(2,'green');
create table cvtab (vv text);
insert into cvtab values ('CV');
with cv as (select vv from cvtab), t as (select id, color from hwss)
    update t set color = (select vv from cv) where id in (select id from t);
select * from hwss order by id;
→ [{"id":1,"color":"CV"},{"id":2,"color":"CV"}]

-- Composite-PK body self-read — the capture exposes all view columns; a self-read predicate
-- over `note` (`note in (select note from t where a=1)`) resolves through the composite-PK body.
create table hwcpk (a integer, b integer, note text, primary key (a,b));
insert into hwcpk values (1,2,'x'),(3,4,'y');
with t as (select a, b, note from hwcpk) update t set note='z' where note in (select note from t where a=1);
select * from hwcpk order by a, b;
→ [{"a":1,"b":2,"note":"z"},{"a":3,"b":4,"note":"y"}]

-- `t(a,b)` column-rename CTE self-read — the capture honors the rename, so `from t` exposes
-- the renamed names (`k`, `c`).
create table hwren (id integer primary key, color text);
insert into hwren values (1,'red'),(2,'green');
with t(k,c) as (select id, color from hwren) update t set c='r' where k in (select k from t);
select * from hwren order by id;
→ [{"id":1,"color":"r"},{"id":2,"color":"r"}]

-- Multiple self-reads in ONE statement — `from t` appears in BOTH the WHERE and the RETURNING.
-- Both resolve through the single context-backed capture ref minted under the CTE name; the
-- shared ref reads the same frozen snapshot from each position. Both rows update; the RETURNING
-- count reads the pre-mutation snapshot (2). Exercises the shared-instance ref path that the
-- single-position tests above do not.
create table hwmr (id integer primary key, color text);
insert into hwmr values (1,'red'),(2,'green');
with t as (select id, color from hwmr) update t set color='x' where id in (select id from t) returning (select count(*) from t) as cnt;
→ [{"cnt":2},{"cnt":2}]
select * from hwmr order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"x"}]

-- Two self-reads in ONE WHERE — `id in (select id from t where id<2)` ∪ `color in (select color
-- from t where id>2)`. Both `from t` bind to the same frozen capture; row 1 (id<2) and row 3
-- (color in {captured blue}) update, row 2 is left. Confirms two same-name refs co-resolve.
create table hwmr2 (id integer primary key, color text);
insert into hwmr2 values (1,'red'),(2,'green'),(3,'blue');
with t as (select id, color from hwmr2)
    update t set color='y' where id in (select id from t where id<2) or color in (select color from t where id>2);
select * from hwmr2 order by id;
→ [{"id":1,"color":"y"},{"id":2,"color":"green"},{"id":3,"color":"y"}]

-- No-self-read CTE-target — a CTE-target UPDATE whose predicate does NOT self-read pays
-- nothing (no capture built, byte-identical plan); a plain positive write lands.
create table hwns (id integer primary key, color text);
insert into hwns values (1,'red'),(2,'green');
with t as (select id, color from hwns) update t set color='n' where id=1;
select * from hwns order by id;
→ [{"id":1,"color":"n"},{"id":2,"color":"green"}]

-- Out of scope — INSERT-source self-read keeps current behavior (the target name is shadowed
-- out of the INSERT source's scope, so `from t` is table-not-found). Not silently wrong; the
-- new single-source capture path is INSERT-gated out. (Follow-up: cte-dml-insert-source-self-read.)
create table hwins (id integer primary key, color text);
insert into hwins values (1,'red');
with t as (select id, color from hwins) insert into t (id, color) select id+10, color from t;
-- error: Table 't' not found

-- Out of scope — join-bodied (multi-source) CTE self-read keeps current behavior: the new
-- single-source capture path is gated to single-source bodies, so the multi-source descend
-- still rejects the self-read as unprovable-correlation (no silent-wrong plan).
create table hwjp (pid integer primary key, label text);
create table hwjc (cid integer primary key, pref integer, note text, foreign key (pref) references hwjp(pid));
insert into hwjp values (10,'P10');
insert into hwjc values (1,10,'a');
with t as (select c.cid as cid, c.note as note, p.label as label
    from hwjc c join hwjp p on p.pid = c.pref) update t set note='B' where cid in (select cid from t);
-- error: cannot be proven correlated

-- ===================================
-- Review-added interaction probes (cte-name-dml-write-target review pass): the source
-- sibling-read, composite-PK, and statement-level WITH CONTEXT cases the implement pass
-- left to the reviewer. All route the CTE body through the ephemeral substrate.
-- ===================================

-- INSERT into a CTE target whose SELECT source reads a SIBLING CTE: the statement's CTEs
-- thread into the source build, so `from s` (a sibling) resolves while `t` (the target)
-- is shadowed out of its own body's scope.
create table p_src (id integer primary key, color text);
create table p_dst (id integer primary key, color text);
insert into p_src values (1,'a'),(2,'b');
with s as (select id, color from p_src), t as (select id, color from p_dst)
    insert into t (id, color) select id, color from s;
select * from p_dst order by id;
→ [{"id":1,"color":"a"},{"id":2,"color":"b"}]

-- Composite-PK CTE body: the multi-column key identifies the base row through the
-- ephemeral substrate exactly as for a composite-PK base table / view.
create table cpk (a integer, b integer, note text, primary key (a,b));
insert into cpk values (1,2,'x'),(3,4,'y');
with t as (select a, b, note from cpk) update t set note='z' where a=1 and b=2;
select * from cpk order by a, b;
→ [{"a":1,"b":2,"note":"z"},{"a":3,"b":4,"note":"y"}]

-- Statement-level WITH CONTEXT on a CTE-targeted INSERT threads to the lowered base op:
-- the base column default `now_ms` reads the supplied context value through the substrate.
create table ectx (id integer primary key, created integer default now_ms) using memory with context (now_ms integer);
with t as (select id from ectx) insert into t with context now_ms = 999 values (1);
select * from ectx order by id;
→ [{"id":1,"created":999}]

-- ===================================
-- Inline subquery as a DML write target. `update (select …) as v set …` and
-- `delete from (select …) as v where …` make a parenthesized subquery a real write
-- target: the subquery body routes through the SAME view-mutation substrate a named
-- view / CTE target uses (an ephemeral view-like adapter). Per docs/view-updateability.md
-- a subquery in `from` is structurally an inlined CTE, so a single-source
-- projection-and-filter body produces a byte-identical base-op plan to the equivalent
-- named view. The user `where`/`set` reference the body's columns via the mandatory
-- alias (`v.col`) or the bare form. inline-subquery INSERT is deliberately rejected.
-- See docs/vu-operators.md § Inline subquery DML target.
-- ===================================

create table isq_base (id integer primary key, color text);
insert into isq_base values (1,'red'),(2,'green'),(3,'blue');

-- Round-Trip Law — UPDATE through the inline subquery target lands on the base table.
-- The `where` filters on the alias-qualified `v.id`; the `set` assigns the bare column.
update (select id, color from isq_base) as v set color = 'x' where v.id = 1;
select * from isq_base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"green"},{"id":3,"color":"blue"}]

-- DELETE through the inline subquery target (with the optional leading FROM).
delete from (select id, color from isq_base) as v where v.id = 2;
select * from isq_base order by id;
→ [{"id":1,"color":"x"},{"id":3,"color":"blue"}]

-- `delete (select …) as v` — no leading FROM — parses the same way (optional-FROM parity).
delete (select id, color from isq_base) as v where v.id = 3;
select * from isq_base order by id;
→ [{"id":1,"color":"x"}]

-- Alias-qualified `v.col` in the WHERE (an expression position): `v.id` binds to the
-- body's `id` column through the substrate's alias-qualified view-column descend. (SET
-- targets are bare column names — qualified `set v.col` is not SQL, here or in SQLite —
-- so the SET assigns the bare `color`.)
insert into isq_base values (2,'green'),(3,'blue');
update (select id, color from isq_base) as v set color = 'q' where v.id = 2;
select * from isq_base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"q"},{"id":3,"color":"blue"}]

-- `as v(a,b)` column rename list — the renamed names are what `set`/`where` reference;
-- they map through to the right base column (`c` → base `color`, `k` → base `id`).
update (select id, color from isq_base) as v(k, c) set c = 'renamed' where k = 3;
select * from isq_base order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"q"},{"id":3,"color":"renamed"}]

-- Parity: the equivalent named view, from the same starting rows and the same DML
-- sequence, leaves byte-identical base-table state.
create table isq_view_base (id integer primary key, color text);
insert into isq_view_base values (1,'red'),(2,'green'),(3,'blue');
create view isq_tv as select id, color from isq_view_base;
update isq_tv set color = 'x' where id = 1;
delete from isq_tv where id = 2;
select * from isq_view_base order by id;
→ [{"id":1,"color":"x"},{"id":3,"color":"blue"}]
-- And the inline subquery against a fresh copy lands identically.
create table isq_inline_base (id integer primary key, color text);
insert into isq_inline_base values (1,'red'),(2,'green'),(3,'blue');
update (select id, color from isq_inline_base) as v set color = 'x' where v.id = 1;
delete from (select id, color from isq_inline_base) as v where v.id = 2;
select * from isq_inline_base order by id;
→ [{"id":1,"color":"x"},{"id":3,"color":"blue"}]

-- RETURNING through the inline subquery target (the OLD/NEW machinery the base op embeds).
create table isq_ret (id integer primary key, color text);
insert into isq_ret values (1,'red');
update (select id, color from isq_ret) as v set color='ret' where v.id=1 returning id, color;
→ [{"id":1,"color":"ret"}]

-- RETURNING <q>.* qualifier validation against an inline-subquery target: the only
-- valid qualifier is the mandatory alias (`resolveSubqueryTarget` sets `view.name =
-- source.alias`), so `v.*` expands and any other qualifier errors.
update (select id, color from isq_ret) as v set color='x2' where v.id=1 returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

update (select id, color from isq_ret) as v set color='vret' where v.id=1 returning v.*;
→ [{"id":1,"color":"vret"}]

-- Composite-PK body: the multi-column key identifies the base row through the ephemeral
-- substrate exactly as for a composite-PK base table / view.
create table isq_cpk (a integer, b integer, note text, primary key (a,b));
insert into isq_cpk values (1,2,'x'),(3,4,'y');
update (select a, b, note from isq_cpk) as v set note='z' where v.a=1 and v.b=2;
select * from isq_cpk order by a, b;
→ [{"a":1,"b":2,"note":"z"},{"a":3,"b":4,"note":"y"}]

-- Join-bodied inline target (key-preserving inner join): composes through the same
-- multi-source substrate as a join view — "works without special-casing" (L670).
create table isq_jp (pid integer primary key, label text);
create table isq_jc (cid integer primary key, pref integer, note text,
    foreign key (pref) references isq_jp(pid));
insert into isq_jp values (10,'P10');
insert into isq_jc values (1,10,'a');
update (select c.cid as cid, c.note as note, p.label as label
    from isq_jc c join isq_jp p on p.pid = c.pref) as v set note='B' where v.cid=1;
select cid, note from isq_jc order by cid;
→ [{"cid":1,"note":"B"}]

-- Sibling CTE read in the SET value resolves: the statement's CTEs thread into scope, and
-- an inline subquery has NO own-name to shadow out of its body, so `cv` resolves.
create table isq_sib_b (id integer primary key, color text);
insert into isq_sib_b values (1,'red');
create table isq_sib_vals (vv text);
insert into isq_sib_vals values ('fromcte');
with cv as (select vv from isq_sib_vals)
    update (select id, color from isq_sib_b) as v set color = (select vv from cv) where v.id = 1;
select * from isq_sib_b order by id;
→ [{"id":1,"color":"fromcte"}]

-- Halloween / self-reference — the inline body's `from base` and the user predicate's
-- `from base` both read the REAL table; the substrate's eager-capture discipline holds, so
-- this is a deterministic positive write (not a reject): every row whose id is in the
-- captured set {1,2} updates. (Unlike the CTE-name target, whose own name is shadowed out
-- of its body, so the equivalent CTE self-read rejects — here the predicate names the real
-- table, which resolves.)
create table isq_hw (id integer primary key, color text);
insert into isq_hw values (1,'red'),(2,'green');
update (select id, color from isq_hw) as v set color='x' where v.id in (select id from isq_hw);
select * from isq_hw order by id;
→ [{"id":1,"color":"x"},{"id":2,"color":"x"}]

-- Key-mutating self-reference — the predicate column (`id`) is itself rewritten by the SET.
-- Eager-capture must hold: the affected set {1,2} is captured from the ORIGINAL table BEFORE
-- any write, so both rows mutate deterministically (1→11, 2→12) rather than the rewrite
-- feeding back into the predicate (which would skip or re-match rows — the Halloween bug).
create table isq_hwk (id integer primary key, color text);
insert into isq_hwk values (1,'a'),(2,'b');
update (select id, color from isq_hwk) as v set id = id + 10 where v.id in (select id from isq_hwk);
select * from isq_hwk order by id;
→ [{"id":11,"color":"a"},{"id":12,"color":"b"}]

-- Reject — a subquery write target with NO explicit alias errors clearly at parse; the
-- alias is user-meaningful (the where/set reference it), never a generated default. Both
-- UPDATE and DELETE forms reject.
update (select id, color from isq_base) set color='q';
-- error: requires an alias
delete from (select id, color from isq_base) where id=1;
-- error: requires an alias

-- Reject parity — a non-updatable body (aggregate / DISTINCT) reached through the inline
-- subquery target raises the SAME body-shape diagnostic the equivalent view body raises.
create table isq_agg (id integer primary key, g integer, v integer);
insert into isq_agg values (1,1,10),(2,1,20);
update (select g, sum(v) as s from isq_agg group by g) as v set s=0 where v.g=1;
-- error: is not updateable in phase 1
create view isq_vagg as select g, sum(v) as s from isq_agg group by g;
update isq_vagg set s=0 where g=1;
-- error: is not updateable in phase 1
update (select distinct color from isq_base) as v set color='z';
-- error: is not updateable in phase 1

-- Reject — a DML-bodied inline target (a RETURNING DML body is admitted in a FROM position
-- but is not a meaningful write target): rejected up front with a structured diagnostic.
update (insert into isq_base values (9,'q') returning id, color) as v set color='z' where v.id=9;
-- error: has no recoverable base operation

-- Reject — a VALUES-bodied inline target has no base lineage; it rejects downstream in the
-- view-shape analysis (not at the up-front DML-body guard) with the same no-base-lineage class.
update (values (1,'a')) as v(id, color) set color='z' where v.id=1;
-- error: has no recoverable base operation

-- Reject — inline-subquery INSERT is deliberately NOT admitted in v1; `insert into (` falls
-- through to the unchanged "Expected table name" parse error (an INSERT target needs a
-- column-list mapping, not an aliased relation — the CTE-name INSERT covers the use case).
insert into (select id from isq_base) values (9);
-- error: Expected table name

-- Inline-subquery target over a CTE — `update (select … from t) as v` where `t` is a CTE now
-- writes through: the inline body's single-source chain is flattened to the terminal base
-- table (the inline-subquery dual of the CTE-over-CTE multi-level inlining above).
create table isq_ml (id integer primary key, color text);
insert into isq_ml values (1,'red'),(2,'green');
with t as (select id, color from isq_ml)
    update (select id, color from t) as v set color='z' where v.id=1;
select * from isq_ml order by id;
→ [{"id":1,"color":"z"},{"id":2,"color":"green"}]

-- DELETE inline-subquery over a CTE.
create table isq_mld (id integer primary key, color text);
insert into isq_mld values (1,'red'),(2,'green');
with t as (select id, color from isq_mld)
    delete from (select id, color from t) as v where v.id=2;
select * from isq_mld order by id;
→ [{"id":1,"color":"red"}]

-- Reject parity — an inline body whose CTE chain has a non-updateable (aggregate) intermediate
-- rejects with that intermediate's body-shape reason, same as the CTE-name target chain.
create table isq_agg2 (id integer primary key, g integer, v integer);
insert into isq_agg2 values (1,1,10),(2,1,20);
with a as (select g, sum(v) as s from isq_agg2 group by g)
    update (select g, s from a) as v set s=0 where v.g=1;
-- error: is not updateable in phase 1

-- ===================================
-- `with defaults (…)` × CTE-name / inline-subquery DML write targets. The omitted-insert
-- defaults clause lives on the body select (`SelectStmt.defaults`), so it rides for free
-- onto a derived write target — no engine code beyond the clause-rehome + the write-target
-- dispatch. Reachability matrix: it is ACTIVE on a CTE-name INSERT target (omitted columns
-- fill through the ephemeral substrate, exactly as for a named view) and INERT on an inline
-- subquery target (the only inline writes are UPDATE/DELETE, which never consult defaults;
-- inline-subquery INSERT is rejected — so the CTE name is the only derived target that fires
-- defaults). A body-shape reject (aggregate / set-op / SELECT-source-needing-append) fires
-- regardless of the clause — `with defaults` never rescues a non-updateable body.
-- See docs/vu-inverses.md § View defaults.
-- ===================================

-- (1) CTE-name INSERT: an omitted column is filled by the body's `with defaults` through the
-- ephemeral substrate — the headline omitted-fires case (parity with the view df2_v case).
create table p1 (id integer primary key, color text, created integer);
with t as (select id, color from p1 with defaults (created = 111))
    insert into t (id, color) values (1, 'red');
select * from p1 order by id;
→ [{"id":1,"color":"red","created":111}]

-- (2) CTE-name INSERT: a supplied value wins over the default (no contradiction).
create table p2 (id integer primary key, created integer);
with t as (select id, created from p2 with defaults (created = 111))
    insert into t (id, created) values (1, 999);
select * from p2 order by id;
→ [{"id":1,"created":999}]

-- (3) CTE-name INSERT: the defaulted column is PROJECTED AWAY by the CTE body (it has no slot
-- in the body's output) — the default still fills the base column (parity with df2_v / df3_v).
create table p3 (id integer primary key, created integer);
with t as (select id from p3 with defaults (created = 222))
    insert into t (id) values (1);
select * from p3 order by id;
→ [{"id":1,"created":222}]

-- (4) Typo in a `with defaults` column name → sited write error. This is the proof the clause
-- is ACTUALLY consumed through the ephemeral path (not silently dropped): a typo must fail loud.
create table p4 (id integer primary key, created integer);
with t as (select id from p4 with defaults (nope = 1))
    insert into t (id) values (1);
-- error: not a column

-- (5) Inline-subquery UPDATE carrying `with defaults` on its body → clause INERT. UPDATE never
-- consults defaults, so the row's untouched `created` keeps its value (5), only `color` changes.
create table p5 (id integer primary key, color text, created integer);
insert into p5 values (1,'red',5);
update (select id, color from p5 with defaults (created = 999)) as v set color='x' where v.id=1;
select * from p5 order by id;
→ [{"id":1,"color":"x","created":5}]

-- (6) Multi-level CTE chain: the inner CTE's defaults and the consumer's defaults MERGE through
-- the flattener (`mergeDefaults`, cte-flatten.ts), CONSUMER WINNING on a column collision —
-- `a` survives from the inner clause but is overridden to 10 by the consumer; `b` rides from inner.
create table p6 (id integer primary key, a integer, b integer);
with inner1 as (select id from p6 with defaults (a = 1, b = 2)),
     t as (select id from inner1 with defaults (a = 10))
    insert into t (id) values (1);
select * from p6 order by id;
→ [{"id":1,"a":10,"b":2}]

-- (7) Aggregate CTE body carrying defaults → still a body-shape reject. The clause is appended
-- AFTER the body is proven decomposable, so an unupdatable body rejects with the same diagnostic
-- it raises without the clause — reject parity, not a defaults bypass.
create table p7 (id integer primary key, g integer, v integer);
insert into p7 values (1,1,10);
with t as (select g, sum(v) as s from p7 group by g with defaults (g = 9))
    insert into t (g) values (2);
-- error: is not updateable in phase 1

-- (8) Inline-subquery INSERT is still rejected even with a `with defaults` body — `insert into (`
-- falls through to the unchanged "Expected table name" parse error (no inline-INSERT path exists).
create table p8 (id integer primary key, created integer);
insert into (select id from p8 with defaults (created = 1)) values (1);
-- error: Expected table name

-- (A) Compound (set-op) CTE body carrying defaults → set-op body-shape reject (clause inert here).
create table q1 (id integer primary key, created integer);
insert into q1 values (1,5);
with t as (select id from q1 union select id from q1 with defaults (created = 9))
    insert into t (id) values (2);
-- error: phase 1

-- (B) CTE INSERT with a SELECT source while a default MUST be appended → VALUES-source reject.
-- The appended-defaults rewrite is VALUES-only (single-source.ts), parity with the view.
create table q2 (id integer primary key, created integer);
create table q2src (id integer primary key);
insert into q2src values (7);
with t as (select id from q2 with defaults (created = 111))
    insert into t (id) select id from q2src;
-- error: VALUES source

-- (C) CTE INSERT, SELECT source, but EVERY column supplied (no default needs appending) → works.
create table q3 (id integer primary key, created integer);
create table q3src (id integer primary key, c integer);
insert into q3src values (7, 70);
with t as (select id, created from q3 with defaults (created = 111))
    insert into t (id, created) select id, c from q3src;
select * from q3 order by id;
→ [{"id":7,"created":70}]

-- ===================================================================
-- Set-op MEMBERSHIP body with a multi-source (INNER JOIN) branch
-- (`set-op-write-multisource-leg-compose`): a `union exists … as <flag>` body one of whose
-- branches is an INNER join is now writable for UPDATE / DELETE. The data/delete fan composes the
-- join branch via an inner per-branch base-PK capture (`__vmupd_keys$N`) chained off the outer
-- set-op capture, so the join branch decomposes through the multi-source machinery without
-- colliding with the outer capture (the internal `k.k0_0` error is gone). INSERT into a join
-- branch is deferred (`set-op-write-multisource-leg-insert`), so `is_insertable_into` reports NO
-- while is_updatable / is_deletable report YES — both surfaces matching the dynamic write.
-- ===================================================================
create table mj1 (id integer primary key, x integer, color text null);
create table mj2 (id integer primary key, y integer null);
insert into mj1 values (1,10,'red'),(2,20,'blue');
insert into mj2 values (1,100),(2,200);
create view MV as
  select mj1.id as id, mj1.x as x from mj1 join mj2 on mj1.id = mj2.id where mj1.color = 'red'
  union exists left as inL, exists right as inR
  select id, x from mj1 where color = 'blue';

-- read (data columns + membership probes).
select id, x, inL, inR from MV order by id;
→ [{"id":1,"x":10,"inL":true,"inR":false},{"id":2,"x":20,"inL":false,"inR":true}]

-- static surfaces: is_updatable / is_deletable = YES (the join branch composes); is_insertable_into
-- = YES (the join branch's shared key `id` is supplied, so its shared-surrogate envelope accepts the
-- insert — `set-op-write-multisource-leg-insert`). Matches the dynamic write exactly.
select is_insertable_into, is_updatable, is_deletable from view_info('MV');
→ [{"is_insertable_into":"YES","is_updatable":"YES","is_deletable":"YES"}]
-- every column (data + flag) is writable-through-effect with a null base (the membership-write
-- shape — a data fan-out / flag flip, not a single base column).
select cid, is_updatable, base_table, base_column from column_info('MV');
→ [{"cid":0,"is_updatable":"YES","base_table":null,"base_column":null},{"cid":1,"is_updatable":"YES","base_table":null,"base_column":null},{"cid":2,"is_updatable":"YES","base_table":null,"base_column":null},{"cid":3,"is_updatable":"YES","base_table":null,"base_column":null}]

-- data UPDATE through the join branch: `x` maps to mj1.x, so the fan updates mj1 only (mj2 untouched).
update MV set x = x + 1 where inL = true;
select id, x from mj1 order by id;
→ [{"id":1,"x":11},{"id":2,"x":20}]
select id, y from mj2 order by id;
→ [{"id":1,"y":100},{"id":2,"y":200}]

-- DELETE through the join branch fans to both candidate sides of the inner join (the lenient
-- ambiguous-delete default), so the joined row id=1 leaves both mj1 and mj2; the plain branch's
-- row id=2 is untouched (it is not a member of the join branch's captured set).
delete from MV where inL = true;
select id, x from mj1 order by id;
→ [{"id":2,"x":20}]
select id, y from mj2 order by id;
→ [{"id":2,"y":200}]

-- INSERT into a join branch is now SHIPPED via the nested shared-surrogate envelope splice
-- (`set-op-write-multisource-leg-insert`): inL=true routes the VALUES row into the join branch's base
-- tables (mj1 gets (5,50,null), mj2 gets (5,null) — shared key `id` supplied); inR=false omits the
-- plain branch.
insert into MV (id, x, inL, inR) values (5, 50, true, false);
select id, x from mj1 order by id;
→ [{"id":2,"x":20},{"id":5,"x":50}]
select id, y from mj2 order by id;
→ [{"id":2,"y":200},{"id":5,"y":null}]

-- ===================================================================
-- TWO join branches in ONE body (`set-op-write-multisource-leg-compose`, the distinct-inner-name
-- requirement): both branches are INNER joins, so the fan builds an inner per-branch capture for
-- EACH under a DISTINCT fresh name (`__vmupd_keys$1`, `__vmupd_keys$2`). A single UPDATE touching
-- both branches exercises both captures at once — two branches sharing a name would re-introduce
-- the original collision, so a correct both-branch update proves the monotonic counter never resets.
-- ===================================================================
create table tba (id integer primary key, x integer);
create table tbb (id integer primary key, x integer);
create table tbc (id integer primary key);
create table tbd (id integer primary key);
insert into tba values (1,10),(2,20);
insert into tbb values (1,11),(2,22);
insert into tbc values (1),(2);
insert into tbd values (1),(2);
create view TWO as
  select tba.id as id, tba.x as x from tba join tbc on tba.id = tbc.id
  union exists left as inL, exists right as inR
  select tbb.id as id, tbb.x as x from tbb join tbd on tbb.id = tbd.id;

select is_insertable_into, is_updatable, is_deletable from view_info('TWO');
→ [{"is_insertable_into":"YES","is_updatable":"YES","is_deletable":"YES"}]

-- The left join branch's rows (from tba) and the right's (from tbb) carry different x values, so
-- each view row is a member of exactly one branch.
select id, x, inL, inR from TWO order by id, x;
→ [{"id":1,"x":10,"inL":true,"inR":false},{"id":1,"x":11,"inL":false,"inR":true},{"id":2,"x":20,"inL":true,"inR":false},{"id":2,"x":22,"inL":false,"inR":true}]

-- UPDATE both branches at once (where id=1 captures one row per branch): the left fan updates
-- tba.x (via `__vmupd_keys$1`), the right fan updates tbb.x (via `__vmupd_keys$2`) — distinct
-- inner captures, no collision.
update TWO set x = x + 100 where id = 1;
select id, x from tba order by id;
→ [{"id":1,"x":110},{"id":2,"x":20}]
select id, x from tbb order by id;
→ [{"id":1,"x":111},{"id":2,"x":22}]

-- DELETE both branches at once (where id=2 captures one row per branch): each branch builds its
-- own inner capture (`__vmupd_keys$1` / `__vmupd_keys$2`) and the lenient inner-join delete fans
-- to BOTH sides, so the joined row id=2 leaves all four base tables. Proves both inner captures
-- materialize and tear down for a DELETE (the ordered-capture substrate, not just UPDATE).
delete from TWO where id = 2;
select id, x from tba order by id;
→ [{"id":1,"x":110}]
select id from tbc order by id;
→ [{"id":1}]
select id, x from tbb order by id;
→ [{"id":1,"x":111}]
select id from tbd order by id;
→ [{"id":1}]

-- INSERT into BOTH join branches at once (inL=true, inR=true): each branch builds its OWN nested
-- shared-surrogate envelope under a DISTINCT identity descriptor — the left lands tba+tbc, the right
-- lands tbb+tbd — proving two nested envelope children never collide (`set-op-write-multisource-leg-insert`).
insert into TWO (id, x, inL, inR) values (7, 70, true, true);
select id, x from tba order by id;
→ [{"id":1,"x":110},{"id":7,"x":70}]
select id from tbc order by id;
→ [{"id":1},{"id":7}]
select id, x from tbb order by id;
→ [{"id":1,"x":111},{"id":7,"x":70}]
select id from tbd order by id;
→ [{"id":1},{"id":7}]

-- ===================================================================
-- Membership `set <joinFlag> = true` FLIP into a join branch (`set-op-write-multisource-leg-insert`):
-- the nested envelope source reads the OUTER `__vmupd_keys` capture (`where not k.<flag>`), so a
-- captured row ABSENT from the join branch inserts into its base tables, while a row already present
-- is a clean no-op. The outer capture is materialized before the nested insert (Halloween-safe).
-- A dedicated fixture (the join branch and plain branch source DISTINCT base tables) so the flip's
-- insert does not collide with the plain branch's own base row.
-- ===================================================================
create table mf_p (id integer primary key, x integer);
create table mfj1 (id integer primary key, x integer);
create table mfj2 (id integer primary key);
insert into mf_p values (1,10),(2,20);
insert into mfj1 values (1,10);
insert into mfj2 values (1);
create view MF as
  select mfj1.id as id, mfj1.x as x from mfj1 join mfj2 on mfj1.id = mfj2.id
  union exists left as inJ, exists right as inP
  select id, x from mf_p;

select is_insertable_into, is_updatable, is_deletable from view_info('MF');
→ [{"is_insertable_into":"YES","is_updatable":"YES","is_deletable":"YES"}]

-- id=1 is a member of BOTH branches; id=2 is only in the plain branch (mfj1/mfj2 have no id=2).
select id, x, inJ, inP from MF order by id;
→ [{"id":1,"x":10,"inJ":true,"inP":true},{"id":2,"x":20,"inJ":false,"inP":true}]

-- Flip inJ=true on the captured-ABSENT row (id=2): it inserts into the join branch's base tables.
update MF set inJ = true where id = 2;
select id, x from mfj1 order by id;
→ [{"id":1,"x":10},{"id":2,"x":20}]
select id from mfj2 order by id;
→ [{"id":1},{"id":2}]

-- Flip inJ=true on a row already present in the join branch (id=1): a clean no-op (`where not k.inJ`),
-- so mfj1/mfj2 are untouched.
update MF set inJ = true where id = 1;
select id, x from mfj1 order by id;
→ [{"id":1,"x":10},{"id":2,"x":20}]
select id from mfj2 order by id;
→ [{"id":1},{"id":2}]

-- ===================================================================
-- Combined data-fan + join-flip in ONE update (`set x = …, inJ = true`)
-- (`set-op-write-multisource-leg-insert`): the data assignment and the membership
-- flip coexist on one plan — the data fan rewrites the present (plain-leg) row while
-- the flip's nested envelope inserts the join branch's base tables with the SAME
-- assigned value folded into its projection (via `qualifyDataRefsWithCapture`).
-- ===================================================================
create table cf_p (id integer primary key, x integer);
create table cfj1 (id integer primary key, x integer);
create table cfj2 (id integer primary key);
insert into cf_p values (1,10),(2,20);
insert into cfj1 values (1,10);
insert into cfj2 values (1);
create view CF as
  select cfj1.id as id, cfj1.x as x from cfj1 join cfj2 on cfj1.id = cfj2.id
  union exists left as inJ, exists right as inP
  select id, x from cf_p;

select id, x, inJ, inP from CF order by id;
→ [{"id":1,"x":10,"inJ":true,"inP":true},{"id":2,"x":20,"inJ":false,"inP":true}]

-- On the captured-absent row id=2: set x=99 AND flip inJ=true. The flip's join-leg
-- insert carries the assigned x=99 (NOT the captured x=20), and the plain leg's own
-- row is data-updated to x=99 too.
update CF set x = 99, inJ = true where id = 2;
select id, x from cfj1 order by id;
→ [{"id":1,"x":10},{"id":2,"x":99}]
select id from cfj2 order by id;
→ [{"id":1},{"id":2}]
select id, x from cf_p order by id;
→ [{"id":1,"x":10},{"id":2,"x":99}]

-- ===================================
-- RETURNING <q>.* qualifier validation through updatable views.
-- A wrong qualifier (bogus.*) must error with the same diagnostic as the base-table
-- path ("Table '<q>' not found in FROM clause for qualified RETURNING *").
-- A correct <view>.* qualifier must still expand normally.
-- ===================================

-- single-source filter view fixtures
create table qv_men (name text primary key, color text);
create view qv_green as select name, color from qv_men where color = 'green';
insert into qv_men values ('Alice', 'green'), ('Bob', 'blue');
-- run

-- single-source INSERT: wrong qualifier errors
insert into qv_green (name) values ('Carl') returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

-- single-source UPDATE: wrong qualifier errors
update qv_green set color = 'green' where name = 'Alice' returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

-- single-source DELETE: wrong qualifier errors
delete from qv_green where name = 'Alice' returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

-- regression: correct view-name qualifier still expands (must not break)
insert into qv_green (name) values ('Dave') returning qv_green.*;
→ [{"name":"Dave","color":"green"}]

update qv_green set color = 'green' where name = 'Dave' returning qv_green.*;
→ [{"name":"Dave","color":"green"}]

delete from qv_green where name = 'Dave' returning qv_green.*;
→ [{"name":"Dave","color":"green"}]

-- multi-source join view fixtures
create table qv_p (pid integer primary key, label text);
create table qv_c (cid integer primary key, pref integer, note text,
    foreign key (pref) references qv_p(pid));
insert into qv_p values (1, 'L1'), (2, 'L2');
insert into qv_c values (10, 1, 'x'), (20, 2, 'y');
create view qv_join as
    select c.cid as cid, c.note as note, p.label as label
    from qv_c c join qv_p p on p.pid = c.pref;
-- run

-- multi-source UPDATE: wrong qualifier errors
update qv_join set note = 'X' where cid = 10 returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

-- multi-source DELETE: wrong qualifier errors
delete from qv_join where cid = 20 returning bogus.*;
-- error: not found in FROM clause for qualified RETURNING *

-- regression: correct view-name qualifier still expands through the join view
update qv_join set note = 'X2' where cid = 10 returning qv_join.*;
→ [{"cid":10,"note":"X2","label":"L1"}]

delete from qv_join where cid = 10 returning qv_join.*;
→ [{"cid":10,"note":"X2","label":"L1"}]

-- ===================================================================
-- Standalone non-equi (theta) INNER join view: `isInnerJoinBody` keys only on `joinType`,
-- so a join whose ON is a non-equi predicate is admitted for UPDATE / DELETE exactly like an
-- equi-join. Static surfaces: is_insertable_into=YES, is_updatable=YES, is_deletable=YES.
-- Only rows where the theta predicate holds are visible (id=1: x=10, 5<10<15 → visible).
-- ===================================================================
create table nj_a (id integer primary key, x integer);
create table nj_b (id integer primary key, lo integer, hi integer);
insert into nj_a values (1,10),(2,20);
insert into nj_b values (1,5,15);
create view NJV as
  select nj_a.id as id, nj_a.x as x from nj_a join nj_b on nj_a.x > nj_b.lo and nj_a.x < nj_b.hi;

select is_insertable_into, is_updatable, is_deletable from view_info('NJV');
→ [{"is_insertable_into":"YES","is_updatable":"YES","is_deletable":"YES"}]

-- Only id=1 is visible (x=10 satisfies 5 < x < 15).
select id, x from NJV order by id;
→ [{"id":1,"x":10}]

-- UPDATE through the non-equi join: x maps to nj_a.x.
update NJV set x = 12 where id = 1;
select id, x from nj_a order by id;
→ [{"id":1,"x":12},{"id":2,"x":20}]

-- DELETE through the non-equi join: id=1 (x=12 still in range) is removed from nj_a.
delete from NJV where id = 1;
select id, x from nj_a order by id;
→ [{"id":2,"x":20}]

-- ===================================================================
-- Membership set-op with a non-equi (theta) INNER join leg: the join leg composes for
-- UPDATE / DELETE (is_updatable=YES, is_deletable=YES). INSERT is deferred precisely BECAUSE the
-- join is non-equi: `setOpJoinLegsInsertable` probes the leg with `analyzeMultiSourceInsert`,
-- which rejects a non-equi ON (`unsupported-join`) → is_insertable_into=NO. (An equi-join
-- membership leg with a qualifying shared key WOULD report YES — `set-op-write-multisource-leg-insert`.)
-- Proves the flag-less and membership paths agree on non-equi inner-join admittance for UPDATE/DELETE.
-- ===================================================================
create table nmj_a (id integer primary key, x integer);
create table nmj_b (id integer primary key, lo integer, hi integer);
create table nmj_p (id integer primary key, x integer);
insert into nmj_a values (1,10),(2,20);
insert into nmj_b values (1,5,15);
insert into nmj_p values (3,30);
create view NMV as
  select nmj_a.id as id, nmj_a.x as x
    from nmj_a join nmj_b on nmj_a.x > nmj_b.lo and nmj_a.x < nmj_b.hi
  union exists left as inJ, exists right as inP
  select id, x from nmj_p;

select is_insertable_into, is_updatable, is_deletable from view_info('NMV');
→ [{"is_insertable_into":"NO","is_updatable":"YES","is_deletable":"YES"}]

-- id=1 is visible through the join leg (x=10 satisfies 5<10<15); id=3 through the plain leg.
select id, x, inJ, inP from NMV order by id;
→ [{"id":1,"x":10,"inJ":true,"inP":false},{"id":3,"x":30,"inJ":false,"inP":true}]

-- UPDATE through the non-equi join leg.
update NMV set x = 13 where inJ = true;
select id, x from nmj_a order by id;
→ [{"id":1,"x":13},{"id":2,"x":20}]

-- DELETE through the non-equi join leg (id=1 still visible: x=13 satisfies 5<13<15).
delete from NMV where inJ = true;
select id, x from nmj_a order by id;
→ [{"id":2,"x":20}]
