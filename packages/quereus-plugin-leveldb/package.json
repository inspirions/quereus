{
  "name": "@quereus/plugin-leveldb",
  "version": "4.11.0",
  "type": "module",
  "description": "LevelDB storage plugin for Quereus - Node.js persistent storage",
  "keywords": [
    "quereus",
    "quereus-plugin",
    "sql",
    "database",
    "storage",
    "leveldb",
    "kv-store",
    "persistent"
  ],
  "publisher": "Got Choices Foundation",
  "repository": {
    "type": "git",
    "url": "https://github.com/gotchoices/quereus.git",
    "directory": "packages/quereus-plugin-leveldb"
  },
  "license": "MIT",
  "main": "dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist",
    "!**/*.tsbuildinfo"
  ],
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    },
    "./plugin": {
      "types": "./dist/src/plugin.d.ts",
      "import": "./dist/src/plugin.js"
    }
  },
  "peerDependencies": {
    "@quereus/isolation": "workspace:^",
    "@quereus/quereus": "workspace:^",
    "@quereus/store": "workspace:^"
  },
  "engines": {
    "quereus": "^0.24.0"
  },
  "quereus": {
    "provides": {
      "vtables": [
        "store"
      ]
    },
    "settings": [
      {
        "key": "basePath",
        "label": "Base Path",
        "type": "string",
        "default": "./data",
        "help": "Directory of the single shared LevelDB database"
      },
      {
        "key": "createIfMissing",
        "label": "Create If Missing",
        "type": "boolean",
        "default": true,
        "help": "Create the database if it doesn't exist"
      },
      {
        "key": "syncCommits",
        "label": "Sync Commits",
        "type": "boolean",
        "default": true,
        "help": "fsync each transaction commit so it survives power loss (slower commits when on)"
      },
      {
        "key": "moduleName",
        "label": "Module Name",
        "type": "string",
        "default": "store",
        "help": "Name to register the virtual table module under"
      },
      {
        "key": "isolation",
        "label": "Transaction Isolation",
        "type": "boolean",
        "default": true,
        "help": "Enable transaction isolation (read-your-own-writes, snapshot isolation)"
      }
    ]
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "lint": "echo 'No lint configured'",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "cd ../.. && node --import ./packages/quereus-plugin-leveldb/register.mjs node_modules/mocha/bin/mocha.js \"packages/quereus-plugin-leveldb/test/**/*.spec.ts\" --reporter min --colors"
  },
  "dependencies": {
    "@quereus/quereus": "workspace:^",
    "@quereus/store": "workspace:^",
    "abstract-level": "^3.1.0",
    "classic-level": "^3.0.0"
  },
  "devDependencies": {
    "@quereus/isolation": "*",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.9.3",
    "chai": "^6.2.2",
    "mocha": "^11.7.6",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
