description: The rules the view-updateability code must obey are now written up as eight numbered, code-pointing invariants in the repo-wide register, so a reviewer can read them against the implementation in one pass.
files:
  - docs/invariants.md (`## VU — View updateability`, VU-001..VU-008)
  - scripts/check-docs.mjs (VU added to the invariant-heading regex)
  - docs/doc-conventions.md (VU added to the area list)
  - docs/view-updateability.md, docs/vu-roundtrip.md, docs/vu-inverses.md, docs/vu-mutation-context.md (back-links)
  - docs/vu-operators.md (§ Distinct + § Sort, Limit, Offset reconciled to VU-008 — fixed this pass)
  - packages/quereus/test/property.spec.ts (§ View Round-Trip Laws — the guard the round-trip invariants point at; unchanged)
----

## What shipped

Extracted the normative view-updateability claims into `docs/invariants.md` as a new `VU`
area (VU-001..VU-008), mirroring the `MV-*` shape: each with `code:` / exactly one `guard:` /
`doc:`, each ≤120 words. Registered `VU` in the checker heading regex and the doc-conventions
area list. Added `> **Invariant:**` back-links at the five owning satellite sections. The
implement stage touched docs + the checker only — no engine source or test.

This review made one inline fix (below) to `docs/vu-operators.md`; still docs-only.

## Review findings

**Diff read fresh, before the handoff.** The change is the VU invariant register + checker
area registration + satellite back-links. Scrutinized for accuracy against the engine, not
just format.

**Correctness of the invariants — checked, all hold.**
- All 12 `code:` pointers resolve to **real function definitions** (not merely a substring or
  comment surviving the checker's loose match): `buildViewMutation`, `rewriteViewInsert`,
  `analyzeView`, `classifyViewBody`, `propagate`, `raiseMutationDiagnostic`, `resolveBaseSite`,
  `deriveViewColumns`, `classifyInvertibility`, `validateAuthoredInverses`,
  `analyzeMultiSourceInsert`, `analyzeDecompositionInsert` — each confirmed as an `export
  function`/`function` at a named line.
- Every diagnostic reason the prose names is a real emitted `reason:` string
  (`no-inverse`, `predicate-contradiction`, `recursive-cte`, `unsupported-limit`,
  `unsupported-distinct`).
- VU-008's prose matches the code comment at `single-source.ts:523-528` verbatim in intent
  (classifier passes LIMIT/OFFSET/DISTINCT through so lineage reaches the base, then the
  mutation rejects rather than widen).
- The guard file `test/logic/93.4-view-mutation.sqllogic` exists; the guard title
  `View Round-Trip Laws` is a real `describe` block; running it gives **61 passing** (PutGet /
  GetPut / lineage-agreement / reject-don't-widen / shared-key families all present and green).

**Major findings: none.** No new tickets filed.

**Minor finding — FIXED inline this pass.** `docs/vu-operators.md` § Distinct and
§ Sort, Limit, Offset described a *passthrough / fan-out* mutation semantics ("Mutations apply
to all base rows that collapse to the affected view row", "Pure passthrough. `order by` and
`limit` do not affect propagation") that **contradicts shipped VU-008** — the engine *rejects*
a `distinct` / `limit` / `offset` body with `unsupported-distinct` / `unsupported-limit`. This
also contradicted the *same doc's* § Non-decomposable CTE bodies (line 293), which already
states those bodies reject. This is a live wrong-about-shipped-behavior contradiction, not a
conditional/aspirational note, so it is a defect, not a tripwire. The implementer parked it as
"observation, not fixed" to keep the diff scoped; I fixed it because a reader landing on
§ Distinct was told the opposite of what the engine does. Rewrote both sections to state the
read-side passthrough vs. the mutation-side reject, with VU-008 back-links. Also resolved the
implementer's open question about VU-008's `doc:` target: it stays pointed at
`vu-roundtrip.md § The three round-trip laws` (which states reject-don't-widen); the
now-corrected vu-operators sections agree with it.

**Tripwires (noticed, parked in-place, not filed as tickets):**
- VU-008's `code:` line names only `single-source.ts` `analyzeView`, but a **multi-source
  join** body also rejects LIMIT/DISTINCT at `multi-source.ts:1082/1089`. Not a defect — one
  representative code site is the register convention, and the single guard covers both paths.
  No change; noting so a future editor who moves the single-source reject knows the invariant
  has a second home.
- **Guard coarseness (VU-003/004/005/008 share the guard string `View Round-Trip Laws`, three
  share one `doc:` anchor).** The checker verifies only that the string appears in the spec
  file, not that a distinct `it()` reds for each law. Confirmed the block *does* contain
  per-law tests (the 61 passing split into PutGet / GetPut / lineage-agreement /
  reject-don't-widen families, plus negative self-tests that red on injected violations), so
  the guards are honest though coarse. This matches the ticket's self-guarding allowance; a
  dedicated per-law guard is a fair future ask but is conditional ("*if* the shared block ever
  masks a single-law regression"), so recorded here rather than filed.
- **VU-001 / VU-002 guards are the soft ones.** VU-001 uses a path-only sqllogic guard;
  VU-002 leans on the property block's `expectMutationReject` cases. Neither is a dedicated
  "rewrite-to-base equivalence" or "diagnostic completeness" test. Acceptable per the ticket;
  a harder guard is optional future work, not a defect.

**Docs reflect reality:** confirmed — every file the change touched was read, and the one
satellite that contradicted the new register (`vu-operators.md`) was reconciled this pass.

## Validation performed

- `node scripts/check-docs.mjs` — Checks A (links/anchors, incl. the new VU-008 links added to
  vu-operators.md), B (invariant format — all 8 bodies ≤120 words, IDs ascend, one guard each),
  D (stability), and `selfTest()` **green**. Only failures are the 3 pre-existing size-ratchet
  overages (see below).
- Round-trip guard — `node test-runner.mjs --grep "View Round-Trip Laws"`: **61 passing**.
- `yarn workspace @quereus/quereus run lint` — **exit 0** (docs-only change; no TS affected).

## Pre-existing failures (not this ticket)

`node scripts/check-docs.mjs` reports 3 size-ratchet overages — `docs/runtime.md`,
`docs/schema.md`, `docs/sync.md` — none in this ticket's diff, all already tracked in
`tickets/.pre-existing-known.md` under the in-flight slug `docs-megadoc-ratchet-overage`.
Check C (ratchet) is independent of the invariant-heading regex changed here. Not re-reported
per the pre-existing protocol. The VU docs edited here (including vu-operators.md, which has no
ratchet entry) stayed within budget — no new ratchet failure.
