description: Code comments that pointed at the old single view-updateability doc by section name were repointed to the smaller split files, so a reader who follows a comment lands on the right page.
prereq:
files:
  - packages/quereus/src/planner/mutation/set-op.ts
  - packages/quereus/src/planner/building/view-mutation-builder.ts
  - packages/quereus/test/logic/93.6-set-op-flagless-write.sqllogic
  - packages/quereus/src/planner/**/*.ts (37 files repointed by the implement stage)
  - docs/view-updateability.md (overview) + docs/{vu-operators,vu-inverses,vu-mutation-context,vu-roundtrip,vu-setops}.md
----

## What the work was

`docs-vu-split` broke the old single `docs/view-updateability.md` into an overview plus
five satellites (`vu-operators`, `vu-inverses`, `vu-mutation-context`, `vu-roundtrip`,
`vu-setops`). Engine code comments still cited moved sections by the old file path. The
implement stage repointed each such comment (37 files, comment-text only, `yarn build`
green proves nothing escaped a comment).

## Review findings

Reviewed the full implement diff (commit `d4ac3ed4`) against the **actual** current
headings of every satellite doc — not the ticket's reproduced tables — plus the pre-split
doc (`967c3f17~1`) to distinguish split-caused drift from pre-existing drift.

### Checked, correct — left as-is
- **`§ Authored inverses`, `§ Scalar Invertibility`, `§ View defaults`, `§ Tags` →
  `vu-inverses.md`** — all four are real headings there; the overview keeps only "Moved to …"
  pointer stubs, so the satellite is the authoritative target. Correct.
- **`§ Mutation Context` → `vu-mutation-context.md`** — the doc title (`# Mutation Context`);
  overview stub only. Correct.
- **`§ Round-Trip Laws and the Derived Backward Walk`, `§ The predicate-honest complement`
  → `vu-roundtrip.md`** — both real headings; overview stubs. Correct.
- **`§ Selection`, `§ Inner Join`, `§ Outer Joins`, `§ Common Table Expressions`,
  `§ Inline subquery DML target` → `vu-operators.md`** — all present as headings
  (`Selection (σ)`, `Inner Join`, `Outer Joins`, `Common Table Expressions and the CTE-name
  DML target`, `Inline subquery DML target`). Correct.
- **Sections that STAYED in the overview** (`§ Philosophy`, `§ returning`, `§ Multi-Base-Table
  Mutations`, `§ The Update Site Model`, `§ Mutation Propagation`, `§ Current limitations`,
  bare whole-doc refs) — all still real content in `view-updateability.md`; correctly left
  pointing there. The two-target splits (e.g. `§ Inner Join` → operators + `§ returning` →
  overview) land each half on its real home. Verified `view-complement.ts`'s Philosophy /
  complement split — awkward mid-sentence path insertion but factually correct.

### Found and FIXED inline (minor — comment-only, zero behavioral change)
- **`§ Set Operations` was mis-repointed to `vu-operators.md` for the five citations that are
  actually about set-op *write* decomposition** — which lives in `vu-setops.md`, not
  `vu-operators.md`. The implement note's judgment call ("these mean per-operator
  union/intersect/except writability") mischaracterized the code: `set-op.ts`'s comment reads
  "Set-operation membership-column write decomposition — the first set-op view writability",
  and `vu-setops.md § Set-operation membership writes` literally opens by naming
  `planner/mutation/set-op.ts` and `building/view-mutation-builder.ts`; the `93.6` test's
  `set-op-flagless-predicate-honest-writes` / `set-op-flagless-range-sigma-oracle` /
  `set-op-projected-constant-product-coordinate` tags are all documented in `vu-setops.md`.
  `vu-operators.md` has **no** `§ Set Operations` heading and nothing about membership-column
  writes or the flag-less oracle — a reader following those comments landed on the wrong page,
  exactly the drift this ticket exists to kill. Repointed all five to
  `docs/vu-setops.md § Set-operation membership writes`:
  - `src/planner/mutation/set-op.ts:25`
  - `src/planner/building/view-mutation-builder.ts:501`
  - `test/logic/93.6-set-op-flagless-write.sqllogic:6, :590, :714`

### Found, out of scope — NOT changed (recorded as observation)
- **`decomposition.ts:30-31` cites `docs/view-updateability.md § Decomposition put fan-out`,
  a `§` that matches no heading in any doc.** Confirmed pre-existing: the comment predates the
  split (present at `967c3f17~1`), and even in the pre-split doc "Decomposition put fan-out"
  was only an *Implementation Map table row*, never a section heading — so this ref was always
  dangling. It is not a moved-section repoint, so it is outside this ticket's scope, and the
  correct target is genuinely ambiguous (the comment already cites `docs/lens.md § The Default
  Mapper` immediately before it, which arguably covers it). Left untouched — too trivial and
  too pre-existing to warrant a ticket; noted here so a future reader knows it is a known,
  harmless cosmetic inaccuracy, not fresh drift.
- **`multi-source.ts:2669` splits `§ Inner Join, cross-source set` → `vu-operators.md § Inner
  Join` + `view-updateability.md cross-source set`.** Cross-source `set` is discussed in BOTH
  places — a detailed mechanism paragraph in `vu-operators.md § Inner Join` (right next to the
  `§ Inner Join` half the comment already points there) and a one-line capability bullet under
  `view-updateability.md § Current limitations`. The overview half lands on a real mention, so
  it is not a dead link; it is merely slightly further from the mechanism than ideal. Not worth
  churning a correct-enough comment.

### Tripwires
- None. The two observations above are present-tense minor inaccuracies (one pre-existing, one
  cosmetically-suboptimal), not conditional "fine-now-until-X" concerns, so neither warrants a
  code `NOTE:` tag.

## Validation performed (post-fix)
- `yarn build` → EXIT 0 (all packages — the tell that nothing escaped a comment).
- `yarn lint` → EXIT 0 (quereus eslint + `tsc -p tsconfig.test.json` over spec files).
- `yarn workspace @quereus/quereus test` → **6934 passing, 13 pending, 0 failing** (~1m). The
  `.sqllogic` edits are `--` comments (ignored by the parser); the `.ts` edits are doc comments.
- Post-fix re-grep: no `§ Set Operations` citation remains anywhere; all five now resolve to a
  real `vu-setops.md` heading.

No pre-existing test failures encountered.
