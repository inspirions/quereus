description: Optimizer developer guide now documents the one agreed way to detect node kinds/capabilities, and a lint guard blocks the fragile old style from returning to the canonical file.
files: docs/optimizer-conventions.md, packages/quereus/src/planner/framework/README.md, packages/quereus/eslint.config.mjs, packages/quereus/src/planner/framework/characteristics.ts
----

## What changed

Docs + one file-scoped lint override. **No production code changed** — `characteristics.ts` untouched (confirmed absent from the implement diff `bc02d2b9`).

- `docs/optimizer-conventions.md` — full rewrite. Retitled "Node Discrimination & Characteristics". Replaces the old blanket anti-`instanceof` guidance with the agreed standard: three questions → three mechanisms (`instanceof` for concrete class identity, branded marker interface + `CapabilityDetectors` guard for cross-class capability, `PlanNodeCharacteristics` over `PlanNode.physical` for physical properties; `nodeType` = dispatch/serialization/EXPLAIN only). New anti-pattern is now the duck-typed `as any` property-presence detector. FD/EC/bindings section preserved verbatim.
- `packages/quereus/src/planner/framework/README.md` — added intent paragraph (instanceof vs capability guard) + cross-reference link to the conventions doc.
- `packages/quereus/eslint.config.mjs` — per-file override raising `@typescript-eslint/no-explicit-any` from `warn` to `error`, scoped to exactly `src/planner/framework/characteristics.ts`.

## Review findings

Adversarial pass over the implement diff (`bc02d2b9`), read before the handoff summary. All acceptance checks re-verified independently.

**Doc ⇄ code accuracy (checked, correct).** Read `characteristics.ts` whole and cross-checked every doc claim against it:
- `PlanNodeCharacteristics.hasSideEffects(node)` takes a node (not `node.physical`) — matches `characteristics.ts:31`. The rewrite correctly dropped the old `= PlanNode.hasSideEffects` alias signature.
- `AggregationCapable` brand `isAggregationCapable`, `getGroupingKeys`, guard `CapabilityDetectors.isAggregating` — all match (`characteristics.ts:233-241, 401-403`).
- Caching worked-example (`CapabilityDetectors.isCached(node) && node.isCached()`, `isRelational`, `hasSideEffects` gating) matches `CachingAnalysis.isCacheable` verbatim (`characteristics.ts:464-483`).
- `nodeType` "not 1:1 with classes" claim is backed by real code — `WindowFunctionCapable`/`AggregateFunctionCapable` both share `nodeType === ScalarFunctionCall` and are told apart by brand (`characteristics.ts:337-360`).

**Lint (checked, passes).** `yarn workspace @quereus/quereus run lint` → exit 0 (runs `eslint 'src/**/*.ts' 'test/**/*.ts'` **and** `tsc -p tsconfig.test.json --noEmit`; both clean).

**Override bites + scope (checked, correct).** Independently re-verified by temp-edit, reverted immediately (tree left clean):
- Appended `const _lintTripwire = (null as any);` to `characteristics.ts` → `error … @typescript-eslint/no-explicit-any`, exit 1. ✔ bites.
- Appended the same `as any` to a *different* planner file (`src/planner/framework/registry.ts`) → `warning`, not error. ✔ scope is exactly the one file; other planner files stay at `warn`.

**Cross-reference integrity (checked, correct).** README relative path `../../../../../docs/optimizer-conventions.md` resolves (confirmed via `realpath`). Anchor `#node-discrimination-three-questions-three-mechanisms` matches the single `## Node discrimination: three questions, three mechanisms` heading (no duplicate heading → no `-1` suffix). Grepped all inbound references to `optimizer-conventions` repo-wide: no other link targets a now-deleted anchor, so the restructure orphaned nothing. `src/planner/rules/README.md:91` ("Copy the rule template from …") still satisfiable — the rewritten doc retains rule skeletons under § Rule Development Guidelines.

**Parked follow-up debt (checked, exists).** Handoff's claimed backlog slug `debt-planner-duck-type-lint` is a naming slip — the actual parked ticket is `tickets/backlog/debt-planner-discrimination-hardening.md`, which covers **both** deferred pieces (the planner-wide `any` audit and the custom AST "no duck-typed detector" lint rule). Work is correctly parked; only the handoff's name was wrong. Not worth editing a superseded ticket.

**Findings requiring action: none.** No minor fixes applied (nothing found to fix), no major tickets filed (no defects), no tripwires recorded (the doc is prose + accurate examples; the only conditional concerns — `any` audit, broader lint rule — are already captured in the backlog debt ticket above, which is the right home).

**Non-goals confirmed intentional.** No codebase-wide `any` purge; no custom AST lint rule; historical review artifacts (`docs/review.html`) left as-is (rewriting them would falsify history). All consistent with the ticket's stated scope.
